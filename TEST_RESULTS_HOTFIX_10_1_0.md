# نتائج اختبارات Hotfix 10.1.0 — واجهة ضريبة القيمة المضافة + إزالة "قريبًا" للشحنات

توثيق تشغيلات فعلية على فرع `hotfix/10.1.0` فوق خط الأساس `8309e0e`
(دمج Phase 10).

> **نتائج GitHub CI: غير متاحة.** الفرع **لم يُدفع** ولم يُنشأ له PR، فلا يوجد
> تشغيل CI لهذا الإصلاح بعد. كل ما يلي من تشغيلات محلية فعلية.

---

## 1) نطاق التغيير

| البند | الحالة |
|---|---|
| ترحيلات جديدة | **لا شيء** |
| تعديل على `0001–0236` | **لا شيء** |
| تعديل على `supabase/seed.sql` | **لا شيء** |
| أي ملف داخل `supabase/` | **لم يتغيّر إطلاقًا** |
| معادلات حساب الضريبة | **لم تُمَس** |
| منطق أعمال الشحن | **لم يُمَس** |

الإصلاح واجهة بحتة تعيد استخدام خلفية مكتملة منذ الترحيلة 0058.

---

## 2) لماذا لم تُعَد تشغيل مجموعات SQL كاملة

قاعدة القرار: **لم يتغيّر أي ملف قاعدة بيانات أو SQL** — لا ترحيلة، ولا دالة،
ولا `seed.sql`، ولا أي ملف تحت `supabase/`. المخطط والدوال والثوابت التي
تغطّيها اختبارات SQL هي **بالضبط** ما جرى التحقق منه على `main` عند `8309e0e`
(29/29 عادية · 7/7 تزامن · 15/15 ترقية)، فإعادة تشغيلها هنا كانت ستُعيد إنتاج
النتائج ذاتها على مخطط مطابق بايت ببايت.

ما شُغِّل بدلًا من ذلك — **فحص تغطية SQL** (نفس منطق خطوة CI):

- عدد ملفات `*.test.sql`: **51** (بلا تغيير)
- **الاختبارات اليتيمة: صفر** — كل ملف ترقية ما زال مربوطًا بـrunner في
  `scripts/`

وبما أن الـRPCs المستخدَمة (`create_vat_rate_version`/`cancel_vat_rate_version`)
لم تتغيّر، فثوابتها الخلفية (القفل المالي الحصري، رفض تاريخ سريان سابق، رفض
إلغاء إصدار بدأ سريانه، إعادة فتح السابق عند الإلغاء) مُغطّاة أصلًا في
`financial_integrity_*` ولم يظهر ثابت خلفي جديد يستحق اختبارًا. **لم يُخترع أي
اختبار SQL بلا داعٍ.**

---

## 3) نتائج الاختبارات

| الفحص | الأمر | النتيجة |
|---|---|---|
| Vitest | `npx vitest run` | **543 / 543 ناجح** عبر **39** ملفًا |
| TypeScript | `npx tsc --noEmit` | **صفر أخطاء** |
| ESLint | `npx eslint .` | **صفر أخطاء** (4 تحذيرات قائمة مسبقًا في `adjustments-entry-form-zero-charge-state-machine.test.tsx`، لم تُمس) |
| البناء الإنتاجي | `npx next build` | **نجح** — **54 / 54** صفحة (كانت 53) |
| فحص تغطية SQL | مطابق لخطوة CI | **صفر اختبارات يتيمة** |

الفارق 526 → 543 هو ملف Vitest الجديد (17 اختبارًا).

### تفصيل `tests/vat-rates-actions-and-navigation.test.ts`

- **حدود الصلاحيات:** الإنشاء والإلغاء يبوّبان على `vat_rates.manage` حصرًا،
  ولا يستدعيان `vat_rates.view` إطلاقًا.
- **إعادة استخدام الخلفية:** يُثبَت أن الإجراءات تستدعي
  `create_vat_rate_version` و`cancel_vat_rate_version` **القائمتين** بالوسائط
  المتوقعة — لا خلفية جديدة أُدخلت.
- **دقة النسبة:** النسبة تصل الـRPC **كنص** (`typeof === "string"`)
  و**بايت ببايت** (`"15.375"` تبقى `"15.375"`، و`"15.000"` يحتفظ بصفره) — وهو
  ما يحرسه هذا الملف تحديدًا ضد أي `Number()` مستقبلي.
- **التحقق قبل القاعدة:** رفض السالب، وغير الرقمي، وتاريخ السريان الفارغ،
  وتجاوز 3 منازل عشرية (مطابقة `numeric(6,3)`) — كلها تُرفض دون بلوغ الـRPC.
- **تمرير رسائل القاعدة:** رفض «تاريخ سريان سابق» و«إصدار بدأ سريانه» يصل
  المستخدم كما هو لا كرسالة عامة.
- **التنقّل:** «الشحنات» لم تعد `comingSoon`، وما زالت تفرض `shipments.view`،
  ولا يوجد أي عنصر تنقّل موسوم زورًا بـ«قريبًا».

---

## 4) خطأ التُقط أثناء التطوير

الصيغة الأولى للإجراء مرّرت `formData.get("notes")` مباشرة إلى Zod. الدالة
تُرجع `null` عند غياب الحقل، ومخطط الملاحظات يقبل `string | undefined` لا
`null` — فكان أي استدعاء بلا حقل ملاحظات يُرفَض بخطأ تحقّق مضلِّل. والأهم أن
ذلك جعل ثلاثة اختبارات «تنجح للسبب الخطأ» (كانت تُرفض بسبب الملاحظات لا بسبب
القاعدة التي تدّعي اختبارها). صُحِّح بتطبيع `?? undefined` في الإجراء، فصارت
اختبارات التحقق تعزل القاعدة المقصودة فعلًا.

---

## 5) مسائل مفتوحة

لا توجد.

**ملاحظة مُسجَّلة (خارج نطاق هذا الإصلاح):** `backups.manage` ما زالت صلاحية
بلا ميزة، و2FA مفتاح معطَّل صراحةً بنص أمين — كلاهما دَين تقني مُسجَّل ولا
يحجب شيئًا.
