# نتائج الاختبار الفعلية — Final Hotfix 3.2.1

كل الأرقام أدناه من تشغيل فعلي أخير ضد قاعدة بيانات مبنية من الصفر (`local_harness_setup.sql` + الترحيلات 0001–0081 بالترتيب + `supabase/seed.sql`)، بتاريخ هذه الجلسة. لا رقم افتراضي أو منسوخ من تشغيل سابق — كل تشغيلة أُعيدت فعليًا عند كتابة هذا الملف، بما فيها **إثبات سلبي** (Negative Proof) لكل من البندين 2 و3: نفس الاختبارين الجديدين شُغِّلا أيضًا ضد قاعدة بُنيت من 0001–0080 فقط (بلا 0081) وفشلا فعليًا بالرسالة المتوقعة تمامًا، قبل أن يُعاد بناء القاعدة بـ0081 ويُثبَت نجاحهما — أي أن كلا الاختبارين الجديدين حساس فعليًا للخلل الذي يُغلقه هذا الهوتفكس، لا مجرد اختبار شكلي (Tautology).

## 1) بناء قاعدة البيانات من الصفر

- `local_harness_setup.sql` → نجح.
- الترحيلات 0001–0081 بالترتيب دون توقف → **81/81 نجحت** (الجديدة: 0081 فقط — هذا هوتفكس بترحيلة واحدة).
- `supabase/seed.sql` → نجح.

## 2) اختبار الترقية (Foundation → latest، بلا إعادة تشغيل seed.sql)

`scripts/run_upgrade_test.sh`:

```
==> Upgrade test PASSED
=== ALL UPGRADE-FROM-0039 TESTS PASSED (0040-latest, including Phase 3 0058-0064, applied WITHOUT re-running seed.sql) ===
```

## 3) الإثبات السلبي (Negative Proof) — كلا اختباري 0081 يفشلان فعليًا بدونها

قبل تشغيل مجموعة الاختبار الكاملة، شُغِّل الاختباران الجديدان ضد قاعدة بيانات منفصلة مبنية من 0001–0080 فقط (بلا 0081):

| الاختبار | النتيجة بدون 0081 | الرسالة الفعلية |
|---|---|---|
| `sales_integrity_hotfix_3_2_1.test.sql` (A) | **فشل فعليًا** ✅ (هذا متوقَّع ومطلوب) | `FAIL A: ... يجب أن يرفعه إلى calculation_version=2 ...، وجد 1` |
| `sales_integrity_patch_3_1_concurrency.test.sql` (L1) | **فشل فعليًا** ✅ (هذا متوقَّع ومطلوب) | `FAIL L1: ... فشل بخطأ بدل النجاح بعد الانتظار — permission denied for function acquire_financial_master_lock_exclusive ...` |

هذا يثبت أن الاختبارين الجديدين حسّاسان فعليًا لوجود/غياب إصلاحات 0081 — وليسا اختبارين شكليين ينجحان دائمًا بصرف النظر عن الكود. بعد ذلك أُعيد بناء قاعدة الاختبار الرئيسية بـ0001–0081 كاملة، ونجح الاختباران فيها كما في القسم 4 أدناه.

## 4) اختبارات SQL — كل الملفات، أرقام فعلية من هذا التشغيل (تشغيل واحد متسلسل على نفس قاعدة البيانات، 0001–0081)

| الملف | النتيجة |
|---|---|
| `rls_and_permissions.test.sql` | 139/139 ✅ |
| `financial_master_data.test.sql` | 52/52 ✅ |
| `financial_integrity_patch_2_1.test.sql` | 34/34 ✅ |
| `financial_integrity_patch_2_2.test.sql` | 17/17 ✅ |
| `financial_integrity_hotfix_2_2_1.test.sql` | 14/14 ✅ |
| `financial_integrity_hotfix_2_2_2.test.sql` | 11/11 ✅ |
| `sales_core.test.sql` | 57/57 ✅ |
| `sales_integrity_patch_3_1.test.sql` (Part 1، أحادي الجلسة) | 14/14 ✅ |
| `sales_integrity_patch_3_1_concurrency.test.sql` (Part 2، تزامن حقيقي عبر `dblink`) | H1/H2/I(1)/I(2)/J/J2/**L1 جديد**/**L2 جديد**/K + تنظيف — 10/10 نقطة تحقق ✅ |
| `sales_integrity_patch_3_2.test.sql` | 15/15 ✅ |
| `sales_integrity_hotfix_3_2_1.test.sql` (**جديد بالكامل**) | 6/6 نقطة تحقق (D/A/B/E/C + الملخص النهائي) ✅ |

**المجموع: 369 نقطة تحقق SQL ناجحة في تشغيلة واحدة متسلسلة على قاعدة بيانات واحدة مبنية من الصفر** (361 من Patch 3.2 + 8 جديدة في هذا الهوتفكس: L1+L2 في ملف التزامن، وD/A/B/E/C+الملخص = 6 في الملف الجديد؛ 361 + 2 [L1,L2] + 6 [hotfix] = 369).

### تفصيل قسم L1/L2 (بند 2 — ACL القفل المالي لـ`service_role`)

- **L1** — اتصال A يُنشئ عملية بيع متعددة البنود ويبقى مفتوحًا (يحمل القفل المالي المشترك)؛ اتصال B ينفّذ `SET ROLE service_role` ثم `UPDATE` مباشر على `daily_gold_prices` لنفس العيار/التاريخ — يجب أن **يُحجب فعليًا** حتى التزام A، ثم **ينجح دون أي خطأ صلاحيات** (لا `permission denied`)، وأن يُثبَت أن التحديث طُبِّق فعليًا وأن كلا بندي عملية A استخدما نفس السعر القديم المتسق.
- **L2** — كتابة `service_role` مباشرة وعادية (بلا أي عملية بيع متزامنة) على `daily_gold_prices` يجب أن تنجح ببساطة دون أي حجب أو خطأ.

### تفصيل ملف `sales_integrity_hotfix_3_2_1.test.sql` (بند 3 — دلالة `sales_orders.calculation_version`)

- **D** — خط أساس: عملية بيع جديدة كاملة بعد هذا الهوتفكس تحمل `calculation_version=2` على مستوى الرأس والبند معًا (لم يتأثر بهذا الهوتفكس، `create_sales_order()` لم تتغيّر).
- **A** — رأس عملية بيع محاكى كـv1 (قديم) يُصبح `calculation_version=2` بعد تعديل ناجح واحد **حتى لو وصفي فقط بالكامل** (بلا أي تغيير مالي على أي بند) — لأن `update_sales_order()` تُعيد بناء `subtotal`/`gross_profit`/`payment_fee_amount`/`net_sales_profit` فعليًا في كل استدعاء ناجح.
- **B** — البند غير المتغيّر ماليًا في نفس ذلك التعديل يبقى `calculation_version=1` دون ترقية قسرية — رغم أن رأس نفس العملية أصبح `2` الآن. **رأس v2 + بند v1 ضمن نفس العملية هو السلوك المقصود والصحيح**، لا تناقضًا.
- **E** — سجل تدقيق `sale.update` لهذا التعديل يحمل `old_values.calculation_version=1` (القيمة الفعلية قبل التعديل) و`new_values.calculation_version=2` (القيمة الفعلية بعده) — لا تكرار القيمة القديمة على الجانبين كما كان الخلل قبل 0081.
- **C** — تعديل لاحق يُغيّر فعليًا مدخلات البند المالية يرفعه إلى `calculation_version=2` (سلوك بند 7 من Patch 3.2 دون أي تغيير)، ورأس العملية يبقى `2` (لم يرتد إلى 1).

## 5) اختبار HTTP/PostgREST الحقيقي

`scripts/run_postgrest_http_test.sh` — ثنائي PostgREST v12.2.3 حقيقي، اتصال HTTP فعلي:

```
=== ALL REAL HTTP/POSTGREST INTEGRATION TEST ASSERTIONS PASSED ===
```

**37/37 تأكيد ناجح** — بلا تغيير عن Patch 3.2 (لم يُضَف تأكيد HTTP جديد لهذا الهوتفكس عمدًا: بنوده الثلاثة ليس لها سطح ملاحظة إضافي عبر HTTP لم يكن مغطى أصلًا — البند 1 إصلاح React بحت، البند 2 مُثبَت عبر `dblink`/`SET ROLE service_role` مباشرة على القاعدة [PostgREST لا يستدعي أبدًا كـ`service_role` عبر HTTP بمسار اختبار موجود]، والبند 3 مُثبَت عبر SQL مباشر). هذا التشغيل يُثبت فقط أن 0081 **لم تكسر** أيًا من تأكيدات Patch 3.2 الأربعين — 37 لأن HTTP لم يتغيّر، لا نقصانًا.

## 6) فحوصات مستوى الشيفرة

| الفحص | النتيجة |
|---|---|
| `npx tsc --noEmit` | صفر أخطاء ✅ |
| `npx eslint .` | صفر أخطاء/تحذيرات ✅ |
| `npx vitest run` | **47/47** عبر 6 ملفات ✅ (45 سابقة + 2 جديدتان في `tests/sales-entry-form-conflict-reload.test.tsx`) |
| `npm run check:numeric-types` | 23/23 عمود NUMERIC خام مطابق ✅ — لا عمود جديد في هذا الهوتفكس (0081 لا تضيف أي عمود) |
| `npm run build` (Next.js/Turbopack) | نجح، 25 مسارًا ✅ |

### الإثبات السلبي لاختبار React الجديد (بند 1)

`tests/sales-entry-form-conflict-reload.test.tsx` شُغِّل أيضًا بعد إزالة `key={order.row_version}` مؤقتًا (محاكاة الكود القديم قبل هذا الهوتفكس، داخل الاختبار نفسه لا في الصفحة الفعلية) — **فشل الاختباران فعليًا**، بعرض `weight_grams`/`customer_name` القديمين رغم وصول `existingOrder` جديد بـ`row_version` أحدث (`expected document not to contain element, found <input ... value="1.0000" />`). أُعيد `key={order.row_version}` فور التحقق، ونجح الاختباران معًا — النتيجة الفعلية بعد الإصلاح مذكورة في الجدول أعلاه (47/47 عبر `npx vitest run`).

## 7) القيود الصارمة — تحقق نهائي

- **لا تعديل واحد** على أي ترحيل من 0001 إلى 0080 — ترحيلة هوتفكس جديدة واحدة فقط: `0081_hotfix_3_2_1_lock_acl_and_order_calculation_version.sql`.
- **لا بدء** لـReturns أو Shipping أو Settlements أو Inventory أو أي مرحلة جديدة.
- **كل إصلاحات 3.1 و3.2 محفوظة سليمة** — كل ملفات اختبارها (10 ملفات) أُعيد تشغيلها بالكامل ضمن نفس التشغيلة المتسلسلة أعلاه وتمر 100%، بما فيها القسمان الوحيدان اللذان تغيّرا فعليًا (I في ملف التزامن — لم يُمس، وF في ملف Patch 3.2 — لم يُمس؛ فقط أُضيفت أقسام L1/L2 الجديدة في نهاية ملف التزامن، بلا حذف أو تعديل لأي قسم موجود).
- **لا خفض Coverage ولا تغيير test expectation فقط حتى يصبح Green** — كل تعديل في هذا الهوتفكس هو إما ملف اختبار **جديد بالكامل** (`sales_integrity_hotfix_3_2_1.test.sql`) أو أقسام **مُضافة** لملف قائم (L1/L2) — لا سطر واحد من أي `assert` موجود سابقًا حُذِف أو خُفِّف.
- كل بند من الثلاثة (1–3) له اختبار فعلي مطابق، مذكور بالاسم أعلاه، مع إثبات سلبي (Negative Proof) لكل من البندين 2 و3 يثبت حساسية الاختبار للخلل الفعلي.
