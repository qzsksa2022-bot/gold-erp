# نتائج الاختبار الفعلية — Final Hotfix 4.2.1 (ترحيلات 0106–0112)

كل الأرقام أدناه من تشغيلات فعلية في هذه الجلسة، على قواعد بيانات مبنية من الصفر في كل مرة. لا رقم افتراضي أو منسوخ من تشغيل سابق — كل ملف اختبار أُعيد تشغيله فعليًا عند كتابة هذا الملف، بما فيها إعادة تشغيل اختبار HTTP/PostgREST مرتين بعد اكتشاف وإصلاح مشكلة استقرار زمنية (تفصيل في القسم 5) لم تكن لها أي علاقة بمنطق Hotfix 4.2.1 المالي نفسه.

## 1) بناء قاعدة البيانات من الصفر

- `local_harness_setup.sql` → نجح.
- الترحيلات 0001–0112 بالترتيب دون توقف → **112/112 نجحت** (الجديدة لهذه الرقعة: 0106–0112، سبع ترحيلات، فوق Patch 4.2 القائمة 0099–0105 دون أي تعديل عليها).
- `supabase/seed.sql` → نجح.

## 2) اختبار الترقية العام (Foundation → latest، بلا إعادة تشغيل seed.sql)

`scripts/run_upgrade_test.sh` يُطبِّق 0040–0112 تلقائيًا عبر glob بلا تعديل على السكربت نفسه:

```
=== ALL UPGRADE-FROM-0039 TESTS PASSED (0040-latest, including Phase 3 0058-0064, applied WITHOUT re-running seed.sql) ===
==> Upgrade test PASSED
```

**11/11 نقطة تحقق ناجحة.** Hotfix 4.2.1 لا تضيف أي صلاحية أو بيانات ضمن نطاق فحص هذا الاختبار العام (الذي يتوقف عند صلاحيات/بيانات Phase 2/Phase 3 الأساسية فقط، ولا يمسّ Returns).

## 3) إعادة تشغيل إثبات ترقية Patch 4.2 الخاص — للتأكُّد من عدم كسره صامتًا

`scripts/run_upgrade_test_patch_4_2.sh` (يبني حتى 0091 + `seed.sql`، يُطبِّق حصون Patch 4.2 القديمة، ثم يُطبِّق **0092–latest، أي حتى 0112 الآن**، بدل التوقف عند 0105 كما كان وقت تسليم Patch 4.2 نفسها):

```
ALL upgrade_patch_4_2_legacy_returns.test.sql ASSERTIONS PASSED (Section 1 requires_sale_refresh remediation path A, Section 2 legacy financial backfill B/C)
==> Patch 4.2 legacy-upgrade test PASSED
```

**11/11 نقطة تحقق ناجحة، بلا أي تغيير عن نتيجة Patch 4.2 الأصلية.** يُثبِت أن Hotfix 4.2.1 (تحديدًا: المُشغِّلات الإلحاقية الجديدة على `sales_return_refund_events` في 0106، وإعادة كتابة `approve_sales_return()` في 0109) لم تكسر صامتًا مسار ترقية `requires_sale_refresh`/Backfill المالي الخاص بـPatch 4.2 نفسها.

## 4) إثبات سلامة الترقية المخصَّص لبيانات دفتر الاسترداد القديمة سابقة لـHotfix 4.2.1 (Section 22 من مواصفة المستخدم)

إثبات دائم جديد **مُضاف للمستودع نفسه**:

- **الحصون (Fixture):** `supabase/tests/fixtures/hotfix_4_2_1_legacy_upgrade_pre_fixture.sql` — يُطبَّق على قاعدة بُنِيت حتى ترحيلة **0105 فقط** + `seed.sql` (Patch 4.2 مُسلَّمة، Hotfix 4.2.1 لم تُطبَّق بعد)، ويُنشئ بيانات دفتر استرداد حقيقية بتوقيعات الدوال **القديمة** (ما قبل 0106) في ثلاث حالات:
  - **`HF421-ACTIVE`** — بيعة 400.00، مرتجع معتمَد، حدث استرداد واحد مُسجَّل بالكامل، تُرِك **نشطًا** (لم يُعكَس أبدًا).
  - **`HF421-REVERSED`** — بيعة 300.00، مرتجع معتمَد، حدث استرداد واحد، **أُلغِي بالدالة القديمة** `reverse_sales_return_refund_event()` (تعديل `UPDATE` مباشر على الصف الأصلي، لا إدراج ملحق).
  - **`HF421-FEEV1`** — بيعة 1000.00 (صنف واحد)، خصم 100.00، مبلغ استرداد معتمَد 400.00 — اعتُمِد بمحرك العمولة **v1 القديم** (يُغطِّي كل ما تبقى من الطلبية بصنف واحد فيعكس العمولة الكاملة 100.00 رغم أن 400.00 فقط من أصل 1000.00 اعتُمِد فعليًا للاسترداد النقدي) — هذا بالضبط العيب المالي التاريخي الذي هذه الرقعة كُلِّفت بإغلاقه للاعتمادات **الجديدة**.
- **اختبار ما بعد الترقية:** `supabase/tests/upgrade_hotfix_4_2_1_legacy_refunds.test.sql` — يُشغَّل في اتصال `psql` **منفصل تمامًا** بعد تطبيق 0106–latest فوق الحصون أعلاه (محاكاة حرفية لترقية إنتاج حقيقية).
- **السكربت المُنسِّق:** `scripts/run_upgrade_test_hotfix_4_2_1.sh` — يبني قاعدة رميّة من الصفر، يطبِّق 0001–0105 + `seed.sql` الحقيقي، يُشغِّل الحصون، يطبِّق 0106–latest، ثم يُشغِّل اختبار ما بعد الترقية.

**النتيجة الفعلية لهذه الجلسة (`scripts/run_upgrade_test_hotfix_4_2_1.sh` كاملًا، أُعيد تشغيله مرتين للتأكُّد):**

```
ALL upgrade_hotfix_4_2_1_legacy_refunds.test.sql ASSERTIONS PASSED (Sections 1/2/4/13 legacy refund-ledger/fee-engine upgrade safety)
==> Hotfix 4.2.1 legacy-upgrade test PASSED
```

**6/6 نقاط تحقق رئيسية (15 `assert` فرعيًا) ناجحة**، مقسَّمة على ثلاثة مسارات:

| المسار | ما أُثبِت |
|---|---|
| **(A) `HF421-ACTIVE`** | العمود القديم المتجمِّد `status` يبقى `active` كما كان (A.1)؛ **صفر** صفوف في الجدول الملحق الجديد `sales_return_refund_event_reversals` لحدث لم يُعكَس أبدًا (A.2)؛ `get_sales_return()` بعد الترقية يشتق `actual_refunded_total=400.00` وحالة الحدث `active` من الملحق الجديد لا من العمود القديم (A.3/A.4). |
| **(B) `HF421-REVERSED`** | الصف الأصلي **بلا أي تغيير** بعد الترقية — الأعمدة القديمة متجمِّدة تاريخيًا فقط (B.1)؛ Backfill 0106 أنشأ سجلًا واحدًا بالضبط في الجدول الملحق، حاملًا نفس حقائق الإلغاء التاريخية (السبب/المنفِّذ/التاريخ/الوقت) بلا اختلاق (B.2/B.3)؛ `actual_refunded_total` يبقى `0.00` **مطابقًا تمامًا** لما كان عليه قبل الترقية، و`get_sales_return()` يشتق `status=reversed` من الملحق الجديد (B.4/B.5). |
| **(C) `HF421-FEEV1`** | `payment_fee_reversal_amount` التاريخي (محسوب بمحرك v1 القديم) يبقى `100.00` — القيمة الخاطئة تاريخيًا — **دون أي إعادة حساب صامتة** إلى القيمة الصحيحة 40.00 التي كان محرك v2 سيحسبها (C.1)؛ `payment_fee_reversal_calculation_version=1` مُعيَّن صراحةً عبر Backfill 0106 (C.2). |

**الخلاصة:** العيبان الجوهريان اللذان حدَّدتهما المراجعة المصدرية (دفتر استرداد غير إلحاقي حقيقيًا فعليًا، وعمولة مُعاد حسابها على أساس قيمة الصنف لا مبلغ الاسترداد المعتمد) أُغلِقا لكل اعتماد/إلغاء **جديد** من الآن فصاعدًا، مع الحفاظ الحرفي على كل قيمة تاريخية محسوبة بالمنطق القديم (لا إعادة حساب صامتة أبدًا) — مُثبَت بثلاثة سيناريوهات تُغطِّي كل تركيبة حالة قديمة ممكنة.

## 5) اختبارات SQL — كل الملفات، أرقام فعلية من هذا التشغيل (تشغيل واحد متسلسل على نفس قاعدة البيانات، 0001–0112)

| الملف | النتيجة |
|---|---|
| `financial_master_data.test.sql` | 52/52 ✅ |
| `financial_integrity_patch_2_1.test.sql` | 34/34 ✅ |
| `financial_integrity_hotfix_2_2_1.test.sql` | 14/14 ✅ |
| `financial_integrity_patch_2_2.test.sql` | 17/17 ✅ |
| `financial_integrity_hotfix_2_2_2.test.sql` | 11/11 ✅ |
| `rls_and_permissions.test.sql` | 139/139 ✅ |
| `sales_core.test.sql` | 57/57 ✅ |
| `sales_integrity_patch_3_1.test.sql` (Part 1، أحادي الجلسة) | 14/14 ✅ |
| `sales_integrity_patch_3_1_concurrency.test.sql` (Part 2، تزامن حقيقي عبر `dblink`) | 10/10 ✅ |
| `sales_integrity_patch_3_2.test.sql` | 15/15 ✅ |
| `sales_integrity_hotfix_3_2_1.test.sql` | 6/6 ✅ |
| `sales_returns_core.test.sql` (**مُعدَّل** — سيناريو 3 فقط، الأساس التراكمي v2 الجديد: 33.33/33.34/33.33 بدل 33.33/33.33/33.34، الإجمالي 100.00 دون تغيير) | **77/77 ✅** |
| `sales_returns_hotfix_4_2_1.test.sql` (**جديد بالكامل** لهذه الرقعة) | **35/35 ✅** |
| `sales_returns_concurrency.test.sql` (**مُوسَّع** — R7 جديد فوق R1–R6 القائمة) | **R1–R7 — 7/7 سيناريوهات ✅** |

**المجموع: صفر أخطاء عبر 14 ملف اختبار SQL في تشغيلة واحدة متسلسلة على قاعدة بيانات واحدة مبنية من الصفر (0001–0112 + `seed.sql` الحقيقي)، صفر انحدار عابر للوحدات (Foundation/Phase 2/Sales/Returns Core/Patch 4.1/Patch 4.2/Hotfix 4.2.1 معًا في تسلسل واحد).** التعديل الوحيد على اختبار قائم مسبقًا (`sales_returns_core.test.sql` سيناريو 3) موثَّق ومقصود — يعكس تغيُّر الأساس الحسابي لمحرك عكس العمولة (v1→v2)، لا كسرًا عرضيًا؛ الإجمالي المُثبَت (100.00 عبر ثلاث خطوات) لم يتغيَّر.

### تفصيل `sales_returns_hotfix_4_2_1.test.sql` (35 نقطة تحقق، جديد بالكامل)

تغطية مخصَّصة لكل من الأقسام التالية من مواصفة المستخدم، بسيناريوهات حقيقية لا إعادة اشتقاق لما تغطيه ملفات أخرى أصلًا:

- **Section 6/17:** `p_reference` يُخزَّن ويُعاد كما هو حرفيًا؛ `refund_method_name_snapshot` يبقى ثابتًا تاريخيًا حتى لو أُعيدت تسمية طريقة الدفع لاحقًا.
- **Section 7/8/9/10:** محرك `compute_sales_return_fee_reversal_v2()` — أساس `proportional_reversal` التراكمي عبر عدة مرتجعات على نفس الطلبية؛ أساس `full_reversal` النقدي (`approved_refund_amount`) لا عدد الأصناف.
- **Section 12:** `customer_never_received` + `not_collected` يُعيد عكس عمولة صفرًا صراحةً.
- **Section 13:** `payment_fee_reversal_calculation_version=2` لكل اعتماد جديد.
- **Section 14 (سيناريوهات A–E):** تغطية شاملة إلزامية لكل تركيبة سياسة/حالة مذكورة صراحةً في المواصفة.
- **Section 15:** `search_sales_orders_for_return()` تنجح لمُمثِّل يملك `returns.create` فقط دون `sales.view`؛ `get_returnable_sales_order()` كذلك.
- **Section 18 (سيناريوهات 1–10):** إثبات append-only مباشر — `UPDATE`/`DELETE` مباشر على `sales_return_refund_events` من خارج بوابة الإفلات يُرفَض دومًا؛ سجل إلغاء واحد بالضبط لكل حدث؛ upgrade path يُثبَت في ملف منفصل (القسم 4 أعلاه).
- **Section 20:** تكافؤ `preview_sales_return()`/`approve_sales_return()` يمتد الآن ليشمل حساب العمولة v2 نفسه، لا الحقول المالية الأساسية وحدها.

### تفصيل سيناريو R7 الجديد في `sales_returns_concurrency.test.sql` (Section 19، تزامن حقيقي عبر `dblink`)

اتصال A يُلغي (`reverse_sales_return_refund_event()`) حدث استرداد ويُبقي معاملته مفتوحة (غير مُلتزَمة)؛ اتصال B يحاول إلغاء **نفس الحدث** بالتوازي — يُحجَب فعليًا (مُثبَت عبر `dblink_is_busy`)، ثم يُرفَض بوضوح بعد التزام A بالرسالة "سجل الاسترداد هذا متراجَع عنه بالفعل"؛ التحقُّق النهائي المباشر من `sales_return_refund_event_reversals` يؤكِّد سجلًا واحدًا بالضبط (`count=1`) — لا إمكانية لإلغاء مزدوج حتى تحت تسابق حقيقي، بفضل قيد `UNIQUE(refund_event_id)` كخط دفاع أخير فوق ترتيب القفل الصريح.

## 6) اختبار HTTP/PostgREST الحقيقي — نتائج فعلية

`scripts/run_postgrest_http_test.sh` (ثنائي PostgREST v12.2.3 حقيقي، JWT حقيقي مُوقَّع، `@supabase/postgrest-js`، لا محاكاة):

```
=== ALL REAL HTTP/POSTGREST INTEGRATION TEST ASSERTIONS PASSED ===
```

**81/81 تأكيدًا ناجحًا** (77 من Patch 4.2 وما قبلها بصفر انحدار + 4 أقسام تأكيد جديدة لهذه الرقعة، بعضها يحمل أكثر من فحص فرعي في نفس السطر). أبرز ما أُثبِت عبر HTTP فعلي لهذه الرقعة تحديدًا:

- `payment_fee_reversal_calculation_version` يُعاد كـ`typeof "number"` (وسم صحيح، لا نقل Decimal كسلسلة نصية) بقيمة `2` لمرتجع اعتُمِد بمحرك v2 الجديد، ومحجوب كليًا (لا يظهر في مفاتيح الاستجابة إطلاقًا) لمُمثِّل بلا `sales.view_profit`.
- `record_sales_return_refund()` مع `p_reference` جديد يُخزَّن ويظهر حرفيًا على حدث الاسترداد المُعاد عبر `get_sales_return()`، إلى جانب `refund_method_name_snapshot` غير الفارغة و`status='active'` المُشتقة حيًّا من الجدول الملحق.
- بعد `reverse_sales_return_refund_event()`، الحدث نفسه يعرض `status='reversed'` (مُشتَق حيًّا، لا عمود مُعدَّل في مكانه) بينما `reference` يبقى محفوظًا دون تغيير.
- الدالة الجديدة `search_sales_orders_for_return()` تجد الطلبية المُوسَّمة برقمها بالضبط وتُعيد `subtotal` كـ`typeof "string"` (نقل Decimal آمن).

**إصلاح جانبي ضروري اكتُشِف أثناء هذا التشغيل (موثَّق بالتفصيل في الملحق التاسع عشر من `DELIVERY_REPORT.md`):** حساب "اليوم" في `postgrest-http-test.mjs` كان يستخدم التاريخ الخام بتوقيت UTC (`new Date().toISOString()`)، بينما كل دالة بحث سعر/عمولة/ضريبة تفتَرِض `business_today()` (توقيت الرياض، UTC+3) كقيمة افتراضية — الفارق (حتى 3 ساعات حول منتصف الليل UTC) تسبَّب فعليًا في فشل عابر حقيقي وقع أثناء هذه الجلسة نفسها (`لا يوجد سعر ذهب مسجَّل لهذا العيار بتاريخ 2026-08-18` ثم `لا يوجد إصدار ضريبة قيمة مضافة معتمد بتاريخ 2026-08-17` — فارق يوم كامل بين الاثنين، يُثبِت المشكلة بوضوح). أُصلِح بمواءمة حساب `todayIso` في السكربت مع `business_today()` بالضبط (`Asia/Riyadh`، UTC+3، بلا توقيت صيفي)، مع إضافة بيانات تأسيسية لكلا التاريخين (`current_date`/`business_today()`) في `supabase/tests/postgrest_http_test_setup.sql` كضمان إضافي مستقل. إصلاح استقرار اختبار بحت — لا تغيير في أي منطق تطبيقي أو مالي.

## 7) فحوصات الطبقة الأمامية (TypeScript/React) — نتائج فعلية

- `npx tsc --noEmit` → **صفر أخطاء** (شمل كل الملفات المُعدَّلة: `src/types/database.ts`، `src/features/returns/{schema,actions,queries}.ts`، `refund-events-panel.tsx`، `src/app/(app)/returns/[id]/page.tsx`، وملف الاختبار الجديد `refund-events-panel.test.tsx`).
- `npx eslint .` → **صفر أخطاء وتحذيرات** عبر المشروع بالكامل.
- `npx vitest run` → **52/52 ناجح عبر 7 ملفات** (6 قائمة بلا تغيير + ملف جديد بالكامل `refund-events-panel.test.tsx`، 5 اختبارات — التغطية الإلزامية لإصلاح Section 16):
  - حالة **مفتوحة** (غير مُغلَقة): زر "تسجيل استرداد" ظاهر، زر "تراجع" ظاهر لحدث نشط.
  - حالة **مُغلَقة** (`refundFinalizedAt` غير فارغ): زر "تسجيل استرداد" **غائب كليًا**، زر "تراجع" **غائب كليًا** حتى لو كان الحدث لا يزال `active` (نقطة الإصلاح الجوهرية — لم يعد الأمر معتمِدًا على حالة الحدث وحدها)، زر "إعادة فتح التسوية" ظاهر.
  - حدث مُلغى مسبقًا (`status='reversed'`): لا زر تراجع يُعرَض له في أي حالة (مفتوحة أو مُغلَقة) — بدلًا منه شارة "متراجَع عنه" فقط.
  - مرتجع بحالة غير `approved` (مثل `pending`): زر "تسجيل استرداد" لا يظهر إطلاقًا بصرف النظر عن حالة التسوية.
- `npm run check:numeric-types` → **نجح، 46/46 عمود NUMERIC مطابق** لِـ`number` في `database.ts` (بلا زيادة عن Patch 4.2 — كل الأعمدة المالية الجديدة في هذه الرقعة `boolean`/`integer`/`text`، لا `numeric` خام إضافي)، مُشغَّل فعليًا ضد قاعدة تحمل الترحيلات كاملة حتى 0112.
- `npm run build` (Next.js/Turbopack) → **نجح**، 27 مسارًا، بلا مسارات جديدة (Hotfix 4.2.1 يوسِّع نفس مسارات Returns القائمة، لا يضيف مسارات جديدة).

## 8) خلاصة

**35 نقطة تحقق SQL مخصَّصة جديدة + 77 نقطة تحقق Core (تعديل مقصود موثَّق فقط، صفر انحدار حقيقي) + 7/7 سيناريوهات تزامن حقيقية (R7 جديد) + 6 نقاط تحقق ترقية مخصَّصة (15 `assert` فرعيًا) + 11/11 اختبار ترقية عام + 11/11 إعادة تشغيل إثبات ترقية Patch 4.2 بلا تغيير + صفر أخطاء عبر كامل مجموعة اختبارات SQL (14 ملفًا، قاعدة بيانات واحدة طازجة، 0001–0112) + 81 تأكيدًا HTTP/PostgREST حقيقيًا (4 أقسام جديدة) + 52 اختبار Vitest عبر 7 ملفات (5 اختبارات جديدة إلزامية لـSection 16) + 46/46 عمود NUMERIC + فحوصات `tsc`/`eslint`/`next build` نظيفة بالكامل** — كلها من تشغيلات فعلية في هذه الجلسة على قواعد بيانات مبنية من الصفر، لا أرقام مفترَضة أو منسوخة، بما فيها إصلاح استقرار زمني حقيقي اكتُشِف ووُثِّق ذاتيًا أثناء التنفيذ (القسم 6 أعلاه). **لم تبدأ Shipping ولا Settlements ولا Services/Adjustments ولا Inventory ولا Reports — العمل متوقف الآن نهائيًا، بانتظار المراجعة والموافقة من المستخدم بعد إرسال ZIP هذا التسليم.**
