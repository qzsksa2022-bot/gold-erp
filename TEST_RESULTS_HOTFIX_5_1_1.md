# نتائج الاختبار الفعلية — Final Shipping Hotfix 5.1.1

كل الأرقام أدناه من تشغيلات فعلية في هذه الجلسة ضد قواعد بيانات مبنية من الصفر (`local_harness_setup.sql` + الترحيلات 0001–0132 بالترتيب دون توقف + `supabase/seed.sql`). لا رقم افتراضي أو منسوخ من تشغيل سابق — كل قسم أدناه أُعيد تشغيله فعليًا عند كتابة هذا الملف، بما فيها إعادة بناء عدة قواعد بيانات مؤقتة منفصلة (فحص ترحيلات، حزمة SQL، تزامن، HTTP/PostgREST)، وتصحيح عيب حقيقي واحد اكتُشِف أثناء تشغيل اختبار HTTP الحقيقي (انظر القسم 4).

## 1) بناء قاعدة البيانات من الصفر

- `local_harness_setup.sql` → نجح.
- الترحيلات 0001–0132 بالترتيب دون توقف → **132/132 نجحت** (الجديدتان: 0131 — تسلسل زمني بالنسبة لآخر حدث حالة/COD مسجَّل (بند 6)، و0132 — RPCs نصية آمنة لقراءة `base_cost`/`fee_amount` (بند 5) — لا تعديل على أي من 0001–0130).
- `supabase/seed.sql` → نجح (بلا أي تغيير — Hotfix 5.1.1 لا يضيف بيانات مؤسِّسة جديدة).

## 2) إثبات أمان الترقية (Upgrade Safety) — ثلاثة مسارات ترقية قائمة

تشغيل السكربتات الثلاثة القائمة فعليًا ضد الترحيلتين الجديدتين 0131–0132 مضافتين فوق كل مسار:

| السكربت | يُثبِت | النتيجة |
|---|---|---|
| `run_upgrade_test.sh` | ترقية من قاعدة ما قبل Phase 5 (حتى 0112) ببيانات بيع حقيقية، ثم تطبيق 0113–0132 كاملة | **نجح** |
| `run_upgrade_test_patch_4_2.sh` | ترقية من قاعدة Patch 4.2 (حتى 0105) ببيانات مرتجعات حقيقية | **نجح** |
| `run_upgrade_test_hotfix_4_2_1.sh` | ترقية من قاعدة Hotfix 4.2.1 (حتى 0112) | **نجح** |

**3/3 مسارات ترقية نجحت، صفر تغيير على أي صف بيانات موروث** — تثبت أن 0131/0132 (كلتاهما `create or replace function` بتوقيع غير مُغيَّر أو دالتان جديدتان بالكامل، بلا `ALTER TABLE`) قابلتان للتطبيق بأمان فوق أي قاعدة إنتاجية حقيقية بها بيانات Phase 5/Patch 5.1 أو ما قبلها.

## 3) اختبارات SQL — كل الملفات، أرقام فعلية من هذا التشغيل (تشغيل واحد متسلسل على نفس قاعدة البيانات، 0001–0132)

| الملف | النتيجة |
|---|---|
| كل ملفات الاختبار القائمة من Foundation حتى Patch 5.1 (14 ملفًا، بلا تعديل منطقي) | **بلا انحدار، صفر أخطاء** |
| `shipping_integrity_hotfix_5_1_1.test.sql` (**جديد بالكامل** — تغطية مباشرة لبنود 5/6/7) | **ناجح بالكامل ✅** |

**الإجمالي: 15/15 ملف اختبار SQL نجح ضمن نفس التشغيلة المتسلسلة، صفر انحدار.**

### 3.1) `shipping_integrity_hotfix_5_1_1.test.sql` — تفصيل التغطية

- **القسم A (بند 6 — تسلسل حالة الشحنة):** الشحنة تتقدَّم إلى حدث حالة بتاريخ `v_d0+2`، ثم إثبات أن حدثًا لاحقًا بتاريخ `v_d0+1` (بعد `shipment_date` لكن **قبل** آخر حدث حالة مسجَّل فعليًا) **يُرفَض** — بالضبط الفجوة التي تركتها 0130 (كانت تتحقق فقط من `>= shipment_date`، لا من آخر حدث في السلسلة نفسها). نفس التاريخ (`v_d0+2`) يبقى مقبولًا (تسامح صريح مع نفس اليوم)، وتاريخ لاحق (`v_d0+3`) ينجح بشكل طبيعي.
- **القسم B (بند 6 — تسلسل حالة تحصيل COD):** نفس النمط مطابقًا تمامًا لسلسلة `shipment_cod_events`.
- **القسم C (بند 5 — RPCs نصية آمنة):** استدعاء `list_shipping_carrier_rate_versions_safe()`/`list_customer_return_shipping_fee_versions_safe()` مع تأكيد `pg_typeof(base_cost)::text = 'text'`/`pg_typeof(fee_amount)::text = 'text'`، تطابق القيم حرفيًا (`'22.00'`، `'33.50'`)، ورفض كلتا الدالتين لفاعل يملك `shipments.view` فقط بلا `shipping_rates.view`.

بنود 1/2/3/4/8/9/10 غير مغطاة في ملف SQL هذا عمدًا (تفصيل السبب موثَّق في تعليق رأس الملف نفسه): بند 1/2/3 واجهة أمامية (React) بحتة — مغطاة في القسم 5 أدناه؛ بند 4 إصلاح Server Action فقط، لا منطق RPC جديد؛ بند 8 عمود جدول عرض فقط؛ بند 9 مغطى في القسم 4؛ بند 10 مغطى في القسم 5.

### 3.2) اختبار التزامن الحقيقي (dblink) — بلا انحدار فوق 0131/0132

`shipping_core_phase5_concurrency.test.sql` (A–G، بلا تعديل منطقي) أُعيد تشغيله كاملاً فوق قاعدة تحتوي 0001–0132:

**A–G، 7/7 سيناريوهات تزامن حقيقية ناجحة** — القفل المشترك لتسعير الشحن (A)، تعارض إصدار الصف تحت تزامن حقيقي لحدث الحالة (B) والتكلفة الفعلية (C)، انتظار الإغلاق اليومي الحقيقي (D/E1/E2)، القفل الحصري لإصدار تسعير جديد (F)، وسباق `reverse_sales_return()` مقابل `create_shipment()` باتجاه إرجاع (G، بند 16 من Patch 5.1) — كلها لا تزال تعمل بشكل صحيح بلا أي تغيير سلوكي أحدثته 0131/0132 (كلتاهما تلمسان فقط الفحص الزمني الإضافي داخل الدالتين، لا منطق القفل/التزامن نفسه).

## 4) اختبار HTTP/PostgREST الحقيقي — الفجوة المُفصَح عنها في Patch 5.1 مُغلَقة الآن (بند 9)

خلافًا لِـPatch 5.1 (التي وثَّقت صراحة عدم وجود قسم HTTP/PostgREST حقيقي مخصَّص، انظر `TEST_RESULTS_PATCH_5_1.md` القسم 4)، أُضيف **Part 9** إلى `scripts/postgrest-http-test.mjs` هذه الجلسة — يمتد فوق نفس رحلة HTTP/PostgREST الحقيقية القائمة (ثنائي PostgREST v12.2.3 فعلي، لا محاكاة)، ويثبت عبر شبكة حقيقية:

- **بند 1:** `create_shipment()` عبر HTTP حقيقي يرفض شحنة إرجاع برسوم تختلف عن القيمة القياسية المُحلَّلة (35.00) بلا سبب تجاوز، ثم تنجح بنفس الفرق بمجرد إرفاق السبب — و`get_shipment()` يعكس `customer_return_shipping_fee_standard_amount`/`customer_return_shipping_charge_is_override`/`customer_return_shipping_charge_override_reason` بدقة. (هذا التطبيق على مستوى RPC كان موجودًا فعليًا منذ 0125/0129 — الفجوة الحقيقية لبند 1 كانت أن Server Action/schema/النموذج لم تكن تُمرِّر السبب أصلاً؛ مغطاة في القسم 5).
- **بند 5:** الدالتان الجديدتان `list_shipping_carrier_rate_versions_safe()`/`list_customer_return_shipping_fee_versions_safe()` (0132) تُعيدان `base_cost`/`fee_amount` كـ`typeof "string"` عبر HTTP حقيقي — أبدًا رقم JS خام — وتُرفَضان لفاعل حقيقي موقَّع بشكل مستقل يملك `shipments.view` فقط بلا `shipping_rates.view`.
- **بند 6:** `add_shipment_status_event()`/`record_shipment_cod_collection_state()` (0131) يرفضان عبر HTTP حقيقي حدثًا جديدًا بتاريخ أسبق من **آخر** حدث موجود فعليًا في سلسلته، رغم تجاوزه حدَّ `shipment_date` الأدنى — إثبات الانحدار الفعلي (كانت 0130 تتحقق فقط من `shipment_date`) عبر شبكة حقيقية، لا في حزمة SQL فقط. نفس اليوم يبقى مقبولاً، وتاريخ لاحق ينجح بعد الرفض.

بند 4 (صلاحية `correct_status`-فقط في Server Action) غير قابل للاختبار الهادف على هذه الطبقة — عيب Server Action في Next.js، لا يُستدعى مباشرة عبر PostgREST؛ الدالة نفسها كانت مضبوطة بشكل صحيح بالفعل قبل Hotfix 5.1.1 (مغطاة ضمنيًا بحزمتي SQL/التزامن القائمتين).

**عيب حقيقي اكتُشِف وأُصلِح أثناء تشغيل هذا القسم بالذات في هذه الجلسة (قبل أي تسليم):** تأكيد قائم من Part 8 (`get_shipment()` — حماية الربحية) كان لا يزال يفترض تعاقد ما قبل Patch 5.1: أن `customer_shipping_charge` يجب أن يكون غائبًا تمامًا عن فاعل بلا `sales.view_profit`. لكن Patch 5.1 (0126/0129) أعاد تصميم هذا عمدًا وموثَّقًا داخل تعليق `get_shipment()` نفسه — `customer_shipping_charge`/`effective_customer_shipping_charge`/لقطة رسوم الإرجاع رسمٌ تشغيلي يواجه العميل، لا رقم ربح داخلي، فتبقى ظاهرة دومًا؛ فقط حقول التكلفة/الهامش (`expected_carrier_cost`/`actual_carrier_cost`/`net_shipping_*`/`cod_expected_amount`/`carrier_rate_version_id`/`financial_events`) تبقى محجوبة. لم يُعَد تشغيل حزمة HTTP هذه فعليًا منذ هبوط 0125–0129 (Patch 5.1)، فبقي هذا الانحراف بين الاختبار والتصميم الفعلي غير مُكتشَف حتى هذا التشغيل. أُصلِح التأكيد ليطابق التعاقد الموثَّق حاليًا فعليًا في `get_shipment()` (وقُوِّي: يتحقق الآن أيضًا من غياب `carrier_rate_version_id`/`actual_carrier_cost`/`net_shipping_actual`/`cod_expected_amount` صراحةً، لا أربعة حقول فقط كما كان). هذا ليس عيبًا في أي ترحيلة (لا 0125–0132 ولا 0001–0130) — الدالة كانت صحيحة دومًا؛ العيب كان في تأكيد اختبار HTTP نفسه لم يُحدَّث بعد إعادة تصميم Patch 5.1.

**نتيجة تشغيل `scripts/run_postgrest_http_test.sh` كاملاً (كل الأجزاء 1–9 على نفس القاعدة الحية عبر PostgREST):**

```
=== ALL REAL HTTP/POSTGREST INTEGRATION TEST ASSERTIONS PASSED ===
```

صفر فشل، بعد تصحيح التأكيد أعلاه.

## 5) فحوصات الطبقة الأمامية (TypeScript/React) — نتائج فعلية

- `npx tsc --noEmit` → **صفر أخطاء** (شمل التعديلات الجديدة على `schema.ts`/`actions.ts`/`shipment-entry-form.tsx`/صفحة `/shipments`/`src/features/shipping-rates/queries.ts`/`src/types/database.ts`).
- `npx eslint .` → **صفر أخطاء وتحذيرات** عبر المشروع بالكامل.
- `npx vitest run` → **62/62 ناجح عبر 8 ملفات** — الزيادة عن Patch 5.1 (52/52 عبر 7 ملفات) هي ملف جديد بالكامل `tests/shipment-entry-form-return-fee.test.tsx` (5 اختبارات — بنود 1/2/3: التعبئة التلقائية عند أول منطقة، **الانحدار الفعلي** لبند 2 (تبديل المنطقة يُعيد المزامنة بدل التجمُّد على أول قيمة)، ظهور حقل السبب الإلزامي وتعطيل الإرسال عند التجاوز، بقاء قيمة مُعدَّلة يدويًا بعد تبديل منطقة لاحق، وعدم اعتبار `"35"` مقابل `"35.00"` تجاوزًا) + 5 اختبارات جديدة على `createShipmentSchema` داخل `tests/validation.test.ts` (بند 1 — قبول/رفض `customer_return_shipping_charge_override_reason` حسب طول النص، وتطبيع النص الفارغ إلى `undefined`).
- `npm run check:numeric-types` (`DATABASE_URL=... npx tsx scripts/check-numeric-column-types.ts`) → **نجح، 56/56 عمود NUMERIC** لا يزال مطابقًا لِـ`number` في `database.ts` — بلا تغيير عن Patch 5.1 (Hotfix 5.1.1 لا يضيف أي عمود NUMERIC خام جديد؛ 0132 دوال قراءة فقط تُعيد `::text`).
- `npm run build` (Next.js/Turbopack) → **نجح** — نفس عدد المسارات كـPatch 5.1 (لا صفحات جديدة، فقط أعمدة جدول إضافية على `/shipments` القائمة وحقل نموذج إضافي).

## 6) خلاصة

**132/132 ترحيلة من الصفر نجحت (0131/0132 الجديدتان، صفر تعديل على 0001–0130)** + **3/3 إثباتات ترقية ناجحة على ثلاثة مسارات مختلفة** + **15/15 ملف اختبار SQL نجح ضمن قاعدة واحدة طازجة (`shipping_integrity_hotfix_5_1_1.test.sql` الجديد يغطي بنود 5/6/7 مباشرة)** + **A–G، 7/7 سيناريوهات تزامن حقيقية بلا انحدار فوق 0131/0132** + **اختبار HTTP/PostgREST حقيقي كامل ناجح (Part 9 الجديد يغطي بنود 1/5/6 عبر شبكة حقيقية — يسدّ الفجوة المُفصَح عنها صراحة في Patch 5.1)** + **صفر أخطاء TypeScript/ESLint** + **62/62 اختبار Vitest عبر 8 ملفات (10 اختبارات جديدة: 5 React + 5 schema، تغطي بنود 1/2/3)** + **56/56 عمود NUMERIC** + **بناء إنتاجي ناجح** — كلها من تشغيلات فعلية في هذه الجلسة، لا أرقام مفترَضة أو منسوخة، بما في ذلك عيب حقيقي واحد اكتُشِف وأُصلِح أثناء كتابة هذا الملف بالذات (تأكيد HTTP مُنحرِف عن تصميم Patch 5.1 الموثَّق، القسم 4).

**لم تبدأ Settlements ولا أي مرحلة أخرى — العمل متوقف الآن نهائيًا، بانتظار المراجعة والموافقة من المستخدم بعد إرسال ZIP كامل للمشروع بأكمله (لا ZIP فروقات).**
