# نتائج الاختبار الفعلية — Final Shipping UI & Verification Hotfix 5.1.2

كل الأرقام أدناه من تشغيلات فعلية في هذه الجلسة ضد قاعدة بيانات مبنية من الصفر (`local_harness_setup.sql` + الترحيلات 0001–0132 بالترتيب دون توقف + `supabase/seed.sql`). **لا ترحيلة جديدة في هذا الـHotfix** — كل الإصلاحات كانت منطق واجهة أمامية (touched-state/loading-state) بالإضافة إلى استكمال تغطية اختبارية كانت ناقصة؛ لم يُكتشَف أي عيب على مستوى قاعدة البيانات هذه المرة (خلافًا لـHotfix 5.1.1 الذي اكتشف تأكيد HTTP منحرفًا عن التصميم). لا رقم افتراضي أو منسوخ من تشغيل سابق — كل قسم أدناه أُعيد تشغيله فعليًا عند كتابة هذا الملف، بما فيها إعادة بناء قاعدة بيانات مؤقتة جديدة (`gold_erp_fresh_verify`) وثلاث قواعد ترقية منفصلة، وتصحيح 3 عيوب حقيقية اكتُشِفت أثناء أول تشغيل فعلي لحزمة HTTP الجديدة (Part 10) — انظر القسم 4.

## 1) بناء قاعدة البيانات من الصفر

- `local_harness_setup.sql` → نجح.
- الترحيلات 0001–0132 بالترتيب دون توقف → **132/132 نجحت** — **لا ترحيلة جديدة أُضيفت في Hotfix 5.1.2** (البند 6 من المواصفة: لم تُعدَّل 0001–0132، ولم تُنشأ 0133 لأن لا حاجة SQL فعلية ظهرت — كل الإصلاحات منطق واجهة أمامية بحت).
- `supabase/seed.sql` → نجح (بلا أي تغيير).

## 2) إثبات أمان الترقية (Upgrade Safety) — ثلاثة مسارات ترقية قائمة

تشغيل السكربتات الثلاثة القائمة فعليًا (لا تغيير عليها، إذ لا ترحيلة جديدة لتغطيتها):

| السكربت | يُثبِت | النتيجة |
|---|---|---|
| `run_upgrade_test.sh` | ترقية من قاعدة ما قبل Phase 5 (حتى 0112) ببيانات بيع حقيقية، ثم تطبيق 0113–0132 كاملة | **نجح** |
| `run_upgrade_test_patch_4_2.sh` | ترقية من قاعدة Patch 4.2 (حتى 0105) ببيانات مرتجعات حقيقية | **نجح** |
| `run_upgrade_test_hotfix_4_2_1.sh` | ترقية من قاعدة Hotfix 4.2.1 (حتى 0112) | **نجح** |

**3/3 مسارات ترقية نجحت، صفر تغيير على أي صف بيانات موروث** — متوقع، إذ Hotfix 5.1.2 لا يمس أي ترحيلة.

## 3) اختبارات SQL — كل الملفات، أرقام فعلية من هذا التشغيل (تشغيل واحد متسلسل على نفس قاعدة البيانات، 0001–0132)

| الملف | النتيجة |
|---|---|
| كل ملفات الاختبار القائمة (15 ملفًا، بلا تعديل منطقي — لا ملف SQL جديد لهذا الـHotfix) | **بلا انحدار، صفر أخطاء** |

**الإجمالي: 15/15 ملف اختبار SQL نجح ضمن نفس التشغيلة المتسلسلة، صفر انحدار.** لا ملف اختبار SQL جديد في Hotfix 5.1.2 (البنود 1–4 من المواصفة واجهة أمامية/Server Action بحتة؛ البند 5 مغطى حصرًا عبر HTTP الحقيقي في القسم 4 أدناه، لأن كل ما يثبته موجود بالفعل على مستوى RPC منذ Patch 5.1/Hotfix 5.1.1 — الفجوة كانت في تغطية الاختبار الفعلي عبر الشبكة، لا في منطق قاعدة البيانات).

### 3.1) اختبار التزامن الحقيقي (dblink) — بلا انحدار

الملفات الثلاثة القائمة أُعيد تشغيلها كاملة فوق قاعدة تحتوي 0001–0132 (بلا تعديل منطقي، لا ترحيلة جديدة لتغطيتها):

- `sales_integrity_patch_3_1_concurrency.test.sql` (H1/H2/I/J/J2/K/L1/L2) → **كل السيناريوهات ناجحة**
- `sales_returns_concurrency.test.sql` (R1–R7) → **كل السيناريوهات ناجحة**
- `shipping_core_phase5_concurrency.test.sql` (A–G) → **A–G، 7/7 سيناريوهات ناجحة**

كلها لا تزال تعمل بشكل صحيح — Hotfix 5.1.2 لا يمس أي دالة RPC أو منطق قفل/تزامن.

## 4) اختبار HTTP/PostgREST الحقيقي — استكمال التغطية للبند 5 (A–I)

أُضيف **Part 10** إلى `scripts/postgrest-http-test.mjs` — يمتد فوق نفس رحلة HTTP/PostgREST الحقيقية القائمة (ثنائي PostgREST v12.2.3 فعلي، لا محاكاة)، ويثبت عبر شبكة حقيقية كل بنود item 5 من المواصفة:

- **A) رفض الكتابة المباشرة:** محاولة `INSERT` مباشرة عبر PostgREST الحقيقي ضد `shipping_carrier_rate_versions` و`customer_return_shipping_fee_versions` من فاعل يملك `shipping_rates.manage` — كلتاهما تُرفَضان (migration 0122's RLS lockdown)، مع تأكيد إضافي بقراءة الجدول للتحقق أن لا صف أُنشئ فعليًا (لا مجرد رسالة خطأ مع أثر جانبي صامت).
- **B) نجاح الـRPCs المعتمدة:** `create_shipping_carrier_rate_version()` و`create_customer_return_shipping_fee_version()` تنجحان عبر HTTP حقيقي وتُعيدان `uuid` فعليًا.
- **C) عمليات shipments.view-only:** `shipments_filter_carrier_lookups()`/`shipments_filter_zone_lookups()` تنجحان لفاعل يملك `shipments.view` فقط (Patch 5.1 item 11 — غير مقيَّدة بـ`shipments.create` خلافًا للنسختين الأقدم)، و`list_shipments()` لنفس الفاعل يُظهر `customer_shipping_charge` (رسم يواجه العميل) بينما تبقى تكاليف الناقل/الهامش `null`.
- **D) رسوم إرجاع بلا تسعير معتمد:** `create_shipment()` يرفض رسم إرجاع يدوي لمنطقة بلا أي `customer_return_shipping_fee_versions` مُعدَّة إطلاقًا بدون سبب تجاوز (رسالة migration 0125/0129 المخصَّصة لحالة "لا يوجد إعداد معتمد إطلاقًا" — تختلف عن رسالة "يختلف عن القياسي" المستخدمة في بند 1)، ثم تنجح بمجرد إرفاق السبب، مع `customer_return_shipping_fee_standard_amount = null` (لا شيء ليُقاس عليه) و`is_override`/`reason` مسجَّلتين بشكل صحيح.
- **E) رفض شحنة إرجاع على مرتجع reversed:** `create_shipment()` يرفض إنشاء شحنة إرجاع جديدة على مرتجع أصبح `reversed` (Patch 5.1 item 15/16) عبر HTTP حقيقي.
- **F) COD RPC + رفض المجاوزة المباشرة:** محاولة `INSERT` مباشرة على `shipment_cod_events` (تجاوز `record_shipment_cod_collection_state()`) تُرفَض تمامًا (صفر سياسات RLS لـ`authenticated`، migration 0127)، مع تأكيد أن طول `cod_timeline` عبر `get_shipment()` لم يتغيَّر إطلاقًا قبل/بعد المحاولة.
- **G) بقاء لقطات الناقل/المنطقة التاريخية:** `get_shipment()` لشحنة أُنشئت قبل إعادة تسمية الناقل/المنطقة لا يزال يعرض القيم الأصلية وقت الإنشاء (migration 0129) رغم إعادة التسمية اللاحقة الحقيقية.
- **H) قراءات آمنة تشمل الصفوف الجديدة:** `list_shipping_carrier_rate_versions_safe()`/`list_customer_return_shipping_fee_versions_safe()` تُعيدان الصفوف التي أُنشئت للتو عبر RPCs هذه الجلسة نفسها (لا فقط البيانات المؤسِّسة)، و`base_cost`/`fee_amount` لا تزال `typeof "string"`.
- **I) مرشِّحات `list_shipments()` الأربعة الجديدة:** `p_order_number`، `p_return_number`، `p_original_sale_store_id`، `p_cod_collection_state` — كل واحد منها اختُبِر منفردًا عبر HTTP حقيقي ويُرجع فقط الشحنات المطابقة (مُثبَت بوجود شحنات أخرى غير مطابقة في نفس التشغيلة لضمان أن المرشِّح فعليًا يستبعدها، لا مجرد لا يرفض الطلب).

**3 عيوب حقيقية اكتُشِفت وأُصلِحت أثناء أول تشغيل فعلي لـPart 10 في هذه الجلسة (قبل أي تسليم)، كلها في كود الاختبار نفسه — لا عيب واحد في أي RPC أو ترحيلة:**

1. **بندا A (كلاهما):** الكود الأصلي استخدم `try { await client.from(...).insert(...) } catch`، لكن `@supabase/postgrest-js` **لا يرمي استثناءً** عند خطأ من مستوى PostgREST (رفض RLS، مخالفة قيد، إلخ) ما لم يُستدعَ `.throwOnError()` صراحة — الاستدعاء العادي يُرجِع `{ data, error }` بشكل طبيعي بدلاً من الرفض. لذلك لم يُكتشَف الرفض الفعلي إطلاقًا. أُصلِح بتفكيك `{ error }` من نتيجة `insert()` مباشرة والتحقق منها، مع تأكيد احتياطي إضافي بقراءة الجدول للتأكد من عدم وجود صف فعليًا حتى في حال غياب حقل `error` نفسه.
2. **بند D:** التأكيد استخدم `/يجب إدخال سبب/` كنمط توقُّع لرسالة الخطأ، لكن الفرع المخصَّص لحالة "لا يوجد إعداد معتمد إطلاقًا" (migration 0125/0129) يرفع رسالة مختلفة فعليًا: `'لا يوجد إعداد معتمد لرسوم شحن الإرجاع لهذه المنطقة بتاريخ % — أدخل سببًا (رسوم يدوية بدون إعداد معتمد)'` (مختلفة عن رسالة "يختلف عن القياسي" `'... يجب إدخال سبب التعديل'` المستخدمة في بند 1). السلوك الفعلي كان صحيحًا تمامًا منذ البداية — أُصلِح التأكيد فقط ليطابق الرسالة الفعلية الصحيحة (`/لا يوجد إعداد معتمد لرسوم شحن الإرجاع/`).
3. **بند F:** نفس عيب `try/catch` من بند A بالضبط — أُصلِح بنفس الطريقة (تفكيك `{ error }`).

بعد الإصلاحات الثلاثة، أُعيد تشغيل الحزمة الكاملة (كل الأجزاء 1–10 على نفس القاعدة الحية عبر PostgREST):

```
=== ALL REAL HTTP/POSTGREST INTEGRATION TEST ASSERTIONS PASSED ===
```

صفر فشل — بلا أي انحدار على الأجزاء 1–9 القائمة (Phase 3/Phase 4/Phase 5/Patch 5.1/Hotfix 5.1.1).

## 5) فحوصات الطبقة الأمامية (TypeScript/React) — نتائج فعلية

- `npx tsc --noEmit` → **صفر أخطاء**.
- `npx eslint .` → **صفر أخطاء وتحذيرات** عبر المشروع بالكامل.
- `npx vitest run` → **73/73 ناجح عبر 10 ملفات** — الزيادة عن Hotfix 5.1.1 (62/62 عبر 8 ملفات) هي **11 اختبارًا جديدًا عبر 3 ملفات**، تغطي بالضبط الحالات الناقصة الخمس المطلوبة في البند 4:

  | الملف | الاختبارات الجديدة | يغطي |
  |---|---|---|
  | `tests/shipment-entry-form-return-fee.test.tsx` (3 اختبارات جديدة أُضيفت لملف قائم، الإجمالي أصبح 8) | "item 1 (untouched)"، "item 1 (touched)"، "item 2" | configured→no-config **untouched** (مسح تلقائي للحقل)، configured→no-config **touched** (الاحتفاظ بالقيمة اليدوية + إلزام السبب)، preview **loading** (تعطيل الإرسال أثناء انتظار الاقتراح الجديد وتمييزه بصريًا عن `not_found`) |
  | `tests/shipping-actions-permission-boundary.test.ts` (**جديد بالكامل** — أول اختبار وحدة مباشر لـServer Action في هذا المشروع) | 3 اختبارات | `correct_status`-only permission boundary: يثبت أن `addShipmentStatusEventAction` يستدعي `requireAnyPermission(["shipments.update_status", "shipments.correct_status"])` حصرًا (لا `requirePermission` مطلقًا)، وأن فاعلًا يملك `correct_status` فقط يصل فعليًا إلى الـRPC، وأن فاعلًا بلا أي منهما يُرفَض قبل الوصول للـRPC إطلاقًا |
  | `tests/shipment-view-only-ui.test.tsx` (**جديد بالكامل**) | 5 اختبارات | `shipments.view-only` UI regression: فاعل بصلاحية `shipments.view` فقط لا يرى زر "تحديث الحالة" ولا زر/تسمية "تسجيل حالة التحصيل" إطلاقًا؛ فاعل بـ`shipments.update_status` أو `shipments.correct_status` (أي منهما) يرى زر الحالة؛ فاعل بـ`shipments.manage_cost` يرى زر COD |

- `npm run check:numeric-types` → **نجح، 56/56 عمود NUMERIC** لا يزال مطابقًا لِـ`number` في `database.ts` — بلا تغيير (Hotfix 5.1.2 لا يضيف أي عمود جديد).
- `npm run build` (Next.js/Turbopack) → **نجح** — 28 مسارًا (نفس عدد Hotfix 5.1.1، لا صفحات جديدة — كل التعديلات داخل مكوّن نموذج قائم).

## 6) خلاصة

**132/132 ترحيلة من الصفر نجحت (صفر ترحيلة جديدة — البند 6 من المواصفة محقَّق حرفيًا)** + **3/3 إثباتات ترقية ناجحة** + **15/15 ملف اختبار SQL نجح ضمن قاعدة واحدة طازجة، صفر انحدار** + **A–G وR1–R7 وH1/H2/I/J/J2/K/L1/L2 — كل سيناريوهات التزامن الحقيقية ناجحة بلا انحدار** + **اختبار HTTP/PostgREST حقيقي كامل ناجح (Part 10 الجديد يغطي بنود A–I التسعة كاملة عبر شبكة حقيقية — يستكمل الفجوة المتبقية من Patch 5.1/Hotfix 5.1.1)** + **صفر أخطاء TypeScript/ESLint** + **73/73 اختبار Vitest عبر 10 ملفات (11 اختبارًا جديدًا عبر 3 ملفات، تغطي بالضبط الحالات الخمس الناقصة المطلوبة في البند 4)** + **56/56 عمود NUMERIC** + **بناء إنتاجي ناجح، 28 مسارًا** — كلها من تشغيلات فعلية في هذه الجلسة، لا أرقام مفترَضة أو منسوخة، بما في ذلك 3 عيوب حقيقية اكتُشِفت وأُصلِحت أثناء أول تشغيل فعلي لـPart 10 (كلها في كود الاختبار نفسه، لا في أي RPC أو ترحيلة — القسم 4).

**لم تبدأ Settlements ولا أي مرحلة أخرى — العمل متوقف الآن نهائيًا، بانتظار المراجعة والموافقة من المستخدم بعد إرسال ZIP كامل للمشروع بأكمله (لا ZIP فروقات).**
