# نتائج الاختبار الفعلية — Final Shipping Verification Hotfix 5.1.3

كل الأرقام أدناه من تشغيلات فعلية في هذه الجلسة ضد قاعدة بيانات مبنية من الصفر (`local_harness_setup.sql` + الترحيلات 0001–0132 بالترتيب دون توقف + `supabase/seed.sql`). **لا ترحيلة جديدة في هذا الـHotfix أيضًا** — كل الإصلاحات منطق واجهة أمامية (Preview state machine) بالإضافة إلى استكمال تغطية اختبارية HTTP/React كانت ناقصة. هذا الـHotfix يستجيب لمراجعة كود مصدري فعلية من المستخدم لـZIP الـHotfix 5.1.2 (SHA-256 `3aec32119daced2e41abd3c43741e8d6e124520ec9159f0cfcc4051235bcca86`، مؤكَّد صحيحًا) — اكتشف المستخدم Blocker حقيقي واحد (خلط "خطأ في المعاينة" مع "لا يوجد تسعير معتمد") + عشر فجوات تحقق محددة. لا رقم افتراضي أو منسوخ من تشغيل سابق — كل قسم أدناه أُعيد تشغيله فعليًا عند كتابة هذا الملف، بما فيها اكتشاف وتصحيح عيوب حقيقية جديدة في هذه الجلسة نفسها (انظر القسم 4 وملاحظة التصحيح في القسم 5).

## 1) لا ترحيلات جديدة

الترحيلات تتوقف عند 0132 تمامًا كما كانت — **132/132 ترحيلة من الصفر نجحت، صفر تعديل على 0001–0132**. كل بنود 1–10 من مواصفة هذا الـHotfix كانت منطق واجهة أمامية (Preview state machine) أو استكمال تغطية اختبارية HTTP/React لمنطق RPC صحيح وموجود بالفعل — لا فجوة سلوكية على مستوى القاعدة تستدعي ترحيلة 0133.

## 2) لا جداول جديدة ولا أعمدة جديدة

`check:numeric-types` أُعيد تشغيله للتحقق فعليًا: **56/56 عمود NUMERIC**، بلا تغيير.

## 3) البنود 1–4 — إصلاح Preview State Machine في `shipment-entry-form.tsx`

**المشكلة الجذرية (Blocker) التي حددها المستخدم:** الشرط `if (result.success && result.data.found && result.data.fee_amount) {...} else {...}` كان يزُجّ بحالتين مختلفتين تمامًا في نفس فرع `else` → `not_found`: (أ) الـRPC نجحت فعليًا وقالت `found=false` (لا تسعير معتمد حقيقي)، و(ب) استدعاء الـServer Action نفسه فشل (`success:false` — خطأ قاعدة بيانات/شبكة/جلسة/صلاحية) أو رمى استثناءً. الحالة (ب) لم تكن "لا يوجد تسعير معتمد" إطلاقًا، لكن الواجهة كانت تعرضها بهذا الشكل للمستخدم وتسمح له بإدخال رسوم يدوية + سبب والمتابعة — وهذا يخالف المطلوب الأصلي صراحةً (خطأ المعاينة يجب أن يمنع Submit تمامًا، لا أن يُفسَّر كـno_config).

**الإصلاح (بنود 1/2/3):**

- أُضيفت حالة خامسة صريحة إلى `feePreviewStatus`: `"idle" | "loading" | "found" | "not_found" | "error"` — مع `feePreviewErrorMessage: string | null` مرافقة.
- القاعدة الآن دقيقة تمامًا: `result.success === true && found === true` → `found`؛ `result.success === true && found === false` → `not_found` (لا تسعير حقيقي)؛ `result.success === false` → `error`؛ استثناء مرمي من نداء الـServer Action → `error` أيضًا (داخل `try/catch`، بلا `unhandled rejection`).
- عند `error`: `feeSuggested` يُمسَح، رسالة "لا يوجد تسعير معتمد..." **لا تظهر إطلاقًا** (مُستبعَدة صراحة عبر `feePreviewStatus !== "error"` في حساب `returnFeeIsOverride`)، Submit مُعطَّل بالكامل (عبر invariant البند 4 أدناه)، وتظهر رسالة عربية واضحة: "تعذر التحقق من رسوم شحن الإرجاع. أعد المحاولة قبل إنشاء الشحنة." مع زر **"إعادة التحقق"** صريح يُعيد تشغيل نفس دالة الجلب.
- حقل الرسوم غير المُعدَّل يدويًا (untouched) يُمسَح أيضًا عند `error` (حتى لا يبقى رقم قديم يبدو معتمَدًا)؛ الحقل المُعدَّل يدويًا (touched) يبقى كما هو دون أي مساس.

**البند 4 (Preview Key invariant):** بدل الاعتماد فقط على توقيت تشغيل `useEffect`، أُضيف مفتاح صريح `currentFeePreviewKey = \`${direction}|${shippingZoneId}|${shipmentDate}\`` وحالة `resolvedFeePreviewKey` تسجِّل لأي مفتاح كانت آخر نتيجة محلولة فعليًا. **Submit لشحنة إرجاع يُسمَح فقط إذا `resolvedFeePreviewKey === currentFeePreviewKey` AND الحالة `found` أو `not_found`** — يستبعد `idle`/`loading`/`error` وأيضًا نتيجة محلولة لكن تخصّ منطقة/تاريخ سابقين (invariant واضح، لا مجرد اعتماد على ترتيب تنفيذ الـeffect). آلية طلب/استجابة مُفتّحة بـkey أيضًا داخل دالة الجلب نفسها (`fetchFeePreview`) عبر `feePreviewRequestKeyRef` — استجابة تصل بعد أن بدأ طلب أحدث (تبديل منطقة جديد، أو ضغط "إعادة التحقق") تُكتشَف وتُهمَل.

## 4) البند 5 — اختبارات React الجديدة لمسار الخطأ (4 اختبارات جديدة)

أُضيفت إلى `tests/shipment-entry-form-return-fee.test.tsx` (الإجمالي أصبح 12، كان 8):

- **item A:** `previewCustomerReturnShippingFeeAction` تُرجِع `{success:false, error:"..."}` → رسالة خطأ مميَّزة تظهر، رسالة "لا يوجد تسعير معتمد" **لا** تظهر، Submit معطَّل، وإدخال رسم يدوي **لا** يُفعِّل Submit ولا يُظهِر حقل السبب.
- **item B:** نفس السيناريو لكن عبر استثناء **مرمي فعليًا** من الاستدعاء — نفس النتيجة تمامًا، بلا `unhandled rejection` (لولا معالجة `try/catch` الصحيحة لفشل تشغيل الاختبار كاملاً بخطأ غير مُعالَج).
- **item C (إعادة التحقق → found):** خطأ أولي، ثم ضغط "إعادة التحقق" يُرجِع نتيجة ناجحة (`found=true, 35.00`) — الحقل غير المُعدَّل يمتلئ تلقائيًا بالقيمة، رسالة الاقتراح تظهر، Submit يعود مُفعَّلاً بمجرد اكتمال بقية الحقول.
- **item D (إعادة التحقق → not_found حقيقية):** خطأ أولي، ثم "إعادة التحقق" يُرجِع `found=false` حقيقيًا — الانتقال إلى حالة "لا يوجد تسعير معتمد" الحقيقية يحدث **فقط بعد** الاستجابة الناجحة (لم تظهر أثناء الخطأ نفسه إطلاقًا)، ورسم يدوي + سبب مسموحان بعدها بشكل طبيعي.

**عيب حقيقي اكتُشِف وأُصلِح أثناء كتابة الاختبارات نفسها (لا في المنطق، بل في ربط UI):** زر "إعادة التحقق" كان مربوطًا أصلاً بـ`disabled={isPending}` — لكن `isPending` مُشتركة (عبر `useTransition()` واحدة) مع تأثير معاينة تكلفة الناقل **غير المرتبط إطلاقًا** بهذا الزر، فكان الزر يظهر معطَّلاً فعليًا في نافذة زمنية قصيرة رغم أن حالة المعاينة نفسها `"error"` بوضوح. أُصلِح بإزالة هذا الربط تمامًا — الزر يختفي من الـDOM فور الضغط عليه أصلاً (الحالة تنتقل لـ`"loading"` فورًا)، فلا حاجة لتعطيله عبر علم مشترك غير ذي صلة.

## 5) البند 6 — استكمال اختبار Viewer-only للـList الفعلية (6 اختبارات جديدة)

الملف `tests/shipment-view-only-ui.test.tsx` كان يختبر فقط مكوّني صفحة التفاصيل (`ShipmentStatusActions`/`ShipmentCodStateAction`) رغم اسمه/وصفه العامّين — لم تكن صفحة `/shipments` (القائمة، الـRequirement الأصلي) مغطاة إطلاقًا. أُضيف قسم جديد يستدعي `ShipmentsPage` (Server Component) مباشرة كدالة (بنفس أسلوب اختبار Server Action المباشر القائم من Hotfix 5.1.2)، مع تثبيت `requirePermission`/`listShipmentsPage`/`getShipmentsVisibleStoreLookups`/`getShipmentsFilterCarrierLookups`/`getShipmentsFilterZoneLookups`:

- فاعل بصلاحية `shipments.view` فقط (بلا `shipments.create`، بلا `sales.view_profit`) يرى رقم الشحنة، رقم عملية البيع، ورسوم الشحن على العميل.
- نفس الفاعل **لا** يرى عمودَي "صافي الشحن المتوقع"/"صافي الشحن الفعلي" (الترويسة نفسها غائبة، لا القيمة فقط — `canViewProfit` يحجب الأعمدة في الـJSX نفسه).
- نفس الفاعل **لا** يرى زر "شحنة جديدة" (مُقيَّد بـ`shipments.create`).
- الصفحة تعتمد فعليًا على `shipments_filter_carrier_lookups()`/`shipments_filter_zone_lookups()`/`shipments_visible_store_lookups()` (المُقيَّدة بـ`shipments.view` وحدها) — **وليس** الاستعلامات القديمة المُقيَّدة بـ`shipments.create` (مؤكَّد عبر التحقق من استدعاء الدوال الصحيحة فعليًا).
- اختباران ضابطان (control): فاعل يملك `sales.view_profit` إضافيًا يرى عمودَي الصافي؛ فاعل يملك `shipments.create` إضافيًا يرى زر "شحنة جديدة" — يثبتان أن التأكيدات أعلاه ذات معنى فعليًا، لا اجتياز صوري.

الإجمالي في هذا الملف: **11 اختبارًا** (كان 5).

## 6) البند 5 — استكمال HTTP/PostgREST الحقيقي (Part 10 مُوسَّعة، بنود 7/8/9/10)

`scripts/postgrest-http-test.mjs`'s Part 10 (Hotfix 5.1.2) امتدت بأربعة بنود جديدة، دون المساس بأي من بنود A–I القائمة:

- **item 7 (تكملة A):** محاولة `PATCH` (UPDATE) مباشرة عبر HTTP حقيقي على `shipping_carrier_rate_versions`/`customer_return_shipping_fee_versions` (تغيير `base_cost`/`fee_amount`/`effective_from`) — **بلا أثر**، والقيم الأصلية تبقى ثابتة فعليًا بعد المحاولة (تحقُّق مستقل عبر قراءة لاحقة).
- **item 8 (تكملة C):** `shipments_visible_store_lookups()` تنجح عبر HTTP حقيقي لفاعل يملك `shipments.view` فقط — الاعتماد الثالث لصفحة `/shipments`، لا فقط دوال البحث عن الناقل/المنطقة.
- **item 9 (تكملة G):** بعد إعادة تسمية الناقل/المنطقة، **شحنة جديدة** تُنشأ الآن — لقطتها الخاصة تلتقط الاسمين **الجديدين**، لا القديمين (يُثبت أن الأعمدة لقطة زمنية حقيقية، لا مجرد قيمة مجمَّدة أبدية).
- **item 10 (تكملة F):** محاولة `UPDATE` مباشرة على `shipment_cod_events`، ومحاولة `UPDATE` مباشرة على `shipments.cod_collection_state` نفسه — كلتاهما بلا أثر؛ حالة COD الحالية تبقى ثابتة فعليًا (RPC الرسمية فقط هي من يغيّرها).

**عيب حقيقي اكتُشِف وأُصلِح أثناء أول تشغيل فعلي لهذه البنود الأربعة (في كود الاختبار، لا في أي RPC/RLS):** التأكيدات الأولى اشترطت وجود خطأ مرمي فعليًا (`error !== null`) كإثبات للرفض — لكن سلوك PostgREST/RLS الفعلي لـ`UPDATE` على جدول بلا أي سياسة UPDATE مطابقة ليس بالضرورة رمي خطأ: شرط `WHERE` يطابق ببساطة **صفر صفوف** (RLS تستبعدها قبل أن تصل الـUPDATE إليها إطلاقًا)، فتُعيد PostgREST استجابة 2xx طبيعية بلا خطأ وبلا أي صف متأثر. أُصلِحت التأكيدات بتسلسل `.select("id")` بعد `UPDATE` مباشرة، والتحقق من أن المصفوفة المُعادة فارغة (الإشارة الحقيقية للرفض هنا) — إلى جانب فحص الخطأ كاحتياط، وقراءة لاحقة مستقلة تُؤكِّد بقاء القيمة الأصلية دون تغيير في كل الحالات. **هذا ليس عيبًا في أي RLS/RPC — الحماية كانت صحيحة دومًا؛ العيب كان في شرط تأكيد اختبار HTTP نفسه.**

بعد الإصلاح: `=== ALL REAL HTTP/POSTGREST INTEGRATION TEST ASSERTIONS PASSED ===`، صفر فشل، بلا أي انحدار على الأجزاء 1–10 القائمة (بما فيها بنود A–I الأصلية من Hotfix 5.1.2).

## 7) فحوصات الطبقة الأمامية (TypeScript/React) — نتائج فعلية

- `npx tsc --noEmit` → **صفر أخطاء**.
- `npx eslint .` → **صفر أخطاء وتحذيرات** عبر المشروع بالكامل.
- `npx vitest run` → **83/83 ناجح عبر 10 ملفات** — الزيادة عن Hotfix 5.1.2 (73/73 عبر 10 ملفات) هي **10 اختبارات جديدة عبر ملفين قائمين مُعدَّلين** (لا ملف جديد هذه المرة): 4 في `tests/shipment-entry-form-return-fee.test.tsx` (بند 5، مسار الخطأ/إعادة التحقق)، 6 في `tests/shipment-view-only-ui.test.tsx` (بند 6، صفحة `/shipments`).
- `npm run check:numeric-types` → **نجح، 56/56 عمود NUMERIC** — بلا تغيير.
- `npm run build` (Next.js/Turbopack) → **نجح** — 28 مسارًا، نفس عدد Hotfix 5.1.2، لا صفحات جديدة.

## 8) خلاصة

**132/132 ترحيلة من الصفر (صفر جديدة) + 3/3 إثباتات ترقية + 15/15 ملف اختبار SQL، صفر انحدار + كل سيناريوهات التزامن الحقيقية (H1–L2/R1–R7/A–G) بلا انحدار + اختبار HTTP/PostgREST حقيقي كامل ناجح (Part 10 مُوسَّعة ببنود 7/8/9/10 الأربعة الجديدة، بعد تصحيح عيب في منطق تأكيد UPDATE-denial) + صفر أخطاء TypeScript/ESLint + 83/83 اختبار Vitest عبر 10 ملفات (10 اختبارات جديدة: 4 مسار خطأ + 6 قائمة `/shipments`) + 56/56 عمود NUMERIC + بناء إنتاجي ناجح، 28 مسارًا** — كلها من تشغيلات فعلية في هذه الجلسة، بما في ذلك اكتشاف وتصحيح عيبين حقيقيين إضافيين أثناء التنفيذ نفسه (زر "إعادة التحقق" المربوط خطأً بعلم `isPending` مشترك غير ذي صلة؛ ومنطق تأكيد UPDATE-denial الذي اشترط خطأً وجود خطأ مرمي بدل التحقق من صفر صفوف متأثرة) — كلاهما في الواجهة الأمامية/كود الاختبار، لا في أي RPC/RLS/ترحيلة.

**لم تبدأ Settlements ولا أي مرحلة أخرى — العمل متوقف الآن نهائيًا، بانتظار المراجعة والموافقة من المستخدم بعد إرسال ZIP كامل للمشروع بأكمله (لا ZIP فروقات).**
