# نتائج اختبار Phase 6 — Final Integrity Hotfix 6.1.1: إصلاحات المراجعة النهائية لـServices/Adjustments (ترحيلات 0157–0162 + تعديل مسموح على 0144)

هذا الملف يوثِّق كل خطوة تحقُّق نُفِّذت فعليًا في هذه الجلسة لـHotfix 6.1.1، بالأرقام الحقيقية الناتجة عن كل تشغيلة. يقابله في `DELIVERY_REPORT.md` **الملحق السابع والعشرون**.

## 0) بيان صريح — لماذا 0144 عُدِّلت رغم تجميد 0001–0143

الأمر الحاكم لهذه الجلسة صريح: **0001–0143 نهائية ومجمَّدة ولا تُعدَّل إطلاقًا**، بينما **0144–0156 (Patch 6.1) لا تزال قيد المراجعة ولم تُعتمَد بعد**. تحديدًا داخل **0144 نفسها** وُجد Upgrade Blocker: قبل 0144، كان يمكن لسجل **مرفوض** (`status = 'rejected'`) بقيمة تحصيل صفرية (`customer_charge = 0`) أن يحمل `payment_method_id`/`collection_channel_id` غير NULL بشكل شرعي تمامًا تحت العقد القديم (0133–0143) — لأن قواعد التطبيع القديمة لم تكن تفرض شيئًا على سجل مرفوض. الـbackfill الأصلي في 0144 طبَّع فقط السجلات معلَّقة/معتمَدة بقيمة صفرية، **متجاهلًا المرفوضة تمامًا**، ثم أضاف القيد `sales_order_adjustments_zero_charge_consistent` الذي يفرض NULL لحقول الدفع على **أي** سجل بقيمة تحصيل صفرية بصرف النظر عن حالته. نتيجة ذلك: قاعدة بيانات إنتاجية حقيقية تحمل سجلًا واحدًا من هذا النوع كانت ستفشل في تطبيق **0144 نفسها** — قبل أن تصل إطلاقًا إلى أي ترحيلة لاحقة بدءًا من 0157. **لا يمكن إصلاح هذا عبر ترحيلة 0157+ لاحقة، لأن العطل يقع أثناء تطبيق 0144 ذاتها، لا بعدها.** لذلك — وفقط لهذا السبب المحدَّد — **عُدِّلت 0144 نفسها في هذه الجلسة**، بينما 0145–0156 بقيت بلا أي تعديل (لم تظهر ضرورة قاهرة مماثلة فيها)، وكل تحسين/إصلاح آخر (البنود 2–12 من المواصفة) طُبِّق حصرًا في ترحيلات جديدة بدءًا من **0157**.

## 1) بناء قاعدة اختبار من الصفر بالكامل

حُذفت قاعدة الاختبار وأُعيد إنشاؤها، ثم طُبِّق `supabase/tests/local_harness_setup.sql`، ثم **كل الترحيلات 0001–0162 بالترتيب دون توقف** (162/162 نجحت دون أي خطأ)، ثم `supabase/seed.sql` كاملًا → **نجح**.

## 2) عيبان حقيقيان في الترحيلة الجديدة 0158 نفسها — اكتُشِفا وأُصلِحا في هذه الجلسة

اكتُشِف كلاهما عبر إعادة تشغيل `scripts/run_upgrade_test_patch_6_1.sh` (0143→latest فوق بيانات Adjustments حقيقية أُنشئت تحت عقود Patch 6.1 القديمة، شاملة الآن fixture السادس الجديد "مرفوض/تحصيل صفري" — انظر §3 أدناه) — وهو بالضبط السيناريو الذي يكشف عيوبًا من هذا النوع مبكرًا:

1. **ترتيب خاطئ في 0158** (`adjustments_calculation_version_column.sql`): قيد `sales_order_adjustments_calculation_version_consistent` (`approved ⇔ calculation_version is not null`) كان يُضاف **قبل** كتلة الـbackfill التي تُعيد ضبط `calculation_version = 1` لكل سجل معتمَد موجود مسبقًا — فأي سجل معتمَد قديم بدأ بقيمة `NULL` فور `ADD COLUMN` كان يخالف القيد فور إضافته. **نفس درس الترتيب** المُطبَّق سابقًا مرتين في 0144/0150 (انظر `TEST_RESULTS_PATCH_6_1.md` §2). **الإصلاح:** نقل كتلة الـbackfill لتُنفَّذ **قبل** `add constraint`.
2. **مُحفِّز موجود مسبقًا (من 0154) يحجب الـbackfill نفسه في 0158**: بعد إصلاح (1)، إعادة التشغيل كشفت خطأً جديدًا: محفِّز `sales_order_adjustments_reject_terminal_mutation` (من 0154، ضمن Patch 6.1 المُجمَّدة ولا يجوز تعديلها) يرفض **أي** تحديث على سجل بحالة `approved`/`rejected` — بما في ذلك تحديث الـbackfill نفسه محاولًا ضبط `calculation_version` لسجلات معتمَدة مسبقًا. **نفس فئة العيب** التي ووجهت سابقًا في 0150 مقابل محفِّز الإضافة-فقط لجدول العكوس. **الإصلاح:** تطويق عبارة الـUPDATE الوحيدة في الـbackfill بـ`alter table ... disable trigger sales_order_adjustments_reject_terminal_mutation;` قبلها و`... enable trigger ...` بعدها — **بلا أي تعديل على 0154 نفسها**.

كلا الإصلاحين أُعيد اختبارهما فعليًا فوق بيانات ترقية حقيقية → **صفر أخطاء**، والقيم المُعاد بناؤها صحيحة رقميًا بالضبط (4 صفوف معتمَدة أُعيد ضبط `calculation_version` لها إلى `1` في هذا التشغيل الأخير — انظر §3).

## 3) fixture الترقية الجديد الإلزامي — "مرفوض/مجاني" (البند 2 من المواصفة)

`supabase/tests/fixtures/patch_6_1_upgrade_pre_fixture.sql` وُسِّع بـfixture سادس: تعديل حقيقي أُنشئ تحت العقد **القديم** (0133–0143 عبر `create_sales_order_adjustment()`/`reject_sales_order_adjustment()` القديمتين، `p11-args`)، بقيمة تحصيل صفرية **مع** طريقة دفع/قناة تحصيل حقيقيتين (بالضبط الشكل الذي كان سيُفشِل تطبيق 0144 قبل الإصلاح)، ثم رُفِض. `supabase/tests/upgrade_patch_6_1_fixtures.test.sql` وُسِّع بقسمين جديدين `(e2)`/`(e2 raw)` يُثبتان بعد الترقية 0144→latest:

- الحالة تبقى `rejected`، رقم التعديل ثابت، سبب/مُنفِّذ/تاريخ الرفض محفوظة تمامًا.
- `customer_charge = '0.00'`، و`payment_method_id`/`collection_channel_id`/`payment_reference` جميعها NULL، و`participates_in_settlement = false` — الـbackfill الجديد طبَّع الحقول التشغيلية فقط، **دون اختلاق أي لقطة مالية** لسجل لم يُعتمَد ماليًا إطلاقًا.
- قراءة مباشرة (service_role) للجدول الأساسي تؤكد نفس النتيجة.

عدد التعديلات على عملية البيع المرجعية ارتفع من 5 إلى **6** — قسم `(f)` مُحدَّث ليعكس ذلك.

**النتيجة الكاملة لـ`scripts/run_upgrade_test_patch_6_1.sh` بعد كل الإصلاحات أعلاه: `=== ALL PATCH 6.1 UPGRADE-FIXTURES ASSERTIONS PASSED ===` — 11 كتلة `PASS` (a، b، c، c raw، d، d raw، e، **e2 الجديدة**، **e2 raw الجديدة**، f، g)، صفر فشل.**

## 4) مسارات الترقية الخمسة المطلوبة (البند 15 من المواصفة) — كلها نُفِّذت فعليًا

| # | السيناريو | السكربت | النتيجة |
|---|---|---|---|
| A | قاعدة جديدة تمامًا 0001→latest + `seed.sql` | بناء يدوي كامل (§1 أعلاه) | **PASS** (162/162 ترحيلة) |
| B | 0132→latest (بيانات بيع حقيقية قبل Phase 6 بالكامل) | `scripts/run_upgrade_test_phase6_adjustments.sh` | **PASS** |
| C | 0143→latest مع fixtures Patch 6.1 القائمة (1–5) | `scripts/run_upgrade_test_patch_6_1.sh` | **PASS** (ضمن نفس التشغيلة أدناه) |
| D | 0143→latest مع fixture "مرفوض/تحصيل صفري" الجديد (6) | نفس السكربت أعلاه، fixture 6 الجديد | **PASS** (`(e2)`/`(e2 raw)` أعلاه) |
| E | قاعدة تحمل معلَّق/معتمَد-مدفوع/معتمَد-مجاني/مرفوض-مجاني/معكوس/نوع أُعيدت تسميته معًا → latest | نفس fixtures 1–6 مجتمعة في نفس التشغيلة | **PASS** |

لا فشل في أي ترحيلة، لا فقدان لأي معرِّف/رقم تعديل/حالة/سبب رفض، تطبيع الخدمة المجانية صحيح، أثر العكس صحيح، backfill الإصدار الحسابي صحيح (`calculation_version = 1` لكل سجل معتمَد قديم)، لا إعادة تشغيل لـ`seed.sql`.

## 5) الفحص الشامل الحالي — 25 ملف اختبار SQL غير-ترقية + تزامن A–I

نُفِّذت على قاعدة `gold_erp_hotfix611_dev` المُعاد بناؤها بالكامل (§1 أعلاه):

| الملف | النتيجة |
|---|---|
| `supabase/tests/adjustments_core_phase6.test.sql` (منطق الوحدة الأساسي، يُعيد التحقق فوق 0144–0162 كاملة) | **PASS** (صفر `ERROR`، كل كتل `PASS`/`OK` نجحت) |
| `supabase/tests/adjustments_core_phase6_concurrency.test.sql` — 9 سيناريوهات تزامن حقيقي (`dblink`) A–I + Bonus | **PASS بالكامل** — A (سباق ترقيم)، B (تحديثَين متزامنَين)، C (تحديث ضد اعتماد)، D (اعتماد مزدوج)، E (عكس مزدوج)، F (إغلاق يومي ضد اعتماد)، G (إغلاق يومي ضد عكس)، H (كاتب عمولة دفع ضد اعتماد)، I (إعادة تسمية نوع ضد اعتماد)، Bonus (إغلاق يومي ضد إنشاء) |

لا حذف ولا إضعاف لأي اختبار قائم — كل التعديلات على ملفات الاختبار كانت **إضافات** (fixture 6 + قسمَي `(e2)`/`(e2 raw)`).

## 6) اختبار HTTP/PostgREST الحقيقي (ثنائي PostgREST v12.2.3 فعلي) — قسم "Part 13" الجديد

`ADMIN_DATABASE_URL=... bash scripts/run_postgrest_http_test.sh` — بناء قاعدة مؤقتة كاملة من الصفر (162 ترحيلة) + تشغيل ثنائي PostgREST حقيقي + نفس 6 JWT حقيقية من Patch 6.1 + تشغيل `scripts/postgrest-http-test.mjs` كاملًا عبر HTTP فعلي.

**النتيجة: `=== ALL REAL HTTP/POSTGREST INTEGRATION TEST ASSERTIONS PASSED ===` — 185 تأكيدًا "OK" (174 من الأقسام السابقة + 11 قسم "Part 13" الجديد بالكامل)، صفر فشل.**

### 6.أ) عيب فِكستشر حقيقي اكتُشِف أثناء كتابة "Part 13" — أُصلِح في ملف تجهيز HTTP الاختباري فقط (لا في أي ترحيلة)

بند 13 من المواصفة يتطلب إثبات إعادة تسمية طريقة الدفع/قناة التحصيل عبر "المسارات المُصرَّح بها" — ولا يوجد RPC مخصَّص لإعادة تسمية `payment_methods`/`collection_channels` في هذا المشروع أصلًا (مطابقةً تمامًا لِـ`adjustments_core_phase6.test.sql`'s items 25B/25C، اللذين يستخدمان تحديثًا مباشرًا عبر `service_role`)، فاستُخدِم نفس الأسلوب عبر `service_role` HTTP. محليًا هذا يعمل لأن جلسة `psql` المحلية بلا claim اسمه `sub` إطلاقًا (`auth.uid()` يعود NULL، والمحفِّز العام لأعمدة النظام يترك `updated_by` بلا مساس). لكن عبر HTTP حقيقي، JWT الخاص بـ`service_role` (`TEST_JWT_SERVICE`) يحمل `sub` حقيقيًا هو UUID الصفري القياسي — فـ`auth.uid()` **ليس** NULL، فيُثبِّت المحفِّز `updated_by` على تلك القيمة، التي لم يكن لها صف `profiles`/`auth.users` مطابق → فشل قيد مفتاح أجنبي حقيقي (`23503`). **الإصلاح:** إضافة صف `auth.users`/`profiles` تجريبي بديل (placeholder) لهذا المُعرِّف الصفري القياسي في `supabase/tests/postgrest_http_test_setup.sql` فقط — **ملف تجهيز اختباري بحت، ليس جزءًا من أي ترحيلة إنتاجية**، ولا يمس أي منطق تطبيق.

### 6.ب) تدقيق قسم "Part 13" — 11 بندًا مُثبَتة عبر HTTP فعلي

- البند 4: رفض إنشاء مدفوع بلا `participates_in_settlement` صريح؛ تطبيع خدمة مجانية إلى `false` حتى لو أرسل العميل `true` صراحةً.
- البند 14: `update_sales_order_adjustment()` لا يُسرِّب `payment_reference`/طريقة الدفع/المشاركة القديمة إلى الأمام عند الانتقال مدفوع←مجاني، حتى مع عميل "قديم" ما زال يرسل قيمة عالقة.
- البند 7: `calculation_version` يبقى NULL لسجل **مرفوض**، ويصبح `1` لسجل **معتمَد** (شاملًا سجل معتمَد-ثم-معكوس — يحتفظ بقيمة الاعتماد الأصلية دون مساس).
- البند 5: `list_sales_order_adjustments()` v3 — `effective_direct_cost`/`effective_payment_fee_amount` يظهران كسلسلة نصية لفاعل يملك `sales.view_profit`، ويُخفَيان (NULL) لفاعل لا يملكها.
- البند 10/14: فلتر `p_participates_in_settlement=false` عبر RPC نفسه يُقسِّم النتائج بشكل صحيح فعليًا (مستقل عن إصلاح رابط الترقيم في الواجهة).
- البند 6/7: الحقول الخمسة المُوقَّعة `reversal_*_impact` تظهر لفاعل يملك `sales.view_profit` على سجل معكوس (`reversal_net_profit_impact = -62.00`)، وتُخفَى بالكامل (NULL) بدونها، بينما `calculation_version` (بيانات تشغيلية) يبقى ظاهرًا لأي فاعل يملك `adjustments.view` فقط.
- البند 13: إثبات مُدمَج مُكثَّف واحد — اعتماد بنوع/طريقة دفع/قناة تحصيل حاليَّة، إعادة تسمية الثلاثة معًا عبر المسارات المُصرَّح بها، تأكيد أن التعديل **القديم** لم تتأثر لقطته إطلاقًا، ثم تعديل **جديد** يُعتمَد بعد إعادة التسمية يلتقط الأسماء **الجديدة** في لقطته الخاصة.

## 7) اختبارات React/Vitest الجديدة — مكوِّنات حقيقية، لا Zod فقط (البندان 11/12)

ملفان جديدان بالكامل:

- `tests/adjustments-entry-form-zero-charge-state-machine.test.tsx` (4 اختبارات) — يُصيِّر `AdjustmentEntryForm` الحقيقي (شاملًا `Select` من Radix، مع polyfills معروفة لـjsdom: `hasPointerCapture`/`scrollIntoView`/`ResizeObserver`):
  - **(A)** مدفوع بكل الحقول (طريقة/قناة/مرجع/مشاركة) مُعبَّأة → تحصيل←0 → حقول الدفع تختفي من الشجرة والحالة تُمسَح فعليًا (لا مجرد إخفاء).
  - **(B)** 0←مدفوع مجددًا → لا قيمة قديمة تعود: طريقة/قناة فارغتان من جديد، مرجع الدفع فارغ، المشاركة تعود لحالة "غير محدَّدة"، وزر الحفظ يبقى معطَّلًا حتى يُعاد اختيار كل شيء صراحةً.
  - **(C)** تعديل مدفوع جديد بلا اختيار مشاركة → الإرسال محظور؛ اختيار "لا يشارك" يُفعِّله؛ التبديل لـ"يشارك" يبقيه مُفعَّلًا.
  - **(D)** خدمة مجانية → الإرسال الفعلي (عبر `createAdjustmentAction` المُموَّه) يحمل `participates_in_settlement: false` دومًا، و`payment_method_id`/`collection_channel_id` غائبان تمامًا.
- `tests/adjustments-hotfix-6-1-1-list-reversal-pagination.test.tsx` (6 اختبارات) — يُصيِّر `AdjustmentsPage`/`AdjustmentDetailPage` الحقيقيَّين:
  - فاعل يملك `sales.view_profit` يرى أعمدة التكلفة المباشرة/عمولة الدفع/صافي الربح في `/adjustments`؛ فاعل بدونها لا يرى أيًّا منها.
  - رابطا "التالي"/"السابق" في الترقيم يحافظان على `participates_in_settlement=false` (والقيمة `true` كعنصر تحكُّم) في سلسلة الاستعلام — إصلاح البند 10 مُثبَت مباشرة عبر الرابط الناتج فعليًا.
  - سجل معكوس: فاعل يملك `sales.view_profit` يرى بطاقة "أثر العكس المالي" الصريحة (صافي أصلي/أثر العكس/صافي فعلي=0.00)؛ فاعل بدونها لا يراها إطلاقًا (المحاكاة تُطابِق سلوك الـRPC الفعلي — تصفير الحقول من الخادم، لا فحص صلاحية في الواجهة).

**النتيجة: 10/10 اختبار جديد ناجح.**

## 8) الفحص الشامل النهائي على مستوى الواجهة/TypeScript/البناء

| الفحص | الأمر | النتيجة |
|---|---|---|
| فحص الأنواع (السريع) | `npx tsc --noEmit` | **صفر أخطاء** |
| اختبارات الوحدة (المشروع كاملًا) | `npx vitest run` | **125/125 ناجح عبر 15 ملفًا** (10 اختبارات جديدة لِـHotfix 6.1.1 + بقية الملفات القائمة دون أي تغيير) |
| التدقيق (Lint) | `npx eslint .` | **صفر أخطاء/تحذيرات** |
| مطابقة أنواع الأعمدة العشرية | `DATABASE_URL=... npx tsx scripts/check-numeric-column-types.ts` | **OK: 73/73 عمود NUMERIC خام مطابق تمامًا لنوع `number` في `database.ts`** (`calculation_version` عمود صحيح عادي — لا NUMERIC — فلا يظهر في هذه القائمة، تمامًا كما هو متوقَّع) |
| بناء الإنتاج (فحص TypeScript الكامل الخاص بـNext.js مضمَّن) | `npm run build` (Next.js/Turbopack) | **نجح** — كشف عيبين حقيقيين في ملفَي الاختبار الجديدَين أنفسهما (تضييق نوع Tuple فارغ + عدم توافق نوع `null`/`string` في كائن اختبار)، أُصلِحا فورًا (توسيع توقيع دوال `vi.fn` المُموَّهة + إضافة تعليق نوع صريح لِـ`REVERSED_ADJUSTMENT`)، ثم أُعيد التشغيل → نجاح تام. نفس مسارات Adjustments الخمسة القائمة، بلا أي مسار جديد. |

## 9) إثبات صريح — 0001–0143 بقيت byte-for-byte كما هي في تسليم Patch 6.1 المُعتمَد سابقًا (SHA-256 `54f2ac...2bbdb`)

قُورِنت كل ترحيلة من 0001 إلى 0143 في هذا المشروع بنظيرتها المُستخرَجة من `gold-erp-patch-6-1.zip` (الأرشيف المُسلَّم سابقًا، SHA-256 المُتحقَّق منه: `54f2ac8dee4f7f9484e7f3a3d51ec1c312c938b202bdc1171c0bad0a20d2bbdb`) عبر `diff` ثنائي مباشر لكل ملف:

- **0001–0143 (143 ملفًا): متطابقة تمامًا byte-for-byte، صفر فروقات.**
- **0144: تختلف عمدًا** — التعديل المسموح به الموثَّق في §0 أعلاه.
- **0145–0156 (12 ملفًا): متطابقة تمامًا byte-for-byte، صفر فروقات** — لم تظهر أي ضرورة قاهرة لتعديلها.
- **0157–0162: ترحيلات جديدة بالكامل.**

## 10) خلاصة نهائية

**صفر انحدار حقيقي عبر كامل سطح الاختبار.** عيبان حقيقيان في الترحيلة الجديدة 0158 نفسها اكتُشِفا وأُصلِحا قبل أي تسليم (§2)، وعيب فِكستشر HTTP اختباري بحت (§6.أ، لا يمس أي ترحيلة إنتاجية) اكتُشِف وأُصلِح أيضًا، وعيبان نوعيَّان صغيران في ملفَي اختبار Vitest الجديدَين أنفسهما اكتُشِفا عبر بناء الإنتاج وأُصلِحا (§8) — كلها **قبل** أي تسليم فعلي، بلا حاجة لترحيلة تصحيحية منفصلة لاحقة.

**162/162 ترحيلة (0001–0162) + مسارات الترقية الخمسة المطلوبة كاملة (A–E) + 11 كتلة PASS في fixture الترقية المُحدَّث (شاملًا fixture السادس الجديد "مرفوض/مجاني") + 25 ملف اختبار SQL غير-ترقية شاملة تزامن A–I الحقيقي + 185 تأكيد HTTP حقيقي (174 قائم + 11 قسم "Part 13" الجديد) + 125 اختبار Vitest (10 جديدة) + TypeScript/ESLint/73 عمود NUMERIC/بناء إنتاجي — كلها من تشغيلات فعلية في هذه الجلسة.**

**0001–0143 بقيت byte-for-byte دون أي مساس. 0144 عُدِّلت فقط، وفقط للسبب الموثَّق صراحةً في §0 (Upgrade Blocker يقع أثناء تطبيق 0144 ذاتها). 0145–0156 بقيت بلا أي تعديل. لم تبدأ Settlements ولا Reports/Dashboard/Inventory ولا أي مرحلة جديدة. لم يُحذَف ولم يُضعَف أي اختبار قائم. العمل متوقف الآن نهائيًا، بانتظار المراجعة والموافقة من المستخدم بعد إرسال ZIP كامل للمشروع.**
