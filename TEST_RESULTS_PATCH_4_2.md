# نتائج الاختبار الفعلية — Phase 4 Final Returns Integrity Patch 4.2 (ترحيلات 0099–0105)

كل الأرقام أدناه من تشغيل فعلي في هذه الجلسة ضد قاعدة بيانات مبنية من الصفر (`local_harness_setup.sql` + الترحيلات 0001–0105 بالترتيب دون توقف + `supabase/seed.sql`). لا رقم افتراضي أو منسوخ من تشغيل سابق — كل تشغيلة أُعيدت فعليًا عند كتابة هذا الملف، بما فيها إعادة تشغيل كل ملف بعد تصحيح مشكلة بيئة محلية (كلمة مرور `postgres` المحلية) لم تكن لها أي علاقة بمنطق Patch 4.2 نفسه.

## 1) بناء قاعدة البيانات من الصفر

- `local_harness_setup.sql` → نجح.
- الترحيلات 0001–0105 بالترتيب دون توقف → **105/105 نجحت** (الجديدة لهذه الرقعة: 0099–0105، سبع ترحيلات، فوق Patch 4.1 القائمة 0082–0098 دون أي تعديل عليها).
- `supabase/seed.sql` → نجح.

## 2) اختبار الترقية العام (Foundation → latest، بلا إعادة تشغيل seed.sql)

`scripts/run_upgrade_test.sh` يُطبِّق 0040–0105 تلقائيًا عبر glob بلا تعديل على السكربت نفسه:

```
=== ALL UPGRADE-FROM-0039 TESTS PASSED (0040-latest, including Phase 3 0058-0064, applied WITHOUT re-running seed.sql) ===
==> Upgrade test PASSED
```

**11/11 نقطة تحقق ناجحة.** لم يتطلَّب أي تعديل توقعات جديد — Patch 4.2 لا تضيف أي صلاحية أو جدول ضمن نطاق فحص هذا الاختبار العام (الذي يتوقف عند التحقق من صلاحيات/بيانات Phase 2/Phase 3 الأساسية فقط، ولا يمسّ بيانات Returns إطلاقًا).

## 3) إثبات سلامة الترقية المخصَّص لبيانات Returns القديمة سابقة لـPatch 4.2 (البند 10 من مواصفة المستخدم)

`scripts/run_upgrade_test.sh` العام لا يُغطّي بيانات Returns فعليًا (خارج نطاقه أصلًا)، وحتى إثبات Patch 4.1 المخصَّص (`TEST_RESULTS_PATCH_4_1.md`، القسم 3) بُنِي كإثبات مؤقَّت في جلسته الخاصة ولم يُحفَظ كملف دائم. لهذه الرقعة، بُنِي إثبات دائم **مُضاف للمستودع نفسه** (وليس سكربتًا مؤقَّتًا فقط):

- **الحصون (Fixture):** `supabase/tests/fixtures/patch_4_2_legacy_upgrade_pre_fixture.sql` — يُطبَّق على قاعدة بُنِيت حتى ترحيلة **0091 فقط** + `seed.sql` (قبل Patch 4.1/4.2 كليًا)، ويُنشئ بيانات Returns حقيقية بتوقيعات الدوال **القديمة** (ما قبل 0092: `create_sales_return(p_item_ids uuid[])`، `approve_sales_return()`، `reverse_sales_return()`، `update_sales_order()` غير المُعدَّلة منذ 0084) في ثلاث حالات:
  - **`P42-PENDING`** — بيعة بسعر 1000.00، مرتجع قيد المراجعة يُنشأ عليها، **ثم تُعدَّل البيعة نفسها بعد إنشاء المرتجع** (`update_sales_order()` القديمة ترفع سعر البند إلى 1200.00) — هذا بالضبط السيناريو الذي كشف عنه 0099 كثغرة في Backfill الرجعي لِـ0092 (انظر القسم 4 أدناه).
  - **`P42-APPROVED`** — بيعة بسعر 800.00، مرتجع يُنشأ ويُعتمَد بالدالة القديمة — يترك الحقول المالية الثلاثة الجديدة (`returned_original_sale_amount`/`recovered_original_cost_amount`/`net_sales_profit_adjustment`) بقيمة `NULL` (الدالة القديمة لا تعرفها إطلاقًا).
  - **`P42-REVERSED`** — نفس الشيء، لكن يُتراجَع عن اعتماده لاحقًا بالدالة القديمة — نفس هدف الـBackfill، لكن يجب ألا يُحتسَب ضمن `adjusted_order_net_sales_profit` بعد الإصلاح.
- **اختبار ما بعد الترقية:** `supabase/tests/upgrade_patch_4_2_legacy_returns.test.sql` — يُشغَّل في اتصال `psql` **منفصل تمامًا** بعد تطبيق 0092–0105 فوق الحصون أعلاه (محاكاة حرفية لترقية إنتاج حقيقية) — يُعيد التعرُّف على الصفوف الثلاثة عبر `sales_orders.customer_name` (لا حالة جلسة تعبر بين الاتصالين).
- **السكربت المُنسِّق:** `scripts/run_upgrade_test_patch_4_2.sh` — يبني قاعدة رميّة من الصفر، يطبِّق 0001–0091 + `seed.sql` الحقيقي، يُشغِّل الحصون، يطبِّق 0092–latest، ثم يُشغِّل اختبار ما بعد الترقية — كل خطوة بأمر `psql -v ON_ERROR_STOP=1` منفصل، تمامًا كما تتصرَّف ترقية إنتاج حقيقية.

**النتيجة الفعلية لهذه الجلسة (`scripts/run_upgrade_test_patch_4_2.sh` كاملًا):**

```
ALL upgrade_patch_4_2_legacy_returns.test.sql ASSERTIONS PASSED (Section 1 requires_sale_refresh remediation path A, Section 2 legacy financial backfill B/C)
==> Patch 4.2 legacy-upgrade test PASSED
```

**11/11 نقطة تحقق ناجحة**، مقسَّمة على ثلاثة مسارات:

| المسار | ما أُثبِت |
|---|---|
| **(A) `P42-PENDING`** | `requires_sale_refresh` (0099) أُعيد بناؤه `true` تلقائيًا لكل مرتجع كان `pending` وقت الترقية (A.1)؛ التباعُد **حقيقي** لا مجرد علم منفصل عن الواقع — لقطة البند تبقى 1000.00 القديمة بينما السعر الفعلي الحالي للبيعة أصبح 1200.00 (A.2)؛ `approve_sales_return()` (0101) يرفض الاعتماد صراحةً بالرسالة الدقيقة "يجب تحديث بيانات المرتجع من عملية البيع قبل اعتماده." طالما العلم `true` (A.3)؛ `refresh_pending_sales_return_from_sale()` (0100) يمسح العلم ويُعيد أخذ اللقطة على السعر **الحقيقي الحالي** 1200.00 (A.4)؛ الاعتماد بعدها ينجح ويحتسب استرجاع الإيراد على 1200.00 الصحيح لا 1000.00 القديم (A.5). |
| **(B) `P42-APPROVED`** | الحقول المالية الثلاثة الجديدة لم تعد `NULL` بعد Backfill 0099 (B.1)؛ `net_sales_profit_adjustment` المُعاد بناؤه يطابق صيغة 0099 تمامًا بالحساب الذاتي من حقول الصف نفسه (لا رقم مُثبَّت يدويًا) — `-684.35` فعليًا لهذا المرتجع (B.2)؛ `returned_original_sale_amount = 800.00` يطابق سعر البيع الأصلي (B.3)؛ `get_sales_return()`'s `adjusted_order_net_sales_profit` يحسب بشكل صحيح باستخدام الحقل المُعاد بناؤه (B.4). |
| **(C) `P42-REVERSED`** | نفس الـBackfill يمتد لمرتجع `reversed` قديم أيضًا (C.2)؛ لكن `adjusted_order_net_sales_profit` على طلب هذا المرتجع يساوي `net_sales_profit` الأساسي **فقط** — مُستثنى دائمًا من المجموع (`status='approved'` فقط تُحتسَب) رغم أن حقوله المالية الخاصة أُعيد بناؤها بنجاح عبر نفس الـBackfill (C.3). |

**الخلاصة:** الثغرة الحقيقية في Backfill 0092 (توثيق كامل في رأس ترحيلة 0099 نفسها) كانت اعتماد `approve_sales_return()` على مطابقة `source_sale_row_version` وحدها، والتي يمكن أن تتطابق صدفةً حتى مع لقطة بند قديمة فعليًا (لو لم تُعدَّل البيعة أكثر من مرة بعد الترقية). `requires_sale_refresh` يُصلِح هذا عبر علم مستقل تمامًا لا يعتمد على أي حساب أرقام إصدار — Backfill 0099 يضع `true` لكل مرتجع كان `pending` وقت الترقية دون استثناء (أوسع مما يلزم أحيانًا، لكن أضمن — "أقصى شيء سيحتاج Refresh صريح مرة واحدة" كما تنص المواصفة صراحةً).

## 4) اختبارات SQL — كل الملفات، أرقام فعلية من هذا التشغيل (تشغيل واحد متسلسل على نفس قاعدة البيانات، 0001–0105)

| الملف | النتيجة |
|---|---|
| `rls_and_permissions.test.sql` | 139/139 ✅ |
| `financial_master_data.test.sql` | 52/52 ✅ |
| `financial_integrity_patch_2_1.test.sql` | 34/34 ✅ |
| `financial_integrity_patch_2_2.test.sql` | 17/17 ✅ |
| `financial_integrity_hotfix_2_2_1.test.sql` | 14/14 ✅ |
| `financial_integrity_hotfix_2_2_2.test.sql` | 11/11 ✅ |
| `sales_core.test.sql` | 57/57 ✅ |
| `sales_integrity_patch_3_1.test.sql` (Part 1، أحادي الجلسة) | 14/14 ✅ |
| `sales_integrity_patch_3_1_concurrency.test.sql` (Part 2، تزامن حقيقي عبر `dblink`) | 10/10 ✅ |
| `sales_integrity_patch_3_2.test.sql` | 15/15 ✅ |
| `sales_integrity_hotfix_3_2_1.test.sql` | 6/6 ✅ |
| `upgrade_from_0039.test.sql` (مُشغَّل أيضًا مباشرةً ضد القاعدة الكاملة 0001–0105، إضافة لتشغيله المرجعي عبر `run_upgrade_test.sh` أعلاه) | 11/11 ✅ |
| `sales_returns_core.test.sql` (**مُوسَّع** لهذه الرقعة — يضيف 5 أقسام جديدة Q/R/S/U/V فوق الـ18 قسمًا القائمة من Patch 4.1 A–P، بلا حذف أو تعديل لأي سيناريو قائم) | **77/77 ✅** |
| `sales_returns_concurrency.test.sql` (**مُوسَّع** — يضيف R4/R5/R6 فوق R1–R3 القائمة من Patch 4.1، تزامن حقيقي عبر `dblink`) | **R1–R6 — 6/6 سيناريوهات ✅** |

**المجموع: 457 نقطة تحقق SQL ناجحة (باستثناء ملف التزامن المُحتسَب بالسيناريوهات) + 6/6 سيناريوهات تزامن حقيقية + 11/11 نقطة تحقق في إثبات الترقية المخصَّص لهذه الرقعة (القسم 3 أعلاه)، كلها في تشغيلة واحدة متسلسلة على قاعدة بيانات واحدة مبنية من الصفر.** صفر انحدار على أي من الـ380 نقطة تحقق/سيناريو القائمة من كل المراحل والرقعات السابقة (بما فيها الـ54 نقطة + 3 سيناريوهات الخاصة بـPatch 4.1 نفسها).

### تفصيل الأقسام الجديدة في `sales_returns_core.test.sql` (Q/R/S/U/V — سيناريوهات Patch 4.2 الإلزامية)

- **Q — `requires_sale_refresh` حارس مستقل وأقوى من `source_sale_row_version` (Section 1):** مرتجع طازج يبدأ `requires_sale_refresh=false`؛ محاكاة Backfill 0099 (`update ... set requires_sale_refresh=true`) تُثبِت أن `get_sales_return()` يعرض العلم بصدق؛ `approve_sales_return()` يرفض بالرسالة الصريحة طالما العلم `true`؛ `refresh_pending_sales_return_from_sale()` يمسحه فيسمح بالاعتماد بعدها.
- **R — دورة حياة تسوية الاسترداد الكاملة عبر مسار التسجيل (Section 3/4):** تسوية نهائية → محاولة تسجيل استرداد مباشرة تُرفَض (التسوية مُغلَقة) → إعادة فتح (تتطلَّب سببًا إلزاميًا) → تسجيل استرداد جديد ينجح الآن → تسوية نهائية ثانية تُنشئ سجل تاريخ **ثالثًا** منفصلًا، دون محو السجل الأول أبدًا — `reconciliation_history` ينمو 1→2→3 (`finalized`→`reopened`→`finalized`).
- **S — نفس دورة الحياة، عبر مسار التراجع عن حدث استرداد (`reverse_sales_return_refund_event()`) بدل التسجيل المباشر.**
- **U — تسلسل التواريخ الزمنية (Section 6):** تاريخ استرداد/تراجع استرداد/تراجع مرتجع قبل تاريخ اعتماد المرتجع أو تاريخ المرتجع نفسه — مرفوض برسالة دقيقة؛ تاريخ تراجع استرداد قبل تاريخ الاسترداد الأصلي نفسه — مرفوض؛ تاريخ مستقبلي — لا يزال مرفوضًا (لم ينكسر بهذه الرقعة)؛ تراجع بتاريخ اليوم نفسه صحيح وينجح.
- **V — تكافؤ `preview_sales_return()` مع شجرة تحقق الإنشاء/الاعتماد (Section 5):** معاينة بمدخلات جزئية/تدريجية تنجح؛ `customer_never_received` + `not_collected` تقترح صفرًا؛ شجرة التحقق (سبب الخصم، سبب فرق الاسترداد) مطابقة تمامًا لما يفرضه `create_sales_return()`؛ قيمة `approved_refund_amount` المُدخَلة يدويًا تُعاد كما هي مع `refund_variance` صحيح — لا تُستبدَل صامتًا أبدًا.

### تفصيل السيناريوهات الجديدة في `sales_returns_concurrency.test.sql` (R4/R5/R6 — Section 9، تزامن حقيقي عبر `dblink`)

- **R4 (Section 9-A):** اتصال A يُسوِّي (`finalize_sales_return_refund()`) مرتجعًا ويُبقي معاملته مفتوحة؛ اتصال B يحاول تسجيل استرداد جديد بالتوازي على نفس المرتجع — يُحجَب فعليًا (مُثبَت عبر `dblink_is_busy`)، ثم يُرفَض بوضوح بعد التزام A ("تم إغلاق تسوية استرداد هذا المرتجع بالفعل — يجب إعادة فتح التسوية أولًا")؛ `refund_finalized_at` يبقى غير فارغ (تحقُّق مباشر من الجدول).
- **R5 (Section 9-B):** نفس الشكل، لكن محاولة B هي `reverse_sales_return_refund_event()` على حدث استرداد نشط — نفس الحجب الفعلي، نفس الرفض الواضح بعد الالتزام، وحالة الحدث تبقى `active` دون تغيير.
- **R6 (Section 9-C):** بعد إعادة فتح شرعية لتسوية مُغلَقة، اتصال A يسجِّل استردادًا إضافيًا ويُبقي معاملته مفتوحة؛ اتصال B يحاول تسوية نهائية بالتوازي — يُحجَب فعليًا، ثم **ينجح** بعد التزام A، والمجموع النهائي `actual_refunded_total='550.00'` يعكس استرداد A **المُلتزَم فعليًا** بدقة (لا لقطة قديمة قبل القفل) — لا حالة يمكن فيها ألا تطابق التسوية النهائية السجل الفعلي، حتى تحت تزامن حقيقي.

الستة خارج معاملة `begin/rollback` (تلتزم فعليًا، مطابقةً لطبيعة اختبار `dblink`) — مع تنظيف صريح يحذف أولًا من `sales_return_refund_reconciliation_events` (اكتُشِف أن مفتاحها الأجنبي `ON DELETE RESTRICT` عمدًا — جدول تاريخي إلحاقي بحت لا يجوز أن يختفي بصمت) ثم بقية الجداول، مُثبَت أيضًا بنجاح فعلي وصفر بيانات متبقية.

## 5) اختبار HTTP/PostgREST الحقيقي — نتائج فعلية

`scripts/run_postgrest_http_test.sh` (ثنائي PostgREST v12.2.3 حقيقي، JWT حقيقي مُوقَّع، `@supabase/postgrest-js`، لا محاكاة):

```
=== ALL REAL HTTP/POSTGREST INTEGRATION TEST ASSERTIONS PASSED ===
```

**77/77 تأكيدًا ناجحًا** (64 من Patch 4.1 وما قبلها بصفر انحدار + 13 جديدة لهذه الرقعة). أبرز ما أُثبِت عبر HTTP فعلي (لا محاكاة محلية) لهذه الرقعة تحديدًا:

- الدوال الثلاث الجديدة `returns_operable_store_lookups()`/`returns_visible_store_lookups()`/`returns_refund_method_lookups()` (Section 7) تُعيد فعليًا `{id, name_ar}` فقط، وكل واحدة تُرفَض بشكل مستقل عن الأخرى بحسب الصلاحية المحدَّدة لها (لا `stores.view`/`payment_methods.view` إطلاقًا) — مُمثِّل يملك `returns.view` فقط ينجح على نطاق العرض ويُرفَض على نطاق التشغيل واسترداد العمولة.
- `preview_sales_return()` يقبل فعليًا `p_scenario` الجديد عبر HTTP.
- `get_sales_return()` يعرض `requires_sale_refresh` كـ`boolean` حقيقي (`false` لمرتجع طازج) عبر HTTP.
- `reopen_sales_return_refund_reconciliation()` يُعيد فتح تسوية مُغلَقة فعليًا عبر HTTP بسبب إلزامي، وبعدها ينجح تسجيل استرداد جديد كان سيُرفَض قبل إعادة الفتح؛ `reconciliation_history` ينمو من مُدخَل واحد إلى ثلاثة (`finalized`→`reopened`→`finalized`) دون محو أي مُدخَل سابق، مع تسوية ثانية تحمل فارقًا حقيقيًا (`finalized_with_variance`) تُثبِت مسار سبب الفارق أيضًا عبر HTTP حقيقي.

## 6) فحوصات الطبقة الأمامية (TypeScript/React) — نتائج فعلية

- `npx tsc --noEmit` → **صفر أخطاء** (شمل كل الملفات المُعدَّلة: `src/types/database.ts`، مكوّنات `return-entry-form.tsx`/`refund-events-panel.tsx`/`returns-filters.tsx`، `src/app/(app)/returns/{page,[id]/page}.tsx`، `return-lifecycle-actions.tsx`).
- `npx eslint .` → **صفر أخطاء وتحذيرات** عبر المشروع بالكامل.
- `npx vitest run` → **47/47 ناجح عبر 6 ملفات** (بلا تغيير — Patch 4.2 لم يمسّ منطق Decimal/وحدة اختبار قائمة).
- `npm run check:numeric-types` → **نجح، 46/46 عمود NUMERIC مطابق** لِـ`number` في `database.ts` (زيادة 3 أعمدة عن Patch 4.1 — 43 + `sales_return_refund_reconciliation_events.actual_refunded_total`/`approved_refund_amount`/`variance` — كلها مطابقة لسلوك PostgREST الفعلي، لا `string` وهمي)، مُشغَّل فعليًا ضد قاعدة تحمل الترحيلات كاملة حتى 0105.
- `npm run build` (Next.js/Turbopack) → **نجح**، بلا مسارات جديدة (Patch 4.2 يوسِّع نفس مسارات Returns الأربعة القائمة من Phase 4/Patch 4.1، لا يضيف مسارات جديدة).

## 7) خلاصة

**457 نقطة تحقق SQL + 6/6 سيناريوهات تزامن حقيقية + 11/11 نقطة تحقق ترقية مخصَّصة** (380 من كل المراحل/الرقعات السابقة بصفر انحدار + 88 جديدة أو مُعاد فحصها بالكامل لهذه الرقعة: 23 في `sales_returns_core.test.sql`، 3 سيناريوهات جديدة في `sales_returns_concurrency.test.sql`، 11 في إثبات الترقية المخصَّص الجديد)، **77 تأكيدًا HTTP/PostgREST حقيقيًا** (13 جديدة)، **47 اختبار Vitest**، **46/46 عمود NUMERIC**، وفحوصات `tsc`/`eslint`/`next build` نظيفة بالكامل — كلها من تشغيلة فعلية في هذه الجلسة على قواعد بيانات مبنية من الصفر، لا أرقام مفترَضة أو منسوخة. **لم تبدأ Shipping ولا Settlements ولا Services/Adjustments ولا Inventory ولا Reports — العمل متوقف الآن نهائيًا، بانتظار المراجعة والموافقة من المستخدم بعد إرسال ZIP هذا التسليم.**
