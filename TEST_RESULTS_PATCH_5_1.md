# نتائج الاختبار الفعلية — Phase 5 Shipping Integrity Patch 5.1

كل الأرقام أدناه من تشغيلات فعلية في هذه الجلسة ضد قواعد بيانات مبنية من الصفر (`local_harness_setup.sql` + الترحيلات 0001–0130 بالترتيب دون توقف + `supabase/seed.sql`). لا رقم افتراضي أو منسوخ من تشغيل سابق — كل تشغيلة أُعيدت فعليًا عند كتابة هذا الملف، بما فيها إعادة بناء عدة قواعد بيانات مؤقتة منفصلة أثناء تتبّع عدة عيوب حقيقية اكتُشِفت وأُصلِحت داخل ملفات الاختبار نفسها (انظر الأقسام أدناه).

## 1) بناء قاعدة البيانات من الصفر

- `local_harness_setup.sql` → نجح.
- الترحيلات 0001–0130 بالترتيب دون توقف → **130/130 نجحت** (الجديدة: 0122–0130، تسع ترحيلات فوق Phase 5 الأصلية دون تعديل أي منها).
- `supabase/seed.sql` → نجح (بلا أي تغيير في بذور Phase 5 الأصلية — Patch 5.1 لا يضيف بيانات مؤسِّسة جديدة).

## 2) إثبات أمان الترقية (Upgrade Safety) — ثلاثة مسارات ترقية قائمة

تشغيل السكربتات الثلاثة القائمة فعليًا ضد الترحيلات الجديدة 0122–0130 مضافة فوق كل مسار:

| السكربت | يُثبِت | النتيجة |
|---|---|---|
| `run_upgrade_test.sh` | ترقية من قاعدة ما قبل Phase 5 (حتى 0112) ببيانات بيع حقيقية عبر `create_sales_order()`، ثم تطبيق 0113–0130 كاملة | **نجح** |
| `run_upgrade_test_patch_4_2.sh` | ترقية من قاعدة Patch 4.2 (حتى 0105) ببيانات مرتجعات حقيقية | **نجح** |
| `run_upgrade_test_hotfix_4_2_1.sh` | ترقية من قاعدة Hotfix 4.2.1 (حتى 0112) | **نجح** |

**3/3 مسارات ترقية نجحت، صفر تغيير على أي صف بيانات موروث** — تثبت أن الترحيلات 0122–0130 (إغلاق RLS، قيد GIST، مُشغِّل قفل جدول، لقطات جديدة، إعادة كتابة `get_shipment()`/`list_shipments()`) قابلة للتطبيق بأمان فوق أي قاعدة إنتاجية حقيقية بها بيانات Phase 5 أو ما قبلها.

## 3) اختبارات SQL — كل الملفات، أرقام فعلية من هذا التشغيل (تشغيل واحد متسلسل على نفس قاعدة البيانات، 0001–0130)

| الملف | النتيجة |
|---|---|
| كل ملفات الاختبار القائمة من Foundation حتى Hotfix 4.2.1 (19 ملفًا، بلا تعديل منطقي) | **بلا انحدار، صفر أخطاء** |
| `shipping_core_phase5.test.sql` (**مُعدَّل** — القسمان 45، 49 أُعيد كتابتهما، انظر تفصيلًا أدناه) | **6/6 أقسام ناجحة ✅ بعد التعديل** |
| `shipping_core_phase5_concurrency.test.sql` (**مُعدَّل** — قسم G جديد لبند 16) | **A–G — 7/7 سيناريوهات تزامن حقيقية ناجحة ✅** |
| `shipping_integrity_patch_5_1.test.sql` (**جديد بالكامل**، تغطية مباشرة للبنود 1–18، 21، 23) | **ناجح بالكامل ✅** |

**الإجمالي: 17/17 ملف اختبار SQL نجح ضمن نفس التشغيلة المتسلسلة، صفر انحدار.**

### 3.1) تعديل مُتعمَّد على `shipping_core_phase5.test.sql` — القسم 45 (رسوم شحن الإرجاع)

الاختبار القديم كان يفترض أن تجاوز رسوم شحن الإرجاع القياسية (35.00 → 60.00) **ينجح بلا سبب** — بالضبط السلوك الذي يُصلِحه بند 9 من Patch 5.1. أُعيد كتابة القسم ليؤكد:

1. **S45.3 — الرفض أولًا:** محاولة إنشاء شحنة إرجاع برسوم مختلفة عن القيمة المؤسَّسة (35.00) **بلا** `p_customer_return_shipping_charge_override_reason` → تُرفَض فعليًا.
2. **S45.4–S45.8 — القبول مع السبب:** نفس المحاولة مع سبب حقيقي (`'S45: العميل طلب توصيلًا سريعًا للإرجاع'`) → تنجح، واللقطة الكاملة تُحفَظ بدقة: `customer_shipping_charge=60.00`، `customer_return_shipping_fee_standard_amount=35.00` (القياسي وقت الإنشاء، محفوظ منفصلًا)، `customer_return_shipping_charge_is_override=true`، `customer_return_shipping_charge_override_reason` مطابق للنص المُرسَل حرفيًا.

### 3.2) تعديل مُتعمَّد على `shipping_core_phase5.test.sql` — القسم 49 (أمن الربحية)

الاختبار القديم كان يفترض أن `customer_shipping_charge` يجب أن يكون **غائبًا** عن فاعل بصلاحية `shipments.view` فقط — هذا كان العيب الحقيقي الذي يُصلِحه بند 10/22 من Patch 5.1 (رسم تشغيلي على العميل، لا ربحًا داخليًا). أُعيد كتابة الأسطر التالية:

- **S49.1/1b/1c/7b (حاضرة الآن، لا غائبة):** `customer_shipping_charge`، `effective_customer_shipping_charge`، `has_actual_carrier_cost`، `cod_timeline` — كلها ظاهرة بالكامل لفاعل `shipments.view` فقط.
- **S49.2–S49.6 (تبقى غائبة كما كانت، هذا هو خط الحماية الصحيح):** `expected_carrier_cost`، `actual_carrier_cost`، `net_shipping_expected`، `net_shipping_actual`، `financial_events` — غياب تام، لا `null`، تمامًا كما في التصميم الأصلي.

### 3.3) `shipping_integrity_patch_5_1.test.sql` — تفصيل التغطية (بنود 1–18، 21، 23)

- **بند 1 (إغلاق RLS المباشر):** محاولة `INSERT`/`UPDATE` مباشرة على `shipping_carrier_rate_versions`/`customer_return_shipping_fee_versions` بدور `authenticated` (متجاوِزًا RPC عمدًا) → **تُرفَض فعليًا بخطأ RLS**، بينما `SELECT` يبقى يعمل.
- **بند 2 (قيد استبعاد GIST):** محاولة إدراج نطاقي تاريخ متداخلين لنفس شركة الشحن/المنطقة/الاتجاه تحت `service_role` مباشرة (متجاوِزة الفحص التطبيقي في RPC عمدًا) → **تُرفَض على مستوى القاعدة نفسها** بخطأ استبعاد (`exclusion violation`)، لا فحصًا تطبيقيًا فقط.
- **بند 3 (عدم قابلية تعديل الرمز):** محاولة `UPDATE code` على شركة شحن/منطقة قائمة تحت `service_role` → تُرفَض.
- **بند 4 (تنفيذ القفل الحصري):** `acquire_shipping_rates_lock_exclusive()` قابلة للتنفيذ فعليًا تحت كل من `authenticated`و`service_role` (منح صريح، درس Hotfix 3.2.1).
- **بند 5:** إنشاء نسخة تسعير على شركة شحن/منطقة معطَّلة → يُرفَض، مع بقاء التاريخ السابق سليمًا.
- **بندان 6/21 (تزوير PostgREST):** إرسال `created_by`/`created_at` مزوَّرَين عبر PostgREST مباشرة → **تُرفَض/تُتجاهَل صامتة** (القيم الحقيقية تُفرَض من الخادم)؛ `updated_by` يُختَم بالفاعل الحقيقي دومًا.
- **بند 7 (تدقيق):** بعد إنشاء/تعديل شركة شحن/منطقة/نسخة تسعير → صفوف `audit_logs` حقيقية بالأفعال `shipping_rate.create`، `customer_return_shipping_fee.create`، `shipping_carrier.create`/`update` — **ظاهرة فعليًا** لفاعل يملك `audit_logs.view`.
- **بنود 8/9/10/23:** لقطة رسوم شحن الإرجاع محفوظة بدقة؛ التجاوز بلا سبب مرفوض؛ `customer_shipping_charge` ظاهر دومًا؛ `has_actual_carrier_cost` ظاهر دومًا (`true`/`false` بدقة) بلا كشف الرقم نفسه لفاعل بلا `sales.view_profit`.
- **بندان 15/16:** شحنة إرجاع جديدة على مرتجع `reversed` → تُرفَض؛ الشحنة القائمة مسبقًا تبقى قابلة للقراءة كاملة؛ دالة البحث الضيقة تستبعد المرتجعات المُتراجَع عنها من نتائجها.
- **بندان 13/14 (COD):** انتقالات حالة COD صحيحة تنجح؛ محاولة `UPDATE` مباشرة على `shipment_cod_events` (جدول إضافي-فقط بلا سياسات RLS لـ`authenticated`) → **لا صفوف تتأثر فعليًا** (تحقُّق بمقارنة الحالة قبل/بعد عبر `is not distinct from`، لا الاعتماد على استثناء يُرمى).
- **بند 18 (تسلسل زمني):** حدث حالة أو حدث COD جديد بتاريخ/وقت أسبق من آخر حدث مسجَّل → **يُرفَض صراحة**.
- **بند 17 (لقطات تاريخية):** إعادة تسمية شركة شحن بعد إنشاء شحنة → الشحنة القديمة تحتفظ بـ`carrier_name_snapshot`/`carrier_code_snapshot` الأصليين رغم إعادة التسمية.
- **بند 11 (فلاتر بحث مُصحَّحة):** `shipments_filter_carrier_lookups()`/`shipments_filter_zone_lookups()` تعملان لفاعل `shipments.view` فقط؛ الدالتان القديمتان `shipments_carrier_lookups()`/`shipments_zone_lookups()` (المقيَّدتان بـ`shipments.create`) تبقيان صحيحتين في رفضهما لنفس الفاعل — إثبات أن الإصلاح أضاف مسارًا جديدًا صحيحًا دون تخفيف حماية المسار القديم.
- **بند 12 (فلاتر `list_shipments()`):** الفلترة بـ`order_number` الحقيقي (مُستخرَج فعليًا عبر `get_sales_order(v_order_id)->>'order_number'`، لا تسمية بيانات الاختبار) تُعيد الصف الصحيح فقط؛ كذلك `return_number`/`original_sale_store_id`/`cod_collection_state`.

**عيوب حقيقية اكتُشِفت وأُصلِحت أثناء كتابة هذا الملف بالذات في هذه الجلسة (قبل أي تسليم):**

1. صلاحية `audit_logs.view` كانت ناقصة عن ممثِّل الاختبار الكامل الصلاحيات — أدَّى غيابها إلى فشل تأكيدات بند 7 بـ"0 سجل تدقيق" رغم أن السجلات كانت موجودة فعليًا (محجوبة بـRLS، لا مفقودة) — أُصلِح بإضافة الصلاحية لقائمة منح الممثِّل.
2. اسم فعل تدقيق افتراضي خاطئ (`shipping_carrier.insert`) — الاسم الفعلي حسب مُشغِّل 0124 هو `shipping_carrier.create` — أُصلِح.
3. **false-positive** في تأكيد عدم قابلية تعديل `shipment_cod_events`: منطق معالجة الاستثناء كان يفترض أن "عدم رمي استثناء" = عيب حقيقي دومًا، متجاهلًا أن الجدول **بلا أي سياسة RLS** لـ`authenticated` أصلًا (`UPDATE` من دور غير مخوَّل يطابق 0 صفوف بصمت، لا يرمي استثناء) — أُصلِح بمقارنة الحالة قبل/بعد مباشرة عبر `is not distinct from` بدل الاعتماد على معالج استثناء.
4. خطأ `format()`: استخدام `%` بدل `%s` داخل استدعاء `format(...)` → `unrecognized format() type specifier` — أُصلِح.
5. `assert v_state_after = v_state_before` يفشل زائفًا حين تكون القيمتان `NULL` معًا (`NULL = NULL` يُقيَّم `NULL` لا `TRUE`) — أُصلِح باستخدام `is not distinct from`.
6. افتراض خاطئ أن الوسيط الموضعي السادس لـ`create_sales_order()` هو رقم البيعة — هو فعليًا `p_customer_name`؛ `order_number` مُولَّد دومًا من النظام (مثل `SALE-0000000090`) — أُصلِح باستخراج الرقم الحقيقي عبر `get_sales_order(v_order_id)->>'order_number'` قبل الفلترة.
7. سطر `raise notice` نهائي خارج أي كتلة `do $ ... $` (صياغة غير صالحة، `raise` حصرية لـPL/pgSQL) — أُصلِح باستبداله بـ`select '...' as result;` مطابقةً لاتفاقية بقية ملفات `.test.sql`.

### 3.4) `shipping_core_phase5_concurrency.test.sql` — قسم G الجديد (بند 16)

سيناريو تزامن حقيقي عبر `dblink` (لا محاكاة تسلسل استدعاءات): جلسة A تستدعي `reverse_sales_return()` وتُبقي المعاملة مفتوحة (قفل صف حقيقي على المرتجع)، جلسة B تحاول في نفس اللحظة `create_shipment()` باتجاه `return` لنفس المرتجع:

- B **تُحجَب فعليًا** طوال معاملة A المفتوحة (إثبات انتظار حقيقي، لا افتراض).
- بعد التزام A (المرتجع أصبح `reversed`) → B **تُرفَض صراحة**، لا تنجح بصمت بحالة متضاربة.
- الحالة النهائية للمرتجع: `reversed` بدقة؛ **صفر شحنات** موجودة لهذا المرتجع.

**عيب حقيقي اكتُشِف وأُصلِح أثناء كتابة هذا القسم:** الكتلة الثانية استدعت `get_sales_return()` مباشرة دون إعادة تعيين `set local role authenticated; set local request.jwt.claims = ...` أولًا — لأن كل عبارة `do $ ... $;` علوية في ملف بلا `BEGIN` محيط هي معاملتها الضمنية المستقلة، فإعدادات `SET LOCAL` من الكتلة السابقة لا تنتقل، ما جعل الاستدعاء يعمل بصلاحيات المستخدم الفائق الافتراضية (`auth.uid()` يُحل إلى `NULL`) ويفشل برسالة صلاحيات مضلِّلة. شُخِّص بالتحقق المباشر من `get_user_permissions()` (أثبت أن المنح كانت موجودة فعليًا) ثم بسكربت معزول أثبت أن `has_permission('returns.view')` تُرجِع `true` حين يُضبَط الدور/الادعاءات بشكل صحيح. أُصلِح باستبدال استدعاء RPC بقراءة مباشرة تحت المستخدم الفائق من `public.sales_returns` — مطابقةً للاتفاقية القائمة فعليًا في اختباري A/F لنفس الملف.

**النتيجة النهائية: A–G، 7/7 سيناريوهات تزامن حقيقية ناجحة**، على قاعدة بيانات طازجة تمامًا.

## 4) اختبار HTTP/PostgREST الحقيقي — فجوة معروفة، مُفصَح عنها صراحة

خلافًا لِـPhase 5 الأصلية (التي شملت 20 تأكيدًا HTTP جديدًا عبر ثنائي PostgREST حقيقي)، **لم يُشغَّل قسم HTTP/PostgREST حقيقي مخصَّص لـPatch 5.1 في هذه الجلسة**. كل تحقق من بنود Patch 5.1 (1–23) تم عبر اتصال SQL مباشر بمحاكاة دور `authenticated` (`set local role authenticated; set local request.jwt.claims = ...`) — وهي نفس آلية فرض RLS التي يطبِّقها PostgREST فعليًا خلف الكواليس، وقد كشفت بالفعل عيوبًا حقيقية (انظر القسم 3.3) — لكنها **ليست** اختبار HTTP فعليًا عبر الشبكة ضد ثنائي PostgREST حي كالذي أثبت عيب `preview_*` في Phase 5 الأصلية. هذا يُوثَّق هنا كفجوة معروفة يجب سدها في تسليم لاحق، لا كنتيجة اختبار ناجحة يُدَّعى إجراؤها.

## 5) فحوصات الطبقة الأمامية (TypeScript/React) — نتائج فعلية

- `npx tsc --noEmit` → **صفر أخطاء** (شمل مكوّن `shipment-cod-state-action.tsx` الجديد، وحدة `src/features/shipping-rates/*` الجديدة بالكامل، وصفحة `/master-data/shipping-rates` الجديدة، و`src/types/database.ts` بعد تحديث شامل لأنواع/دوال Shipping).
- `npx eslint .` → **صفر أخطاء وتحذيرات** عبر المشروع بالكامل (تشغيلتان: منتصف الجلسة وبعد التعديلات الأخيرة على ملف اختبار التزامن).
- `npx vitest run` → **52/52 ناجح عبر 7 ملفات** (بلا تغيير — Patch 5.1 لم يضف/يمسّ أي وحدة اختبار Vitest قائمة).
- `npm run check:numeric-types` → **نجح، 56/56 عمود NUMERIC مطابق** لِـ`number` في `database.ts`. **عيب انحراف حقيقي اكتُشِف وأُصلِح أثناء هذا الفحص بالذات:** `shipments.customer_return_shipping_fee_standard_amount` (عمود جديد من 0125) لم يكن له حقل مطابق في نوع `Row` — أُصلِح بإضافة كل الأعمدة الجديدة الثمانية (0125/0129) إلى `database.ts`.
- `npm run build` (Next.js/Turbopack) → **نجح**، بزيادة مسار إداري واحد: `/master-data/shipping-rates`.

## 6) خلاصة

**17/17 ملف اختبار SQL قائم نجح ضمن قاعدة واحدة طازجة 0001–0130 (بما فيها قسمان أُعيد كتابتهما عمدًا ليعكسا سلوكًا أصحّ)** + **اختبار `shipping_integrity_patch_5_1.test.sql` الجديد ناجح بالكامل، تغطية مباشرة لجميع بنود 1–18/21/23** + **A–G، 7/7 سيناريوهات تزامن حقيقية** (قسم G الجديد لبند 16) + **3/3 إثباتات ترقية ناجحة على ثلاثة مسارات مختلفة** + **صفر أخطاء TypeScript/ESLint** + **52 اختبار Vitest** + **56/56 عمود NUMERIC** + **بناء إنتاجي ناجح** — كلها من تشغيلات فعلية في هذه الجلسة، لا أرقام مفترَضة أو منسوخة، بما في ذلك عدة عيوب حقيقية اكتُشِفت وأُصلِحت أثناء كتابة الاختبارات نفسها (موثَّقة بالتفصيل في القسم 3 أعلاه). **فجوة واحدة مُفصَح عنها صراحة:** لا اختبار HTTP/PostgREST حقيقي مخصَّص لهذا الملحق (القسم 4). **لم تبدأ Settlements ولا Services/Adjustments ولا Inventory ولا Reports ولا أي تكامل شركة شحن — العمل متوقف الآن نهائيًا، بانتظار المراجعة والموافقة من المستخدم بعد إرسال ZIP كامل للمشروع بأكمله (لا ZIP فروقات).**
