# نتائج اختبار Phase 6 — Integrity Patch 6.1: سلامة Services/Adjustments المالية والأمنية (ترحيلات 0144–0156)

هذا الملف يوثِّق كل خطوة تحقُّق نُفِّذت فعليًا في هذه الجلسة لِـPatch 6.1، بالأرقام الحقيقية الناتجة عن كل تشغيلة. يقابله في `DELIVERY_REPORT.md` **الملحق السادس والعشرون**.

## 1) بناء قاعدة اختبار من الصفر بالكامل

حُذفت قاعدة الاختبار وأُعيد إنشاؤها، ثم طُبِّق `supabase/tests/local_harness_setup.sql`، ثم **كل الترحيلات 0001–0156 بالترتيب دون توقف** (156/156 نجحت دون أي خطأ — فقط إشعارات `NOTICE ... skipping` المتوقعة من ترحيلات `IF NOT EXISTS`/`ON CONFLICT` قديمة)، ثم `supabase/seed.sql` كاملًا → **نجح**. تكرَّر هذا البناء عدة مرات مستقلة في هذه الجلسة (لكل مسار اختبار قاعدته الخاصة القابلة للحذف).

## 2) عيبان حقيقيان في الترحيلات الجديدة نفسها — اكتُشِفا وأُصلِحا في هذه الجلسة

اكتُشِف كلاهما عبر اختبار ترقية جديد بالكامل بُني خصيصًا لهذه الغاية (`upgrade_patch_6_1_fixtures.test.sql` + `scripts/run_upgrade_test_patch_6_1.sh`، انظر §5 أدناه) — الذي يطبِّق 0144–latest فوق بيانات **حقيقية موجودة مسبقًا** أُنشئت تحت عقود RPC/المخطط **القديمة** (0133–0143)، لا فوق جداول فارغة فقط:

1. **ترتيب خاطئ في 0144** (`adjustments_zero_charge_and_payment_reference_schema.sql`): كتلة `backfill` كانت تُنفَّذ **قبل** إسقاط القيد القديم `sales_order_adjustments_lifecycle_consistent` — فعملية تطهير سجل معتمَد بقيمة تحصيل صفرية (تصفير أعمدة لقطة طريقة الدفع/الرسم) كانت تخالف القيد **القديم** الذي يُلزِم وجودها لأي سجل معتمَد. **الإصلاح:** نقل `alter table ... drop constraint` إلى مباشرةً بعد `alter table` المخططي، **قبل** الـbackfill، مع تعليق توضيحي.
2. **مُحفِّز موجود مسبقًا يحجب الـbackfill في 0150** (`adjustment_reversal_impact_columns.sql`): محفِّز `sales_order_adjustment_reversals_reject_update` (من 0135، إضافي-فقط غير مشروط) كان يرفض تحديث الـbackfill نفسه للأعمدة الخمسة المُوقَّعة الجديدة. **الإصلاح:** تطويق عبارة الـUPDATE الوحيدة بـ`alter table ... disable trigger ...` قبلها و`... enable trigger ...` بعدها.

كلا الإصلاحين أُعيد اختبارهما فعليًا فوق بيانات ترقية حقيقية (لا سيناريو فارغ) → **صفر أخطاء**، والقيم المُعاد بناؤها صحيحة رقميًا بالضبط (تفصيل في §5).

## 3) عيب ثالث — سكربت التجهيز غير-المعاملاتي (`SET LOCAL` خارج معاملة)

`supabase/tests/fixtures/patch_6_1_upgrade_pre_fixture.sql` (يُنشئ بيانات حقيقية تحت العقود القديمة، **تُلتزَم فعليًا لا تُلغى**) كان يستخدم `set local request.jwt.claims = ...` — لكن السكربت عمدًا **بلا** `begin;`/`rollback;` (بياناته يجب أن تُلتزَم)، و`SET LOCAL` خارج كتلة معاملة **لا ينفّذ شيئًا فعليًا** (تحذير `WARNING` فقط، لا خطأ)، فكانت `request.jwt.claims` لا تُضبَط إطلاقًا → فشل `INSERT` الأول ضد `stores` بخطأ RLS. **الإصلاح:** استبدال `SET LOCAL` بـ`SET` عادية (نطاق-جلسة)، مطابقةً لـ`SET ROLE authenticated;` المجاورة التي كانت صحيحة بالفعل.

## 4) عيب رابع — خطأ صياغي في `queries.ts` كان يمنع البناء بالكامل

تعليق JSDoc لِـ`getAdjustmentDetail()` في `src/features/adjustments/queries.ts` احتوى السلسلة الحرفية `original_*/effective_*` — التتابع `*/` **ينهي أي تعليق كتلة JS/TS فورًا** بصرف النظر عن السياق، فأنهى التعليق منتصف الجملة وحوَّل بقية النص إلى "كود" يُفسَّر خطأ، منتجًا سلسلة من ~19 خطأ تحليل نحوي (`TS1005`/`TS1434`/`TS1127`/`TS1161`) بدءًا من نفس السطر. **الإصلاح:** إعادة صياغة الجملة لتفادي التتابع الحرفي `*/` (`original_ / effective_ prefixed field split`). **لا علاقة له بأي منطق أعمال** — خطأ نحوي بحت في نص تعليق، أُصلِح ولم يتكرر (تحقَّق البحث عبر كامل `src/` عن نفس النمط `_*/[a-zA-Z]` داخل تعليقات كتلة — لا وجود آخر له).

## 5) مسارا الترقية الجديدان لِـPatch 6.1

| # | السكربت | يُثبت | النتيجة |
|---|---|---|---|
| 1 | `scripts/run_upgrade_test_phase6_adjustments.sh` (0132→latest، بيانات بيع حقيقية قبل Phase 6 بالكامل) | الوحدة تعمل فور اكتمال 0133–latest (شاملة 0144–0156) فوق بيانات ما قبل Phase 6 | **PASS** |
| 2 | `scripts/run_upgrade_test_patch_6_1.sh` (0143→latest، بيانات Adjustments حقيقية أُنشئت تحت عقود RPC **القديمة** 0133–0143) | كل ما يلي أعلاه (§2/§3) — راجع تفصيل الأصناف (a–g) | **PASS** |

سكربت `run_upgrade_test_patch_6_1.sh` الجديد بالكامل ينفِّذ: بناء 0001–0143 + `seed.sql` → `patch_6_1_upgrade_pre_fixture.sql` (**مُلتزَم فعليًا**، ينشئ 5 تعديلات/خدمات حقيقية عبر RPCs **القديمة** تمامًا: معلَّق-بتكلفة، معتمَد-مدفوع، معتمَد-مجاني **بالخطأ التاريخي** لِـ§9/10 قبل 0144 — الذي يجب أن يُصحَّح بالـbackfill، معتمَد-ثم-معكوس تحت 0141 القديمة، ولقطة اسم نوع أُعيدت تسميته لاحقًا) → 0144–latest في استدعاء `psql` منفصل تمامًا (**دون إعادة تشغيل `seed.sql`**) → `upgrade_patch_6_1_fixtures.test.sql` يقرأ جدول سجلّ نطاق (`p6u61_scratch`) ويُثبت:

- **(a)** fixture معلَّق-بتكلفة بقي دون أي تغيير بعد الترقية.
- **(b)** fixture معتمَد-مدفوع بقي دون أي تغيير (`67.50`).
- **(c)/(c raw)** fixture معتمَد-مجاني: الـbackfill **صحَّح** الرسم من `5.00`→`0.00` والصافي من `-15.00`→`-10.00`، عبر RPC القراءة **وعبر قراءة مباشرة (service_role) للجدول الأساسي أيضًا** — إثبات أن الترحيلة أعادت كتابة الأعمدة فعليًا لا مجرد إخفائها في RPC.
- **(d)/(d raw)** fixture معكوس: الأعمدة الخمسة المُوقَّعة الجديدة (`customer_charge_reversal_amount=-50.00`/`direct_cost_reversal_amount=20.00`/`payment_fee_reversal_amount=0.00`/`gross_profit_reversal_amount=-30.00`/`net_profit_reversal_amount=-30.00`) امتلأت بشكل صحيح بواسطة backfill 0150 لسجل عكس أُنشئ بالكامل تحت 0141 القديمة.
- **(e)** لقطة اسم النوع التاريخية نجت من إعادة التسمية (تحت المخطط القديم) ومن ترحيل المخطط نفسه معًا.
- **(f)** المعرّفات/الأرقام/الربط بعملية البيع سليمة تمامًا (5 تعديلات كما هي متوقعة).
- **(g)** لا صلاحيات جديدة ولا مكررة أُضيفت بواسطة Patch 6.1 — **7 صلاحيات** `adjustments` بالضبط (3 من `seed.sql` + 4 من 0133، لا أكثر).

## 6) الفحص الشامل الحالي لِـ21 ملف اختبار SQL غير-تزامني + 4 تزامن (قاعدة واحدة مُعاد بناؤها بالكامل)

نُفِّذت 20 ملفًا غير-ترقية بالتسلسل على نفس القاعدة (156 ترحيلة + seed):

| المجموعة | العدد | النتيجة |
|---|---|---|
| 16 ملفًا قائمًا من مراحل سابقة (Financial/Sales/Returns/Shipping/RLS، غير تزامني) | 16 | **PASS** |
| `adjustments_core_phase6.test.sql` (منطق Phase 6 الأساسي، غير مُعدَّل بذاته هذه الجلسة لكن يُعيد التحقق فوق 0144–0156) | 1 | **PASS** |
| 4 ملفات تزامن حقيقي (`dblink`): `sales_integrity_patch_3_1_concurrency`/`sales_returns_concurrency`/`shipping_core_phase5_concurrency`/`adjustments_core_phase6_concurrency` | 4 | **PASS** |

**ملاحظة منهجية مهمة:** التشغيلة الأولى لهذه المجموعة أظهرت فشلًا ظاهريًا واحدًا في `rls_and_permissions.test.sql` (توقَّع 3 متاجر نشطة لفاعل all-scope، وجد 9). التحقيق أثبت أن السبب **ليس انحدارًا حقيقيًا** بل عيب في منهجية السكربت المؤقت نفسه: `adjustments_core_phase6_concurrency.test.sql` (يسبقه أبجديًا) يُنشئ 6 متاجر تزامن **مُلتزَمة فعليًا خارج أي معاملة** (متطلَّب بنيوي لاختبارات `dblink` الحقيقية، موثَّق صراحة في رأس الملف: "NOT safe to run against a shared/staging database")، فتراكمت 3+6=9 في نفس القاعدة المُشتركة. أُعيد تشغيل `rls_and_permissions.test.sql` **وحده على قاعدة مُعاد بناؤها من الصفر خصيصًا له** → **نجح بالكامل** (`=== ALL RLS/PERMISSION/AUDIT/ESCALATION TESTS PASSED ===`)، مؤكِّدًا أن الفشل الأول كان اصطناعيًا بحتًا من تسلسل السكربت المؤقت، لا من أي كود أو ترحيلة.

## 7) مسارات الترقية القديمة الثلاثة (غير Patch 6.1) — إعادة تحقق كاملة فوق سلسلة 0001–0156 بالكامل

| # | السكربت | النتيجة |
|---|---|---|
| 1 | `scripts/run_upgrade_test.sh` (Foundation 0039 → latest، بلا `seed.sql` الحالي) | **PASS** |
| 2 | `scripts/run_upgrade_test_hotfix_4_2_1.sh` (بيانات استرداد قديمة قبل Hotfix 4.2.1) | **PASS** |
| 3 | `scripts/run_upgrade_test_patch_4_2.sh` (بيانات مرتجعات قديمة قبل Patch 4.2) | **PASS** |

**صفر انحدار** — الثلاثة تمر عبر كامل سلسلة الترحيلات 0001–0156 الجديدة دون أي تأثير (Patch 6.1 لا يمس أي جدول/RPC خارج نطاق Adjustments).

## 8) اختبار HTTP/PostgREST الحقيقي (ثنائي PostgREST v12.2.3 فعلي، لا محاكاة)

`ADMIN_DATABASE_URL=... bash scripts/run_postgrest_http_test.sh` — بناء قاعدة مؤقتة كاملة من الصفر (156 ترحيلة) + تشغيل ثنائي PostgREST حقيقي + توقيع 6 JWT حقيقية (فاعل ربح كامل، فاعل بلا ربح، `service_role`، فاعل `adjustments.create`-فقط، فاعل جديد `adjustments.manage_cost`-فقط، وفاعل جديد مُقيَّد بمتجر واحد "Store B" بصلاحيتي `adjustments.view`/`adjustments.approve`) + تشغيل `scripts/postgrest-http-test.mjs` كاملًا (~2900 سطر) عبر HTTP فعلي.

**النتيجة: `=== ALL REAL HTTP/POSTGREST INTEGRATION TEST ASSERTIONS PASSED ===` — 174 تأكيدًا "OK"، صفر فشل/عطل، مخرج نظيف.**

### 8.أ) عيب رجعي حقيقي اكتُشِف في "Part 11" القائم (سببه تغييرات SQL هذه الجلسة نفسها)

خمسة تأكيدات في القسم القائم "Part 11" (من Phase 6 Core) كانت تشير إلى أسماء حقول أزالها v2 هذه الجلسة (`net_adjustment_profit`/`direct_cost`/`payment_fee_amount`/`gross_adjustment_profit` على نتائج `get_sales_order_adjustment()`/`list_sales_order_adjustments()`) — أربعة فشلت بفروقات واضحة، وواحد **أسقط السكربت بالكامل** (`FATAL: [DecimalError] Invalid argument: undefined`، لأن `net_adjustment_profit` لم يعد موجودًا إطلاقًا). **الإصلاح:** تحديث الخمسة لاستخدام `has_direct_cost`/`original_direct_cost`/`original_payment_fee_amount`/`original_gross_adjustment_profit`/`original_net_adjustment_profit`/`effective_net_adjustment_profit` — الشكل v2 الصحيح (0151). لا تعديل واحد على أي كود تطبيق أو SQL — إصلاح ميكانيكي بحت في سكربت الاختبار.

### 8.ب) قسم "Part 12" الجديد بالكامل — 6 عيوب اكتُشِفت وأُصلِحت أثناء كتابته (كلها في السكربت نفسه)

1. **×3** استدعاءات RPC كانت تمرر `p_expected_version` من قيمة `undefined` (`row_version` غير مُعاد أصلًا من `create_sales_order_adjustment()` v2) → `JSON.stringify` أسقط المفتاح صامتًا → `PGRST202`. **الإصلاح:** جلب `row_version` من استدعاء قراءة سابق حقيقي (`get_pending_sales_order_adjustment_for_edit()`/`get_sales_order_adjustment()`) بدل الاعتماد على استجابة الإنشاء.
2. **×4 مواضع** نسيان `Array.isArray(x) ? x[0] : x` عند استخراج نتيجة `get_sales_order_adjustment()` (كل استدعاء `returns table(...)` عبر HTTP يُعيد مصفوفة حتى لصف منطقي واحد) — أدى لِـ`undefined` عند قراءة الحقول.
3. اختبار "رفض عبر متجر آخر" استخدم سجلًا **أصبح معتمَدًا بالفعل** من اختبار سابق في نفس التسلسل، فلم يكن يثبت شيئًا حقيقيًا (يفشل بسبب الحالة لا بسبب نطاق المتجر). **الإصلاح:** إنشاء fixture معلَّق مخصَّص وجديد لهذا الاختبار وحده.
4. اختبار محفِّز عدم-قابلية-التعديل بعد الاعتماد (0154) استخدم العميل العادي (يُرفَض أصلًا بواسطة سياسة RLS القديمة 0135 من الطبقة-A بصرف النظر عن المحفِّز الجديد، فلم يكن يثبت المحفِّز تحديدًا). **الإصلاح:** التحويل إلى `service_role` (يتجاوز RLS **لكن ليس المحفِّزات**) ضد سجل معتمَد فعليًا — يعزل ويثبت سلوك محفِّز 0154 تحديدًا.

بعد الإصلاحات الستة: **174/174 تأكيدًا نجح، صفر فشل، تشغيلة كاملة نظيفة أُعيدت مرتين للتأكد.**

### 8.ج) تدقيق قسم "Part 12" — 14 بندًا مُثبَتة عبر HTTP فعلي

عناصر 1D/23، 2، 5/6-مصفوفة-A، 7، 8، 9/10 (×2)، 11، 12/31، 13، 17/32، 22 — كل بند مُثبَت بتأكيد HTTP حقيقي واحد أو أكثر ضد قاعدة حقيقية وثنائي PostgREST حقيقي (القائمة الكاملة بالنص العربي داخل `scripts/postgrest-http-test.mjs` نفسه، ومطبوعة بالكامل كأسطر "OK:" عند التشغيل).

## 9) الفحوصات على مستوى الواجهة/TypeScript

| الفحص | الأمر | النتيجة |
|---|---|---|
| فحص الأنواع | `npm run typecheck` (`tsc --noEmit`) | **صفر أخطاء** (بعد إصلاح خطأ `*/` النحوي في §4 أعلاه) |
| التدقيق (Lint) | `npm run lint` (`eslint`) | **صفر أخطاء/تحذيرات** (بعد إزالة ثابت `STORE_ID_B` غير المُستخدَم فعليًا من سكربت اختبار HTTP) |
| اختبارات الوحدة | `npx vitest run` | **115/115 ناجح عبر 13 ملفًا** (15 اختبارًا جديدًا لِـPatch 6.1: 4 حدود صلاحيات Server Actions جديدة — `setAdjustmentCostAction` × 2، عدم تسريب `p_direct_cost` من `updateAdjustmentAction`، صفر رسم/قناة/مرجع للخدمة المجانية — + 11 اختبار Zod لقواعد الحقل المتقاطع للخدمة المجانية/`setAdjustmentCostSchema`؛ إضافة إلى تحديث ملفي Vitest القائمَين — `adjustments-actions-permission-boundary.test.ts`/`adjustments-view-only-ui.test.tsx` — لمطابقة أشكال RPC v2 الجديدة) |
| مطابقة أنواع الأعمدة العشرية | `DATABASE_URL=... npx tsx scripts/check-numeric-column-types.ts` | **OK: 73/73 عمود NUMERIC خام مطابق تمامًا لنوع `number` في `database.ts`** (يشمل 5 أعمدة عكس جديدة في `sales_order_adjustment_reversals`) |
| بناء الإنتاج | `npm run build` (Next.js/Turbopack) | **نجح** — نفس مسارات Adjustments الخمسة القائمة (`/adjustments`، `/adjustments/[id]`، `/adjustments/[id]/edit`، `/adjustments/new`، `/master-data/adjustment-types`)، بلا أي مسار جديد (Patch 6.1 لا يضيف صفحات، يُعيد كتابة الموجودة فقط) |

## 10) خلاصة نهائية

**صفر انحدار حقيقي عبر كامل سطح الاختبار القائم** (25 ملف اختبار SQL — 20 غير-ترقية شاملة 4 تزامن حقيقي + 5 مسارات ترقية كاملة، اختبار HTTP حقيقي بـ174 تأكيدًا شاملًا 24 قسمًا سابقًا + "Part 12" الجديد بالكامل، 115 اختبار Vitest، بناء الإنتاج، ومطابقة 73 عمودًا عشريًا). الفشل الظاهري الوحيد أثناء هذه الجلسة (§6 أعلاه) ثبت أنه اصطناعي بحت من منهجية سكربت مؤقت، لا من أي كود تسليم.

**أربعة عيوب حقيقية اكتُشِفت وأُصلِحت أثناء هذه الجلسة بالذات، قبل أي تسليم فعلي:**
1. ترتيب خاطئ في ترحيلة 0144 (كان يمكن أن يمنع أي عميل حقيقي من الترقية على بيانات إنتاجية تحمل تعديلات معتمَدة).
2. محفِّز يحجب backfill ترحيلة 0150 (نفس الأثر).
3. `SET LOCAL` صامت خارج معاملة في سكربت تجهيز الاختبار (كان يُخفي فشل RLS الحقيقي بصمت).
4. خطأ نحوي `*/` في تعليق JSDoc منع البناء بالكامل (اكتُشِف عبر `npm run typecheck`، لا أثر وظيفي، لكنه كان يمنع أي تسليم).

إضافة إلى ستة إصلاحات ميكانيكية بحتة في سكربت اختبار HTTP نفسه (§8.أ/8.ب) — **لا تعديل واحد على أي كود تطبيق أو ترحيلة SQL بسببها**. لم تُعدَّل أي ترحيلة من 0001–0143. لم تبدأ Settlements ولا أي مرحلة جديدة. العمل متوقف الآن نهائيًا، بانتظار مراجعة المستخدم.
