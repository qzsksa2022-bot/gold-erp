# نتائج اختبارات Phase 10 — نواة مصروفات الفروع (Store Expenses Core)

توثيق **تشغيلات فعلية حقيقية** لاختبارات Phase 10 مقابل خادم PostgreSQL 16.4
حقيقي، على فرع `phase/10` فوق خط الأساس `f1b0a42`.

- **الترحيلات المشمولة:** 0233–0236
- **البيئة:** PostgreSQL 16.4 (نسخة محمولة، Windows)، مصادقة `scram-sha-256`
  على الـloopback (يلزمها اختبار `dblink`)، وكل اختبار على **نسخة نظيفة** من
  قاعدة template (`CREATE DATABASE ... TEMPLATE`)

> **نتائج GitHub CI: غير متاحة.** الفرع **لم يُدفع** ولم يُنشأ له PR، فلا يوجد
> تشغيل CI لهذه المرحلة بعد. كل ما يلي من تشغيلات محلية فعلية.

---

## 1) اختبارات SQL الخاصة بـPhase 10

| # | الاختبار | النتيجة |
|---|---|---|
| 1 | `supabase/tests/store_expenses_phase10.test.sql` | **PASSED** (exit 0) |
| 2 | `supabase/tests/store_expenses_phase10_concurrency.test.sql` | **PASSED — 10/10 تشغيلات متتالية** |
| 3 | `supabase/tests/upgrade_phase10_store_expenses.test.sql` | **PASSED** (exit 0) |

### ما يُثبته الاختبار الأساسي (10 أقسام)

- **حدود الصلاحيات لكل مفتاح على حدة:** فاعل «عرض فقط» لا يسجّل ولا ينشئ
  تصنيفًا؛ فاعل «تسجيل فقط» يسجّل ولا يعكس؛ فاعل «عكس فقط» لا يسجّل.
- **التحقق من المدخلات:** رفض المبلغ صفرًا/سالبًا/بأكثر من منزلتين عشريتين،
  ورفض التاريخ المستقبلي، ورفض تصنيف معطَّل.
- **نطاق الفروع:** فاعل مقيَّد بفرع ب لا يسجّل في فرع أ، ولا يرى صفوف فرع أ،
  وأي فلتر فرع خارج النطاق **يُرفض** لا يُضيَّق صامتًا (§8).
- **الإلحاق-فقط على مستوى القاعدة:** `UPDATE` و`DELETE` مرفوضان **وهما
  مُجرَّبان بمستخدم superuser يتجاوز RLS تمامًا** — الضمان في الـtrigger لا في
  السياسة.
- **العكس:** سبب إلزامي، رفض العكس المزدوج، رفض عكس حركة عكس، رفض تاريخ عكس
  يسبق الأصل، ومبلغ سالب بتاريخ خاص به. وأُثبت **بنيويًا** أن حركة العكس
  تشتق مبلغها وفرعها وتصنيفها ولقطات اسم التصنيف والربط العكسي من **الصف
  الأصلي** لا من مُدخلات المتصل.
- **التزامن التفاؤلي:** رفض `row_version` قديم، ورفض `row_version` = NULL
  صراحةً (وإلا لتخطّى الفحص كليًا)، وزيادة الإصدار بعد نجاح التعديل.
- **الإغلاق اليومي:** رفض التسجيل في يوم مقفل بلا `expenses.process_closed_day`،
  ورفضه بالصلاحية لكن بلا سبب، وقبوله بالصلاحية والسبب.
- **التدقيق:** صف `audit_logs` واحد **بالضبط** لكل قيد في الدفتر، وتغطية
  أحداث التصنيفات.
- **التقارير:** تطابق حرفي لـ`net_operating_return` بين الدالة القديمة
  والجديدة، وغياب مفاتيح Phase 10 عن الدالة القديمة، وصحة الثلاثية الجديدة.
- **§79 غياب المفاتيح:** فاعل بلا `expenses.view` لا تصله مفاتيح المصروفات —
  مع تأكيد صريح أنه **ما زال يرى** `net_operating_return` أولًا، حتى لا يمر
  الفحص شكليًا على استجابة خالية أصلًا.

### تفصيل اختبار التزامن (ثوابت حقيقية فقط)

| السيناريو | ما يُثبته |
|---|---|
| **A** | عكسان متزامنان لنفس المصروف: الثاني يُرصد **محجوبًا على backend الجلسة الأولى تحديدًا** عبر `pg_blocking_pids()` قبل تحريرها، ثم يُرفض بعد التزامها؛ حركة عكس **واحدة** فقط، والدفتر يتصافى إلى صفر |
| **B** | الإغلاق اليومي (EXCLUSIVE) مقابل تسجيل مصروف (SHARED): التسجيل يُحجب فعليًا طوال فتح معاملة الإغلاق، ثم يُرفض بعد إقفال اليوم؛ صفر حركات في اليوم المقفل |
| **C** | تسجيلان متزامنان عبر جلستين منفصلتين ينتجان رقمَي مصروف **مختلفين** بصيغة `EXP-##########` — تسلسل SEQUENCE لا `MAX()+1` |

**لم يُضَف سيناريو تزامن حيث لا يوجد ثابت حقيقي:** المصروف إلحاق غير مشروط بلا
رصيد مشتق يُسلسَل، فلا وجود لسباق «قارئان يقرران على رصيد قديم».

### اختبار الترقية

يبدأ من خط الأساس المجمَّد **0232** + `supabase/seed.sql` الحقيقي، ثم يطبّق
0233+ في استدعاء `psql` منفصل. يُثبت:

- **(أ)** المفاتيح الخمسة ومنحها للأدوار جاءت من **الترحيلة 0233 نفسها** بلا
  إعادة تشغيل `seed.sql` (بما فيه أن `supervisor` **لا** يملك
  `manage_categories`، وأن `accountant` عرض فقط).
- **(ب)** المحرك صالح للاستخدام فور انتهاء 0233–0236 (إنشاء تصنيف، تسجيل،
  عكس، قراءة الصافي = 0.00).
- **(ج)** الدالتان القديمتان `get_dashboard_summary`/`..._with_comparison`
  **لم تُعدَّلا** (فحص على `pg_get_functiondef`) وأن الغلاف الجديد أُنشئ.

---

## 2) المجموعة الكاملة لاختبارات SQL

| الفئة | العدد | النتيجة |
|---|---|---|
| اختبارات عادية | 29 | **29/29** |
| اختبارات تزامن (dblink، جلستان) | 7 | **7/7** |
| اختبارات ترقية (runners) | 15 | **15/15** |

فحص تغطية CI: **صفر اختبارات يتيمة** — الملفات الثلاثة الجديدة موصولة بمسار
تنفيذ واضح (العادي، التزامن، وrunner الترقية الجديد).

---

## 3) طبقة TypeScript

| الفحص | الأمر | النتيجة |
|---|---|---|
| Vitest | `npx vitest run` | **526/526 ناجح** عبر 38 ملفًا |
| TypeScript | `npx tsc --noEmit` | **صفر أخطاء** |
| ESLint | `npx eslint .` | **صفر أخطاء** (4 تحذيرات قائمة مسبقًا في ملف غير مرتبط، لم تُمس) |
| البناء الإنتاجي | `npx next build` | **نجح** — **53/53** صفحة (كانت 51؛ المضاف `/expenses` و`/master-data/expense-categories`) |

تغطية Vitest الجديدة: حدود الصلاحيات في Server Actions، ثبات القيم المالية
كنصوص (تشغيل فعلي + مسح ثابت لكامل `src/features/expenses/**` يمنع
`Number(`/`parseFloat(`/`parseInt(`)، سلوك الواجهة (حقل سبب اليوم المقفل لا
يظهر إلا بعد رفض الخادم فعليًا)، وعرض التقارير (القيمة القديمة لا تُقدَّم
كنتيجة بعد المصروفات، والرقمان يظهران مستقلَّين)، إضافة إلى **تغطية مسار
التصدير الحقيقي** لتقرير المصروفات عبر الـroute الفعلي.

---

## 4) مسائل مفتوحة

لا توجد مسائل مفتوحة تخص Phase 10.

**ملاحظة بيئية:** سكربتات `scripts/*.sh` لا تُنفَّذ كسكربتات على آلة التطوير
هذه (لا `bash`؛ النسخة المثبَّتة MinGit)، فالمُنفَّذ محليًا هو تسلسل `psql`
المطابق حرفيًا لخطوات كل سكربت. مسار السكربتات نفسه يُغطّى في وظيفة `sql` على
Ubuntu عند الرفع.
