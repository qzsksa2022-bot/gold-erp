# نتائج اختبارات Phase 11 — نواة المشتريات والموردين (Purchases & Suppliers Core)

**الفرع:** `phase/11` (مُنشأ من `aa4a947` — تثبيتة دمج Hotfix 10.1.0 في `main`)
**الترحيلات:** `0237`–`0240` (إضافية بالكامل بعد `0236`)
**البيئة:** PostgreSQL 16.4 حقيقي محليًا + Node/Vitest — لم يُدفع الفرع، ولا توجد نتائج GitHub CI بعد.

---

## 1) ملخص النتائج

| النوع | الملف | النتيجة |
|---|---|---|
| SQL أساسي | `supabase/tests/purchases_core_phase11.test.sql` | **PASSED** |
| تزامن حقيقي (dblink) | `supabase/tests/purchases_phase11_concurrency.test.sql` | **PASSED — 10/10 تشغيلات متتالية** |
| ترقية | `supabase/tests/upgrade_phase11_purchases.test.sql` + `scripts/run_upgrade_test_phase11_purchases.sh` | **PASSED** من خط الأساس المجمَّد 0236 |
| Vitest (المجموعة الكاملة) | 43 ملفًا | **643/643 ناجح** (منها 100 اختبار جديد في 4 ملفات) |
| TypeScript | `tsc --noEmit` | **0 أخطاء** |
| ESLint | `eslint .` | **0 أخطاء** (4 تحذيرات سابقة لهذه المرحلة في `tests/adjustments-entry-form-zero-charge-state-machine.test.tsx`، غير متعلقة بها) |
| بناء الإنتاج | `next build` | **نجح** — المسارات الأربعة الجديدة مسجَّلة |

كل مجموعات SQL شُغِّلت على **قواعد بيانات نظيفة مستقلة** (نسخة من قالب مبني من
جميع الترحيلات + `seed.sql`)، وكل ملف تزامن على قاعدة معزولة خاصة به.

---

## 2) الاختبار الأساسي — `purchases_core_phase11.test.sql`

معاملة واحدة تنتهي بـ`rollback`، ببادئة تثبيتات خاصة (`ba000000-…`) لا
يستخدمها أي ملف آخر. ستة فاعلين بصلاحيات متباينة، أحدهم مقيَّد بفرع واحد.

| القسم | ما يُثبته |
|---|---|
| 1 | حدود صلاحية كتالوج الموردين؛ رفض رمز مورّد مكرر (بلا حساسية لحالة الأحرف)؛ رفض الترحيل باسم مورّد **معطَّل** |
| 2 | رفض من لا يملك `purchases.create`؛ رفض بند إجماليه ≠ الصافي + الضريبة؛ رفض إجماليات رأس لا تطابق مجموع البنود؛ رفض بند غير خاضع للنسبة الأساسية يحمل ضريبة؛ رفض تاريخ مستقبلي؛ رفض فرع خارج نطاق التشغيل؛ ترحيل ناجح بمعالجتين ضريبيتين مختلفتين |
| 2 | **رقم فاتورة المورّد فريد لكل مورّد** — يُرفض تكراره لنفس المورّد، ويُقبل لمورّد آخر |
| 3 | **الترحيل المخزوني الذرّي:** حركة مخزون واحدة **مميزة** لكل بند، كلها موجودة فعلًا، والرصيد تحرَّك بالمقدار الصحيح تمامًا |
| 4 | رفض من لا يملك `purchases.record_payment`؛ رفض دفعة تتجاوز الإجمالي؛ **دفعة جزئية** ورصيد متبقٍ مشتق؛ رفض دفعة ثانية تتجاوز **المتبقي**؛ عكس الدفعة يعيد الالتزام بالضبط؛ رفض عكس الدفعة مرتين |
| 5 | **رفض عكس الفاتورة ما دامت توجد دفعة غير معكوسة** — ثم نجاحه بعد عكسها؛ رفض عكس الفاتورة مرتين؛ رفض دفعة على فاتورة معكوسة؛ **حركات مخزون تعويضية بتاريخ العكس الخاص** |
| 6 | **الإلحاق-فقط** — `UPDATE`/`DELETE` مرفوضان على الجداول الثلاثة حتى من دور يتجاوز RLS؛ وقيدا `CHECK` على مستوى الجدول يمنعان مستندًا غير متسق حتى عبر `INSERT` مباشر يتجاوز كل الـRPCs |
| 7 | **الإغلاق اليومي:** رفض الترحيل في يوم مقفل؛ الصلاحية وحدها لا تكفي — السبب إلزامي؛ نجاحه بالصلاحية + السبب |
| 8 | `payment_status` **مشتقة** من وقائع الدفتر؛ المبالغ نصّية؛ رفض فلتر فرع خارج النطاق (§8)؛ تفاصيل المستند ببنوده ودفعاته؛ كشف الحساب؛ **المستحق يستثني الفواتير المعكوسة**؛ فاعل مقيَّد بفرع لا يرى شيئًا من فرع آخر؛ من لا يملك `purchases.view` لا يقرأ شيئًا |
| 9 | **الحد المحاسبي:** بعد كل ما سبق، `store_expenses` **فارغ تمامًا** |
| 10 | **التدقيق:** حدث واحد لكل مستند شراء، وواحد لكل حركة دفعة، وحدث لإنشاء المورّد |

### ملاحظة على القسم الأول (تجنّب اختبار كاذب)

تقييد الفاعل الخامس بفرع واحد يُنفَّذ **بعد `reset role`** لا كـ`authenticated`:
سياسة `profiles_update` (0010) تشترط `users.edit` التي لا يملكها فاعل مشتريات،
و`UPDATE` محجوب بـRLS **يطابق صفر صفوف بصمت بدل أن يُخطئ** — فيبقى الفاعل على
نطاق «كل الفروع» وتنجح كل تأكيدات عزل الفروع لسبب خاطئ. أُضيف بعده حارس صريح
يتحقق أن التقييد وقع فعلًا. (نفس درس تثبيتات Phase 9.)

---

## 3) اختبار التزامن الحقيقي — `purchases_phase11_concurrency.test.sql`

جلستان `dblink` حقيقيتان، بلا معاملة مُغلِّفة. **كل سيناريو يُثبت التداخل
الفعلي عبر `pg_blocking_pids()`** — أي أنّ خلفية الجلسة الثانية محجوبة **بخلفية
الجلسة الأولى تحديدًا** قبل أن تلتزم الأولى. لا يوجد سيناريو «يُثبَت» بـ`pg_sleep`
وحده، ولا سيناريو شكلي مُختلق.

| # | الثابت | آلية الحماية المُثبتة |
|---|---|---|
| A | لا يُرحَّل رقم فاتورة مورّد مرتين | فهرس فريد جزئي (0238) — الثاني حُجب فعلًا ثم رُفض |
| B | **لا ترحيل مخزوني مزدوج ولا جزئي** | القفل 1008 الخاص بـ**محرك Phase 9** — الفاتورتان لا تشتركان في صف ولا مفتاح فريد، فالحجب دليل مباشر على مرور الشراء عبر المحرك القائم؛ الرصيد تحرّك 8 بالضبط، وحركة مميزة واحدة لكل بند |
| C | عكس واحد كحد أقصى للفاتورة | قفل صف `for update` + فهرس فريد جزئي — والمال **والمخزون** يتصافيان إلى صفر |
| D | **دفعتان متسابقتان لا تتجاوزان المتبقي** | قفل صف `for update` على الفاتورة — الثانية حُجبت ثم رُفضت، والمدفوع 300 بالضبط |
| E | **دفعة مقابل عكس فاتورة** | العكس حُجب على قفل صف الدفعة ثم رُفض؛ لا مستند عكس، والدفعة الملتزم بها باقية |
| F | الإغلاق اليومي (EXCLUSIVE) مقابل الترحيل (SHARED) | الزوج القائم على المفتاح 1002 — ولا **حركة مخزون واحدة** تسربت من الفاتورة المرفوضة (إثبات ذرّية الترحيل) |

**الاستقرار:** 10/10 تشغيلات متتالية على قواعد نظيفة معزولة، بلا أي فشل متقطع.

---

## 4) اختبار الترقية — `upgrade_phase11_purchases.test.sql`

يبني قاعدة إلى الترحيلة **0236 بالضبط** + `supabase/seed.sql` الحقيقي، ثم يطبّق
`0237`–latest في استدعاء `psql` **منفصل** — تمامًا كما تختبر ترقية إنتاج فعلية
(seed.sql لا يُعاد تشغيله أبدًا).

| القسم | ما يُثبته |
|---|---|
| A | الصلاحيات السبع ومنحها للأدوار تأتي من **الترحيلة 0237 نفسها** لا من `seed.sql`؛ supervisor يملك ستًا **دون** `manage_suppliers`؛ accountant عرض فقط |
| B | المحرك كامل الاستخدام فور انتهاء 0237–0240: مورّد، فاتورة، **استلام مخزوني ذرّي**، دفعة جزئية، عكس دفعة، عكس فاتورة، تقارير — والمخزون يعود إلى صفر بعد العكس |
| C | **القرار الخامس بنيويًا:** الترقية لم تُضِف عمودًا ولا قيدًا ولا مفتاحًا أجنبيًا واحدًا إلى مخطط مصروفات Phase 10، ولا دالة كتابة شراء تذكر `store_expenses`، ولا دالة مصروف تذكر المشتريات |
| D | **القرار الثالث:** `get_dashboard_summary()` و`get_dashboard_summary_with_comparison()` و`get_dashboard_summary_with_expenses()` نجت بلا أي معرفة بالمشتريات |
| E | **القرار الثاني:** `record_inventory_stock_movement()` الخاص بـPhase 9 **لم يُعدَّل**؛ ولا دالة شراء تكتب `inventory_stock_movements` مباشرة؛ وكلتا مساري الترحيل تستدعيان المحرك |

---

## 5) Vitest — 4 ملفات جديدة (100 اختبار)

| الملف | التغطية |
|---|---|
| `tests/purchases-actions-permission-boundary.test.ts` | كل عملية تُبوَّب على **صلاحيتها الخاصة** (خمس قدرات مختلفة)؛ الترحيل **لا يطلب صلاحية مخزون** رغم تحريكه المخزون؛ المبالغ نصوص؛ رفض Zod قبل الوصول إلى RPC؛ تمرير رسائل أخطاء القاعدة؛ **لا اسم RPC واحد يتعلق بالمصروفات، ولا إعادة تحقق لمسار المصروفات أو اللوحة** |
| `tests/purchases-money-string-invariant.test.ts` | ثبات المبالغ كنصوص؛ **حساب سنتات صحيحة دقيق** مُثبَت على قيم يخطئ فيها الفاصل العائم (`0.10 + 0.20`)؛ لا صفر سالب؛ **مسح كامل للمجلد** يمنع `Number(`/`parseFloat(`/`parseInt(` في أي ملف بما فيه أجسام `.refine()` |
| `tests/purchases-ui-and-reporting.test.tsx` | نص الحد المحاسبي على الشاشة **بما فيه الإقرار الصريح بأن النظام لا يمنع الازدواج تقنيًا**؛ الإجماليات الحيّة؛ رفض بند غير متسق بدل «تصحيحه»؛ تعطيل الضريبة على معالجة غير أساسية؛ سبب اليوم المقفل **مدفوع من الخادم لا استباقيًا**؛ منع عكس فاتورة بدفعة قائمة قبل جولة فاشلة؛ **لا عمود ضريبة قابلة للاسترداد في تعريف التقرير** |
| `tests/purchases-navigation-and-registries.test.ts` | الصلاحيات السبع مطابقة **لملف الترحيلة 0237 نفسه**؛ التنقل والمسارات؛ **كل حدث تدقيق تُصدره 0239 له تسمية عربية**؛ وانفصال الدفترين في طبقة TypeScript أيضًا |

---

## 6) ما لم يُختبَر لأنه غير موجود عمدًا

لا يوجد اختبار لضريبة مدخلات قابلة للاسترداد، ولا لإقرار ضريبي، ولا لدفتر
أستاذ عام أو تكلفة بضاعة مباعة، ولا لمرفقات أو دورات اعتماد شرائية، ولا لتكامل
بنكي/Salla/ناقل — **لأن أيًّا منها ليس ضمن نطاق هذه المرحلة**، ولا يوجد له كود.
الاختبارات تُثبت **غيابه** حيث يكون الغياب التزامًا محاسبيًا (الأقسام C وD وE
في اختبار الترقية، وقسم التقرير في اختبار الواجهة).

---

## 7) مراجعة ما قبل الرفع — عيبان مُثبَتان وثلاث فجوات تغطية

أُجريت مراجعة مركّزة على التثبيتة `0979099` أسفرت عن الآتي، وكلٌّ أُثبت قبل
إصلاحه لا بعده.

### 7-أ) عيب: ترتيب أقفال 1008 كان بيد العميل (خطر جمود حقيقي)

Phase 11 هي **أول** مستدعٍ في المشروع يأخذ أكثر من قفل 1008 في معاملة واحدة
(محرك Phase 9 يُستدعى مرة لكل إجراء؛ فاتورة الشراء تستدعيه مرة **لكل بند**).
وكانت الأقفال تُؤخذ بترتيب البنود **كما أرسلها العميل** — والواجهة تسمح للمُدخِل
بترتيب البنود كيفما شاء.

**الإثبات قبل الإصلاح** (فحص `pg_locks` مباشرةً): جلسة تحمل قفل الصنف الأول
فقط، وجلسة أخرى تُرحّل فاتورة ببندين بالترتيب المعاكس → الجلسة الثانية **تحمل
بالفعل قفل الصنف الآخر بينما تنتظر** — وهذه بالضبط الحافة الناقصة لدورة جمود
بين فاتورتين متعددتَي البنود، تنتهي بإجهاض PostgreSQL لإحداهما بـ
`deadlock detected` وفشل زائف لعملية شراء سليمة.

**الإصلاح:** التقاط كل أقفال `(صنف، فرع)` المميزة مقدَّمًا **بترتيب قانوني
(معرّف الصنف)** قبل ترحيل أي حركة، في `post_purchase_invoice()` و
`reverse_purchase_invoice()` معًا. الأقفال الاستشارية قابلة لإعادة الدخول داخل
المعاملة، فالتقاط 0229 اللاحق يصبح بلا أثر — **ولم يُمَسّ محرك Phase 9 إطلاقًا**.

**إثبات الطفرة:** بإزالة حلقة الالتقاط القانوني وحدها، يفشل السيناريو G برسالته
الحرفية. الاختبار حسّاس للعيب فعلًا وليس اختبارًا كاذبًا.

### 7-ب) فجوة: الحارس اليومي له **أربعة** مواضع استدعاء، وكان مُختبَرًا في واحد

`_purchase_daily_close_guard()` يُستدعى من الترحيل والدفع وعكس الفاتورة وعكس
الدفعة. كان الاختباران (الأساسي والتزامن) يغطيان **مسار الترحيل فقط**.
أُضيف القسم 7-ب في الاختبار الأساسي (المسارات الثلاثة الباقية، رفضًا وسماحًا
بالصلاحية + السبب) والسيناريو H في اختبار التزامن (الإغلاق مقابل **الدفع**،
وهو المسار المختلف بنيويًا لأنه يأخذ قفل صف الفاتورة **قبل** طلب قفل الإغلاق).

### 7-ج) فجوة: الذرّية كانت مُستنتَجة من قيد `UNIQUE` لا مُختبَرة

`inventory_movement_id NOT NULL UNIQUE` يثبت أن لكل بند حركة واحدة — **ولا يقول
شيئًا** عن كونها الحركة الصحيحة، ولا عن ما يبقى إذا فشل بند بعد نجاح سابقه.
أُضيف:
- **تطابق بند-بحركة، حقلًا بحقل، لكل بنود كل مستندات القاعدة**: الصنف والفرع
  والكمية الموقَّعة والاتجاه (`receive` للموجب، `adjust` للسالب) — مع حارس يمنع
  المسح من المرور وهو فارغ.
- **اختبار فشل منتصف المعاملة**: بند أول صحيح يُرحّل حركته فعلًا، ثم بند ثانٍ
  يفشل (صنف غير موجود، ثم صنف معطَّل) → لا فاتورة، ولا بند يتيم، **ولا حركة
  مخزون واحدة تسربت**، والرصيد لم يتحرك.
- **حارس ضد الإيجابية الكاذبة**: تأكيد بنيوي أن فحص الصنف وترحيل الحركة في
  **نفس دورة الحلقة**، وإلا لكان الاختبار يمرّ دون أن يُنشئ حالة جزئية أصلًا.

### 7-د) فجوة: دلالة التاريخ لم تكن مُفصَّحة، وكشف الحساب بلا رصيد

كان كشف الحساب يعرض **حركة الفترة** فقط، فلا يجيب عن «كم كان المستحق في 30
سبتمبر؟». وكان عمود «المتبقي» في تقرير المشتريات يعرض الحالة **الحالية** على
صفوف مُرشَّحة بالتاريخ، بلا ما يوضّح ذلك.

**الإصلاح:** أُضيف إلى `get_supplier_statement()` ثلاثة أرصدة صريحة —
`opening_balance` (قبل بداية الفترة)، `closing_balance` (**حتى تاريخ التقرير**)،
و`current_balance` (**اليوم**) — تُعرض في الواجهة في مجموعة منفصلة عن أرقام
الحركة، كلٌّ موسوم بتاريخه، مع تنبيه صريح عند اختلاف الأخيرين. وأُعيدت تسمية
حقول تقرير المشتريات إلى «المتبقي حاليًا» و«المستحق حاليًا من فواتير الفترة».

**الثابت المُثبَت:** عكس فاتورة **وعكس دفعة** بتاريخ اليوم، بعد إصدار كشف لفترة
منتهية، **لا يغيّر** رصيدها الافتتاحي ولا الختامي ولا حركتها ولا عدد قيودها —
بينما يتغيّر الرصيد الحالي، والاثنان معروضان كرقمين منفصلين.

### 7-هـ) التصدير الفعلي — مُغطّى الآن

`tests/purchases-export-and-scope.test.ts` (13 اختبارًا): نطاق الفرع يصل الـRPC
حرفيًا، و`null` لا `[]` عند غياب الفلتر، وكل فلتر مُسجَّل يُمرَّر، ورفض الفرع
خارج النطاق **يُرمى** ولا يتحول إلى مجموعة أوسع، **ولا تسرّب لأي إجمالي** في
قيمة الخطأ، **وتكافؤ الشاشة والتصدير** (كل مفتاح تعرضه الشاشة مُصرَّح به في سجل
التصدير — درس Hotfix 8.1.3 §B2).

### خريطة سيناريوهات التزامن إلى المتطلبات المعتمدة

| المتطلب | السيناريو | الحماية المُثبتة |
|---|---|---|
| رقم فاتورة مورّد مكرر | **A** | فهرس فريد جزئي |
| ترحيل مخزوني مزدوج/جزئي | **B** + القسم 3/3ب الأساسي | قفل 1008 القائم + قيد `UNIQUE` + ذرّية المعاملة |
| عكس فاتورة مزدوج | **C** | قفل صف + فهرس فريد جزئي |
| دفعتان تتجاوزان الرصيد | **D** | قفل صف على الفاتورة |
| دفعة مقابل عكس فاتورة | **E** | قفل صف على الفاتورة |
| الإغلاق اليومي مقابل الترحيل | **F** | الزوج 1002 القائم |
| الإغلاق اليومي مقابل **الدفع** | **H** (جديد) | الزوج 1002 القائم |
| الإغلاق اليومي مقابل **عكس الفاتورة/الدفعة** | القسم 7-ب الأساسي (جديد) | الزوج 1002 على تاريخ العكس نفسه (§85) |
| **ترتيب الأقفال / انعدام الجمود** | **G** (جديد) | التقاط قانوني مقدَّم |

**ترتيب الأقفال المُراجَع:** الترحيل يأخذ 1002 مشتركًا ثم أقفال 1008 بترتيب
قانوني. العكس والدفع يأخذان قفل صف الفاتورة ثم 1002 المشترك (ثم 1008 للعكس).
لا دورة ممكنة: كل معاملة تقفل **صفًّا واحدًا على الأكثر**، وأقفال 1002 المشتركة
متوافقة فيما بينها، وأقفال 1008 صارت تُلتقط بترتيب كلّي يشترك فيه كل المستدعين
— فلا يمكن لمعاملة أن تنتظر قفلًا يسبق أي قفل تحمله.

### النتائج بعد الإصلاح

| المجموعة | النتيجة |
|---|---|
| `purchases_core_phase11.test.sql` | **PASSED** (قاعدة نظيفة) |
| `purchases_phase11_concurrency.test.sql` | **PASSED — 8 سيناريوهات، 10/10 تشغيلات** |
| `upgrade_phase11_purchases.test.sql` | **PASSED** من 0236 |
| Vitest — الملفات الخمسة للمشتريات | **113/113** |
| tsc · ESLint · build | **0 · 0 · نجح** |

لم تُعَد المجموعات غير المتأثرة (الـ27 SQL الأخرى والـ15 ترقية) لأن الشجرة لم
تتغيّر تحتها: التعديل محصور في `0239`/`0240` وطبقة المشتريات واختباراتها.

---

## 8) خطر ازدواج الإدخال البشري — إفصاح صريح

المشتريات لا تُنشئ صف `store_expenses` أبدًا، وتقارير المصروفات لا تضمّ
المشتريات أبدًا — وهذا **مضمون بنيويًا** ومُثبَت في اختبارَي الترقية والوحدة.

لكن يبقى خطر **بشري** لا يمكن للنظام منعه: أن يُدخل مستخدم نفس فاتورة الشراء
مرة أخرى يدويًا في شاشة المصروفات كمصروف تشغيلي. لا يوجد ربط بين الدفترين
يسمح باكتشاف ذلك، ولا يمكن لتصنيف نصّي حر أن يوفّر ضمانًا بنيويًا ضده.
المُتَّخَذ حياله: **تحذير صريح مكتوب في شاشة إدخال فاتورة الشراء وفي شاشة قائمة
المشتريات**، ينصّ على أن شراء المخزون ليس مصروفًا تشغيليًا وأن النظام لا يمنع
الازدواج تقنيًا. وهناك اختبار Vitest يفشل إن جرى تليين هذه الجملة إلى وعد.
