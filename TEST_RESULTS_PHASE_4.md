# نتائج الاختبار الفعلية — Phase 4: نواة المرتجعات (Returns Core)

كل الأرقام أدناه من تشغيل فعلي في هذه الجلسة ضد قاعدة بيانات مبنية من الصفر (`local_harness_setup.sql` + الترحيلات 0001–0091 بالترتيب دون توقف + `supabase/seed.sql`). لا رقم افتراضي أو منسوخ من تشغيل سابق — كل تشغيلة أُعيدت فعليًا عند كتابة هذا الملف.

## 1) بناء قاعدة البيانات من الصفر

- `local_harness_setup.sql` → نجح.
- الترحيلات 0001–0091 بالترتيب دون توقف → **91/91 نجحت** (الجديدة: 0082–0091، عشر ترحيلات).
- `supabase/seed.sql` → نجح.

## 2) اختبار الترقية (Foundation → latest، بلا إعادة تشغيل seed.sql)

`scripts/run_upgrade_test.sh` يُطبِّق 0040–0091 تلقائيًا عبر glob بلا تعديل على السكربت نفسه:

```
==> Upgrade test PASSED
=== ALL UPGRADE-FROM-0039 TESTS PASSED (0040-latest, including Phase 3 0058-0064, applied WITHOUT re-running seed.sql) ===
```

لم يتطلَّب أي تعديل توقعات جديد — Phase 4 لا تضيف أي صلاحية أو جدول ضمن نطاق فحص هذا الاختبار (الذي يتوقف عند التحقق من صلاحيات/بيانات Phase 2/Phase 3 الأساسية فقط).

## 3) اختبارات SQL — كل الملفات، أرقام فعلية من هذا التشغيل (تشغيل واحد متسلسل على نفس قاعدة البيانات، 0001–0091)

| الملف | النتيجة |
|---|---|
| `rls_and_permissions.test.sql` | 139/139 ✅ |
| `financial_master_data.test.sql` | 52/52 ✅ |
| `financial_integrity_patch_2_1.test.sql` | 34/34 ✅ |
| `financial_integrity_patch_2_2.test.sql` | 17/17 ✅ |
| `financial_integrity_hotfix_2_2_1.test.sql` | 14/14 ✅ |
| `financial_integrity_hotfix_2_2_2.test.sql` | 11/11 ✅ |
| `sales_core.test.sql` | 57/57 ✅ |
| `sales_integrity_patch_3_1.test.sql` (Part 1، أحادي الجلسة) | 14/14 ✅ |
| `sales_integrity_patch_3_1_concurrency.test.sql` (Part 2، تزامن حقيقي عبر `dblink`) | 10/10 ✅ |
| `sales_integrity_patch_3_2.test.sql` | 15/15 ✅ |
| `sales_integrity_hotfix_3_2_1.test.sql` | 6/6 ✅ |
| `sales_returns_core.test.sql` (**جديد بالكامل**، أحادي الجلسة، 10 أقسام) | **32/32 ✅** |
| `sales_returns_concurrency.test.sql` (**جديد بالكامل**، تزامن حقيقي عبر `dblink`) | **R1/R2 — 2/2 سيناريوهات ✅** |

**المجموع: 403 نقطة تحقق SQL ناجحة في تشغيلة واحدة متسلسلة على قاعدة بيانات واحدة مبنية من الصفر** (369 من كل المراحل/الرقعات السابقة، بلا أي انحدار على أي منها + 34 جديدة لِـPhase 4: 32 في `sales_returns_core.test.sql` + 2 سيناريو في `sales_returns_concurrency.test.sql`).

### تفصيل `sales_returns_core.test.sql` (10 أقسام)

1. إنشاء مرتجع (`create_sales_return()`) — لقطات صحيحة، سيناريو `other` يتطلب ملاحظات، رفض بند غير نشط/خارج العملية، رفض تكرار بند في نفس الطلب.
2. تعديل مرتجع قيد المراجعة (`update_pending_sales_return()`) — إضافة/إزالة بنود، حفظ اللقطة الأصلية لبند غير مُتغيّر، تزامن تفاؤلي (`row_version`).
3. معاينة (`preview_sales_return()`) — لا كتابة، أرقام تقديرية موسومة صراحةً `is_fee_reversal_estimate: true`، حماية ربح.
4. منع الإرجاع المزدوج — محاولة إنشاء مرتجع ثانٍ على بند مُطالَب به بالفعل (قيد المراجعة أو معتمد) تُرفَض برسالة واضحة، مع الفهرس الفريد الجزئي كخط دفاع ثانٍ حتى ضد كتابة مباشرة.
5. اعتماد/رفض (`approve_sales_return()`/`reject_sales_return()`) — حساب أرقام الاسترداد النهائية عند الاعتماد فقط، حذف ناعم متسلسل للبنود عند الرفض، تزامن تفاؤلي.
6. استرداد العمولة التراكمي مع امتصاص فارق التقريب — **المثال المحلول**: عملية بيع بثلاثة بنود (333.33 / 333.33 / 333.34، إجمالي 1000.00) وعمولة دفع أصلية 100.00؛ ثلاثة مرتجعات منفصلة تُغطّي البنود الثلاثة بالتتابع — المرتجعان الأولان يستردّان 33.33 لكل منهما (تناسبي)، والمرتجع الثالث (المُكمِل للتغطية الكاملة) يمتص الرصيد المتبقي **33.34 بالضبط** بدل الحصة التناسبية (33.33) — فيُطابق مجموع العمولة المُستردة تراكميًا (100.00) العمولة الأصلية تمامًا، بلا أي قرش عالق.
7. التراجع عن مرتجع معتمد (`reverse_sales_return()`) — الأرقام المحسوبة عند الاعتماد تبقى محفوظة تاريخيًا، البنود تعود قابلة للإرجاع، القفل المالي على البيعة الأصلية يُرفَع تلقائيًا إن لم يبقَ مرتجع فعّال آخر.
8. القفل المالي الجديد في `update_sales_order()` (0084) — رفض تعديل طريقة الدفع/قناة التحصيل/بيانات أي بند مالية، ورفض إضافة/حذف بند، بمجرد وجود مرتجع معتمد فعّال؛ السماح بتعديل وصفي بحت (اسم/هاتف العميل، الملاحظات، اسم/وصف/رمز البند) رغم وجود المرتجع.
9. الإغلاق اليومي والمتجر المعالِج المختلف عن متجر البيع — إعادة استخدام أقفال/جدول Sales نفسها، تجاوز إلزامي السبب.
10. دفتر الاسترداد النقدي الفعلي (`record_sales_return_refund()`/`reverse_sales_return_refund_event()`) — مستقل عن `approved_refund_amount`، `refund_variance` يُطابَق للمطابقة لا يُفرَض تلقائيًا، إبطال ناعم لسجل خاطئ.

**إثبات صفر سياسات RLS مباشرة:** محاولة `SELECT` مباشر على `sales_returns`/`sales_return_items`/`sales_return_refund_events` كـ`authenticated` تُعيد صفوفًا فارغة **صمتًا** (لا خطأ) — نفس نمط اكتشاف Phase 3، مُثبَت هنا عبر `reset role` (تجاوز RLS بصلاحية `superuser`) كإثبات مضاد أن الصفوف موجودة فعليًا وأن الفراغ ناتج عن RLS لا عن غياب البيانات.

### تفصيل `sales_returns_concurrency.test.sql` (تزامن حقيقي عبر `dblink`، لا محاكاة تسلسل استدعاءات)

- **R1 — منع الإرجاع المزدوج تحت سباق حقيقي:** اتصالان منفصلان (A وB) يحاولان في نفس اللحظة إنشاء مرتجع على **نفس البند** من نفس العملية؛ تُثبَت آلية الحجب الفعلي (استقصاء `dblink_is_busy` قبل الالتزام، لا مجرد ترتيب نجاح متسلسل) — اتصال واحد فقط ينجح، والآخر يُرفَض برسالة واضحة بعد التزام الأول، لا ازدواج ممكن حتى تحت تزامن حقيقي.
- **R2 — سباق إغلاق يوم مقابل إنشاء مرتجع:** `close_sales_day()` ينتظر فعليًا حتى التزام `create_sales_return()` المفتوحة لنفس المتجر/اليوم قبل أن يتابع — القفل المشترك/الحصري لِـReturns مطابق تمامًا لآلية Sales القائمة (0065).

كلا السيناريوهين خارج معاملة `begin/rollback` (يلتزمان فعليًا، مطابقةً لطبيعة اختبار `dblink`) — مع تنظيف صريح بحذف كل الصفوف المُنشَأة في نهاية الملف، مُثبَت أيضًا بنجاح فعلي.

## 4) اختبار HTTP/PostgREST الحقيقي — نتائج فعلية

`scripts/run_postgrest_http_test.sh` (ثنائي PostgREST v12.2.3 حقيقي، JWT حقيقي مُوقَّع، `@supabase/postgrest-js` — نفس المكتبة التي يستخدمها `supabase-js` تحت الغطاء، لا محاكاة):

```
=== ALL REAL HTTP/POSTGREST INTEGRATION TEST ASSERTIONS PASSED ===
```

**59/59 تأكيدًا ناجحًا** (37 قائمة من كل المراحل السابقة بلا انحدار + **22 جديدة لِـPhase 4**، "Part 7"):

- `create_sales_return()`/`preview_sales_return()` تُرجِعان كل قيمة مالية كنص (`typeof === "string"`) عبر HTTP فعلي، ورقم مرتجع حقيقي بالصيغة `RET-##########`.
- منع الإرجاع المزدوج مرفوض فعليًا عبر استدعاء HTTP ثانٍ حقيقي على نفس البند (لا محاكاة محلية).
- `get_returnable_sales_order()`: `order_state`/`returnable` يتغيّران فعليًا عبر دورة الحياة الكاملة (`not_returned` → بند مُطالَب به قيد المراجعة → `partial` بعد الاعتماد → `not_returned` مجددًا بعد التراجع).
- `get_sales_return()` بعد الاعتماد: كل رقم استرداد كنص لممثِّل يملك `sales.view_profit`؛ **غياب تام** (لا `null`) لمفاتيح الربح الأربعة لممثِّل لا يملكها.
- `list_sales_returns()`: نفس حماية الربح بصيغة `null` صريح بدل الغياب (مطابقةً لفرق `get`/`list` القائم في Sales).
- **القفل المالي الجديد (0084) يرفض فعليًا** عبر HTTP محاولة تعديل سعر بند آخر (غير المُرجَع) على نفس العملية بمجرد وجود مرتجع معتمد عليها — يُثبِت أن الحظر يشمل العملية كاملة لا البند المُرجَع فقط.
- دفتر الاسترداد النقدي الفعلي: تسجيل استرداد، `actual_refunded_total`/`refund_variance` يتحدَّثان حيًا، ثم تراجع عن سجل الاسترداد ويعودان لقيمتهما الأصلية.
- حماية تدقيق `return.%` (0091): طلب `GET /audit_logs?action=like.return.*` حقيقي يُعيد صفوفًا لممثِّل يملك `audit_logs.view AND sales.view_profit`، وصفرًا تمامًا لممثِّل يملك `audit_logs.view` فقط — مطابقة تمامًا لإثبات `sale.%` القائم (Part 4).

بيانات الاختبار: `supabase/tests/postgrest_http_test_setup.sql` مُوسَّع بمنح صلاحيات Returns للممثِّلَين الاختباريَين القائمَين (لا ممثِّل ثالث جديد)، وتحويل طريقة الدفع الاختبارية `http_test_pm` من `refund_fee_policy` الافتراضية (`manual`) إلى `proportional_reversal` حتى يعمل مسار الاعتماد الرئيسي دون قيمة يدوية — مسار `manual` نفسه مُغطّى بالفعل بتفصيل في `sales_returns_core.test.sql`.

## 5) فحوصات الطبقة الأمامية (TypeScript/React) — نتائج فعلية

- `npx tsc --noEmit` → **صفر أخطاء** (شمل كل ملفات `src/features/returns/*` و`src/app/(app)/returns/*` الجديدة، و`src/types/database.ts` بعد إضافة الجداول/الدوال الجديدة).
- `npx eslint .` → **صفر أخطاء وتحذيرات** عبر المشروع بالكامل.
- `npx vitest run` → **47/47 ناجح عبر 6 ملفات** (بلا تغيير — Phase 4 لم يمسّ أي منطق Decimal/وحدة اختبار قائمة).
- `npm run check:numeric-types` → **نجح، 39/39 عمود NUMERIC مطابق** لِـ`number` في `database.ts` (يشمل 16 عمودًا جديدًا عبر `sales_returns`/`sales_return_items`/`sales_return_refund_events`، كلها مطابقة لسلوك PostgREST الفعلي — لا `string` وهمي).
- `npm run build` (Next.js/Turbopack) → **نجح**، توليد كل المسارات شاملةً أربعة مسارات Returns الجديدة: `/returns`، `/returns/new`، `/returns/[id]`، `/returns/[id]/edit`.

## 6) خلاصة

**403 نقطة تحقق SQL** (369 من كل المراحل السابقة، صفر انحدار + 34 جديدة)، **59 تأكيدًا HTTP/PostgREST حقيقيًا** (37 سابقة + 22 جديدة)، **47 اختبار Vitest**، وفحوصات `tsc`/`eslint`/`check:numeric-types`/`next build` نظيفة بالكامل — كلها من تشغيلة فعلية واحدة على قاعدة بيانات واحدة مبنية من الصفر في هذه الجلسة، لا أرقام مفترَضة أو منسوخة. **لم تبدأ Shipping ولا Settlements ولا Services/Adjustments ولا Inventory ولا Reports/PDF/Excel — العمل متوقف الآن نهائيًا، بانتظار المراجعة والموافقة من المستخدم بعد إرسال ZIP هذا التسليم.**
