# نتائج الاختبار الفعلية — Phase 5: نواة الشحن (Shipping Core)

كل الأرقام أدناه من تشغيل فعلي في هذه الجلسة ضد قاعدة بيانات مبنية من الصفر (`local_harness_setup.sql` + الترحيلات 0001–0121 بالترتيب دون توقف + `supabase/seed.sql`). لا رقم افتراضي أو منسوخ من تشغيل سابق — كل تشغيلة أُعيدت فعليًا عند كتابة هذا الملف، بما فيها إعادة بناء القاعدة من الصفر أكثر من مرة بعد إصلاح عيب حقيقي اكتُشِف أثناء اختبار HTTP (انظر القسم 4).

## 1) بناء قاعدة البيانات من الصفر

- `local_harness_setup.sql` → نجح.
- الترحيلات 0001–0121 بالترتيب دون توقف → **121/121 نجحت** (الجديدة: 0113–0121، تسع ترحيلات).
- `supabase/seed.sql` → نجح (شمل بيانات شركات الشحن/المناطق/تسعير الإرجاع المؤسِّسة الجديدة).

## 2) إثبات أمان الترقية (Upgrade Safety) — بيانات حقيقية ما قبل Phase 5

بناء قاعدة بيانات إلى الترحيلة 0112 بالضبط + `seed.sql`، إدراج طلب بيع حقيقي عبر `create_sales_order()` (بشكل إنتاجي حقيقي، لا إدراج مباشر)، ثم تطبيق 0113–0121 كخطوة ترقية منفصلة تمامًا كما ستحدث في بيئة إنتاج حقيقية:

- تطبيق 0113–0121 على القاعدة المُرقّاة → **9/9 نجحت، صفر أخطاء**.
- صف طلب البيع الأصلي (`row_version`/`subtotal`/`gross_profit`/`net_sales_profit`) **بقي بلا أي تغيير حرفي** قبل/بعد الترقية (تحقُّق مباشر بالقيم: `750.00` / `393.50` / `318.50`، `row_version=1` في الحالتين).
- شركات الشحن (`SMSA`/`ARAMEX`/`BARQ`/`REDBOX`/`STORE_COURIER`) والمناطق (`RIYADH`/`OUTSIDE_RIYADH`) الأساسية من `seed.sql` موجودة وصحيحة بعد الترقية.
- `create_shipment()` استُدعِيت فعليًا على **نفس طلب البيع القديم الموجود من قبل الترقية** → نجحت، أنشأت شحنة حقيقية (`SHP-0000000001`) بتكلفة يدوية (لا تسعير مُهيَّأ لاتجاه outbound)، مع `net_shipping_expected` (`25.00`) **مستقل تمامًا** عن `net_sales_profit` (`318.50`) الخاص بالبيعة الأصلية — يُثبِت الاستقلال المالي الكامل بين Shipping وSales حتى على بيانات ما قبل الترقية.

## 3) اختبارات SQL — كل الملفات، أرقام فعلية من هذا التشغيل (تشغيل واحد متسلسل على نفس قاعدة البيانات، 0001–0121)

| الملف | النتيجة |
|---|---|
| كل ملفات الاختبار القائمة من Foundation حتى Hotfix 4.2.1 (19 ملفًا، بلا تعديل منطقي) | **بلا انحدار، صفر أخطاء** |
| `shipping_core_phase5.test.sql` (**جديد بالكامل**، أحادي الجلسة/معاملة واحدة تُلغى في النهاية، الأقسام 44–49) | **6/6 أقسام ناجحة ✅** |
| `shipping_core_phase5_concurrency.test.sql` (**جديد بالكامل**، تزامن حقيقي عبر `dblink`) | **A–F — 6/6 سيناريوهات تزامن حقيقية ناجحة ✅** |

### تفصيل `shipping_core_phase5.test.sql` (الأقسام 44–49)

1. **القسم 44 — لقطة تسعير تاريخية:** شحنة إرجاع تُنشَأ باستخدام تسعير SMSA/RIYADH/return الساري وقتها (17.00)، ثم جدولة إصدار مستقبلي جديد (99.00 من الغد) — الشحنة المُنشَأة مسبقًا تحتفظ بـ`expected_carrier_cost`/`carrier_rate_version_id` الأصليين بلا أي تغيير، رغم الإصدار الجديد.
2. **القسم 45 — رسوم شحن الإرجاع للعميل:** `preview_customer_return_shipping_fee()` تقترح 35.00 للرياض و50.00 لخارج الرياض بدقة (مطابقة تمامًا لأرقام `seed.sql`)؛ إنشاء شحنة إرجاع بتجاوز الاقتراح (60.00) يُثبِت أن الاقتراح **قابل للتجاوز دومًا** ولا يمس أي عمود في سجل المرتجع المالي (مقارنة كاملة للصف قبل/بعد، بايت مقابل بايت).
3. **القسم 46 — تاريخ التكلفة الفعلية:** تسجيل (24.00) ← تصحيح (26.50) ← تصحيح ثانٍ (25.75) — القيم الحالية (`actual_carrier_cost`/`net_shipping_actual`) تعكس **آخر تصحيح فقط**، بينما دفتر الأحداث المالية يحتفظ بالثلاثة كاملةً (إضافة-فقط حقيقية).
4. **القسم 47 — فشل التسليم/لم يستلم العميل:** المسار الكامل `out_for_delivery → delivery_failed → customer_never_received → returned_to_store` يُجتاز بالكامل كانتقالات **طبيعية** (`is_correction=false` على كل حدث)، مُثبِتًا أن `customer_never_received` حالة من الدرجة الأولى في آلة الحالة، لا استثناءً يتطلب سببًا.
5. **القسم 48 — شحنة الإرجاع:** ربط صحيح بالمرتجع (`sales_return_id`/`return_number`)، تسعير اتجاه `return` مستقل عن `outbound` (17.00 لأرامكس/الرياض، بينما لا تسعير `outbound` مُهيَّأ إطلاقًا)، ورفض صريح لمحاولة إنشاء شحنة باتجاه `outbound` مع `sales_return_id` (قيد Section 23 على مستوى القاعدة).
6. **القسم 49 — أمن الربحية:** فاعل بصلاحية `shipments.view` فقط يرى `status_timeline` كاملًا لكنه لا يرى أي مفتاح مالي واحد في `get_shipment()` (غياب تام، لا `null`)، ويُرفَض تمامًا عند محاولة `create_shipment()`/`record_shipment_actual_cost()`.

### تفصيل `shipping_core_phase5_concurrency.test.sql` (تزامن حقيقي عبر `dblink`، لا محاكاة تسلسل استدعاءات)

- **A — قفل تسعير الشحن المشترك (1005) مقابل `create_shipment()`:** اتصال A يُنشئ شحنة (يحمل القفل المشترك)، اتصال B يحاول جدولة إصدار تسعير جديد (يحتاج القفل الحصري) في نفس اللحظة — B **يُحجَب فعليًا** طوال معاملة A المفتوحة، ثم ينجح فور التزام A دون أي كتابة ممزَّقة.
- **B — سباق `add_shipment_status_event()` (تزامن تفاؤلي):** اتصالان يتسابقان على نفس تحديث الحالة بنفس `row_version` القديم — B يُحجَب على قفل صف الشحنة، ثم يُرفَض بتعارض إصدار واضح بعد التزام A — لا فقدان تحديث (Lost Update) ممكن حتى تحت تزامن حقيقي.
- **C — سباق `record_shipment_actual_cost()` ("تسجيل أول" مزدوج):** نفس شكل B لكن على أول تسجيل تكلفة فعلية — يُثبَت أن فحص `row_version` (لا فحص "مُسجَّلة سابقًا" فقط) هو ما يمنع التسجيل المزدوج فعليًا تحت تزامن حقيقي، بطبقة حماية أسبق من الفحص المنطقي.
- **D — الإغلاق اليومي مقابل `create_shipment()`:** `close_sales_day()` ينتظر فعليًا حتى التزام `create_shipment()` المفتوحة لنفس المتجر/اليوم — القفل المشترك/الحصري لِـShipping مطابق تمامًا لآلية Sales/Returns القائمة.
- **E — الإغلاق اليومي مقابل `record_shipment_actual_cost()`/`correct_shipment_actual_cost()`:** سيناريوهان فرعيان (E1 تسجيل، E2 تصحيح) — كلاهما يُثبِت أن `close_sales_day()` يحترم القفل المشترك المبني على **تاريخ العملية المالية نفسها**، لا تاريخ الشحنة (قد يختلفان)، مع حفظ التصحيح (26.50) بدقة بعد الإغلاق.
- **F — إثبات ترتيب الأقفال (عكس A):** اتصال A يُنشئ أول إصدار تسعير لِـARAMEX/RIYADH/outbound (يحمل القفل الحصري)، اتصال B يحاول إنشاء شحنة تحتاج نفس التسعير (تحتاج القفل المشترك) — B يُحجَب فعليًا ثم يكتمل بشكل طبيعي بعد التزام A، ويحل السعر الجديد (55.00) بدقة دون أي قراءة قديمة سابقة للقفل. **لا طريق مسدود (deadlock حقيقي، SQLSTATE 40P01) بين الدالتين في أي من اتجاهي التسابق (A وF معًا)** — إثبات بنيوي: `create_shipment()` هي الدالة الوحيدة التي تأخذ كلا القفلين معًا (الإغلاق اليومي ثم تسعير الشحن، بنفس الترتيب دومًا)، ولا دالة أخرى تأخذ أكثر من قفل واحد، فلا يمكن لحلقة انتظار دائرية أن تتشكَّل أصلًا.

**ملاحظة بنيوية مهمة:** `shipment_status_events`/`shipment_financial_events` محميتان بمُشغِّل رفض غير مشروط ضد `UPDATE`/`DELETE` **بلا أي بوابة إفلات** (مختلف عمدًا عن نمط `app.allow_refund_event_backfill` في Returns — Phase 5 لا بيانات موروثة لترحيلها رجعيًا) — هذا يجعل أي شحنة تُنشَأ **غير قابلة للحذف نهائيًا** (قيد `ON DELETE RESTRICT` من `shipments` إلى الجدولين، وقيد مماثل من `shipments` إلى `sales_orders`/`sales_returns`)، لذا ملف اختبار التزامن يُبقي بيانات اختباره (بادئة `P5CC`/`e5...`) بلا حذف نهائي في نهايته عمدًا (موثَّق صراحةً في الملف نفسه) — مطابقةً تمامًا لتصميم القاعدة، لا خللًا في التنظيف.

## 4) اختبار HTTP/PostgREST الحقيقي — نتائج فعلية

`scripts/run_postgrest_http_test.sh` (ثنائي PostgREST v12.2.3 حقيقي، JWT حقيقي مُوقَّع، `@supabase/postgrest-js` — نفس المكتبة التي يستخدمها `supabase-js` تحت الغطاء، لا محاكاة):

```
=== ALL REAL HTTP/POSTGREST INTEGRATION TEST ASSERTIONS PASSED ===
```

**101/101 تأكيدًا ناجحًا** (81 قائمة من كل المراحل السابقة بلا انحدار + **20 جديدة لِـPhase 5**، "Part 8"):

- `create_shipment()` تُرجِع رقم شحنة حقيقيًا بالصيغة `SHP-##########`؛ كل حقل مالي (`customer_shipping_charge`/`expected_carrier_cost`/`net_shipping_expected`/`actual_carrier_cost`/`net_shipping_actual`) يُرجَع كنص (`typeof === "string"`) عبر HTTP فعلي في `get_shipment()`/`list_shipments()`.
- **عيب حقيقي اكتُشِف وأُصلِح أثناء هذا التشغيل بالذات:** `preview_shipment_expected_cost()`/`preview_customer_return_shipping_fee()` (ترحيلة 0117) كانتا تُعيدان عمود التكلفة/الرسوم كـ`numeric` خام (`returns table (..., expected_carrier_cost numeric)`) بدل `text` — بالضبط فئة الخطأ التي بُنِي اختبار HTTP هذا كله لاكتشافها (Financial Integrity Patch 2.2)، ومخالفة صريحة لنمط كل دالة `preview_*` أخرى في المشروع (`preview_sales_order`/`preview_sales_return`، كلتاهما `returns jsonb` مع `::text` داخليًا). طبقة TypeScript (`database.ts`) كانت بالفعل مكتوبة بافتراض `string | null` (متوافقة مع الاتفاقية الصحيحة)، ما يعني أن هذا كان عدم تطابق فعلي بين توقيع القاعدة وتوقّع التطبيق كان سيظهر كفساد دقة صامت في المتصفح لأي قيمة عالية الدقة. **أُصلِح مباشرة في نفس الترحيلة 0117** (لم تُسلَّم بعد لأي بيئة إنتاج — لا حاجة لترحيلة تصحيحية منفصلة) بإضافة `::text` عند الإرجاع وتغيير توقيع الإرجاع إلى `text`؛ أُعيد بناء قاعدة الاختبار بالكامل من الصفر وإعادة تشغيل **كل** ملفات الاختبار (SQL + تزامن + ترقية + HTTP) بعد الإصلاح — كلها نجحت.
- الأرقام الفعلية المؤسِّسة من `seed.sql` (SMSA/RETURN/RIYADH=17.00، رسوم إرجاع الرياض=35.00) تُحَل بدقة عبر HTTP فعلي، لا افتراضًا من الترحيلة وحدها.
- التسعير اليدوي (Section 17): لا تسعير `outbound` مُهيَّأ لأي شركة/منطقة — `preview_shipment_expected_cost()` تُعيد `found=false` بدقة، و`create_shipment()` تقبل تكلفة يدوية مع سبب إلزامي (`expected_carrier_cost_is_manual=true`, `carrier_rate_version_id=null`).
- حماية الربح على مستوى القاعدة: `get_shipment()` تُخفي كل مفتاح مالي **تمامًا** (لا `null`) لممثِّل بلا `sales.view_profit`، بينما `list_shipments()` تُعيد `null` صريحًا لنفس الأعمدة — نفس نمط `get`/`list` القائم في Sales/Returns.
- دورة حياة الحالة: انتقال طبيعي ينجح بصلاحية `shipments.update_status` فقط؛ انتقال خارج التدفق (قفزة مباشرة إلى `delivered`) يُرفَض بلا سبب، ثم ينجح بعد توفير السبب وصلاحية `shipments.correct_status`.
- دفتر التكلفة الفعلية: تسجيل ← تصحيح — القيمة الحالية تعكس التصحيح الأخير فقط، والدفتر يحتفظ بالقيمة الأصلية كاملةً (إضافة-فقط حقيقية عبر HTTP فعلي).
- تصحيح رسوم الشحن على العميل: اللقطة الأصلية (`customer_shipping_charge`) تبقى بلا تغيير، بينما `effective_customer_shipping_charge` يعكس التصحيح — القيمة الأصلية لا تُستبدَل أبدًا.
- **حماية تدقيق دقيقة الحبيبات (0121)، مختلفة عمدًا عن نمط `return.%` الشامل:** طلب `GET /audit_logs?action=like.shipment.*` حقيقي يُظهِر `shipment.create`/`shipment.cost_record`/`shipment.cost_correct`/`shipment.charge_correct` **فقط** لممثِّل يملك `sales.view_profit`، بينما `shipment.status_add` (بلا رقم مالي) يبقى ظاهرًا لممثِّل بلا هذه الصلاحية — إثبات فعلي أن التصميم المتعمَّد (لا صلاحية شاملة بالبادئة، بل قائمة أفعال مالية صريحة) يعمل كما هو موثَّق في الترحيلة 0121.
- التحقق من التسمية (`validate_money_scale()`): رسوم شحن على العميل بأكثر من رقمين عشريين تُرفَض عبر HTTP فعلي، بنفس الآلية المُعاد استخدامها من Sales/Returns.
- بوابات صلاحيات دوال البحث الضيقة (Section 38): `shipments_visible_store_lookups()` تنجح لممثِّل يملك `shipments.view` فقط؛ `shipments_carrier_lookups()`/`shipments_operable_store_lookups()`/إلخ تُرفَض لنفس الممثِّل رغم قدرته على رؤية الشحنات نفسها.

**إصلاح جانبي إضافي، غير متعلق بالمنطق التطبيقي:** ملف بيانات اختبار HTTP المشترك (`supabase/tests/postgrest_http_test_setup.sql`) كان يحتوي عيبًا كامنًا سابقًا (`INSERT ... ON CONFLICT DO UPDATE` بقيمتي تاريخ متطابقتين في نفس عبارة `VALUES` عند تساوي `current_date`/`business_today()`) — يظهر فقط في الأيام العادية (لا نافذة الثلاث ساعات التي كان التعليق الأصلي يحذّر منها فقط)، واكتُشِف فعليًا أثناء هذه الجلسة. أُصلِح بـ`SELECT DISTINCT` قبل الإدراج — إصلاح استقرار بنية اختبار بحت، بلا أي تغيير في منطق تطبيقي.

## 5) فحوصات الطبقة الأمامية (TypeScript/React) — نتائج فعلية

- `npx tsc --noEmit` → **صفر أخطاء** (شمل كل ملفات `src/features/shipping/*` و`src/app/(app)/shipments/*` الجديدة، و`src/types/database.ts` بعد إضافة 7 جداول/19 دالة جديدة).
- `npx eslint .` → **صفر أخطاء وتحذيرات** عبر المشروع بالكامل.
- `npx vitest run` → **52/52 ناجح عبر 7 ملفات** (بلا تغيير — Phase 5 لم يضف/يمسّ أي وحدة اختبار Vitest قائمة).
- `npm run check:numeric-types` → **نجح، 55/55 عمود NUMERIC مطابق** لِـ`number` في `database.ts` (يشمل 8 أعمدة جديدة عبر `shipments`/`shipment_financial_events`/`shipping_carrier_rate_versions`، كلها مطابقة لسلوك PostgREST الفعلي — لا `string` وهمي؛ لاحظ أن `expected_carrier_cost`/`fee_amount` في مخرجَي `preview_*` **ليسا** عمودين خامين — هما نتيجة دالة مُعادة كنص عمدًا، خارج نطاق هذا الفحص، ومُثبَتان بالفعل عبر اختبار HTTP في القسم 4 أعلاه).
- `npm run build` (Next.js/Turbopack) → **نجح**، توليد كل المسارات شاملةً ثلاثة مسارات Shipping الجديدة: `/shipments`، `/shipments/new`، `/shipments/[id]`.

## 6) خلاصة

**6/6 أقسام SQL جديدة** (`shipping_core_phase5.test.sql`) + **6/6 سيناريوهات تزامن حقيقية جديدة** (A–F، `shipping_core_phase5_concurrency.test.sql`) + **صفر أخطاء عبر كامل مجموعة اختبارات SQL القائمة (19 ملفًا، قاعدة بيانات واحدة طازجة 0001–0121)** + **إثبات أمان ترقية فعلي على بيانات بيع حقيقية** + **101 تأكيدًا HTTP/PostgREST حقيقيًا** (81 سابقة + 20 جديدة، شاملةً إصلاح عيب حقيقي في نقل الأرقام العشرية اكتُشِف بهذا الاختبار بالذات) + **52 اختبار Vitest** + **55/55 عمود NUMERIC** + بناء إنتاجي ناجح — كلها من تشغيلات فعلية في هذه الجلسة، لا أرقام مفترَضة أو منسوخة. **لم تبدأ Settlements ولا Services/Adjustments ولا Inventory ولا Reports/PDF/Excel ولا أي تكامل شركة شحن/Salla — العمل متوقف الآن نهائيًا، بانتظار المراجعة والموافقة من المستخدم بعد إرسال ZIP هذا التسليم.**
