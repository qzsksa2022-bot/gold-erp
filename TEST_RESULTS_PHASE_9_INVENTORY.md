# نتائج اختبارات Phase 9 — نواة المخزون (Inventory Core)

هذا الملف يوثّق **تشغيلات فعلية حقيقية** لاختبارات Phase 9 مقابل خادم
PostgreSQL 16.4 حقيقي. يستبدل العبارة السابقة في `DELIVERY_REPORT.md`
(الملحقان 38 و39) بأن اختبارات SQL «مكتوبة بالكامل لكن **لم تُشغَّل فعليًا**»
لعدم توفر خادم Postgres في بيئة التنفيذ حينها.

- **الترحيلات المشمولة:** 0227–0232
- **بيئة التشغيل:** PostgreSQL 16.4 (نسخة محمولة، Windows)، قاعدة نظيفة تُبنى
  من `local_harness_setup.sql` + كل الترحيلات + `supabase/seed.sql`

---

## 1) اختبارات SQL الخاصة بـPhase 9

| # | الاختبار | النتيجة | التفاصيل |
|---|---|---|---|
| 1 | `supabase/tests/inventory_core_phase9.test.sql` | **PASSED** (exit 0) | 8 أقسام: حدود الصلاحيات لكل مفتاح، منع الرصيد السالب، حدود نطاق الفروع، التزامن التفاؤلي بـ`row_version`، القراءة المُقسَّمة صفحيًا، سجل التدقيق، وعزل القفل الاستشاري (0230) |
| 2 | `supabase/tests/inventory_core_phase9_concurrency.test.sql` | **PASSED** (exit 0) | جلستان حقيقيتان عبر `dblink`: رصيد 10، محاولتا سحب -6 متزامنتان |
| 3 | `supabase/tests/upgrade_phase9_inventory.test.sql` | **PASSED** (exit 0) | البناء حتى 0226 + `seed.sql` الحقيقي، ثم 0227–0232 فوقها في استدعاء `psql` منفصل |

### تفصيل اختبار التزامن الحقيقي

السيناريو: رصيد ابتدائي 10 وحدات، محاولتا سحب −6 متزامنتان عبر اتصالَي
`dblink` منفصلين — كل محاولة صالحة منفردة (10−6=4) لكنهما غير صالحتين معًا
(10−6−6=−2).

المُثبَت فعليًا:

- المحاولة B **حُجبت فعليًا** (`dblink_is_busy`) طالما بقيت معاملة A مفتوحة
  تحمل القفل الاستشاري لنفس زوج (صنف، فرع).
- بعد التزام A، أُعيد تقييم B على الرصيد **الفعلي بعد A** (4) لا على قراءة
  قديمة (10)، فرُفضت رفضًا صحيحًا.
- التحقق النهائي عبر `SUM()` حيّ من الدفتر: الرصيد = **4**، وعدد صفوف الحركات
  = **2** بالضبط (الاستلام الأولي + حركة A) — **لا صف لمحاولة B المرفوضة**.

النتيجة الحرفية: `PASS A: negative-stock race — A's outbound (10->4) commits,
B's concurrent outbound is genuinely serialized and correctly rejected once
re-evaluated against the post-A balance (never both accepted)`.

---

## 2) عيبان حقيقيان في المنتج اكتُشفا بالتشغيل الفعلي

كلاهما يُطبَّق بنجاح تام كـSQL، ولا ينكشف إلا عند **الاستدعاء الفعلي** — وهو
بالضبط ما لم تكن CI تفعله.

| # | العيب | الأثر | الإصلاح |
|---|---|---|---|
| 1 | سياسة `inventory_stock_movements_select` (0228) تستدعي `user_visible_store_ids(auth.uid())`، وهي ممنوحة لـ`service_role` فقط (0017) | **كل** `SELECT` مباشر على الجدول من مستخدم `authenticated` يفشل بـ`permission denied for function` | **0231** — التحويل إلى `my_visible_store_ids()` (الغلاف الذاتي القابل للاستدعاء)، بنفس دلالات النطاق تمامًا |
| 2 | `update_inventory_item()` (0229) تشير إلى `id`/`row_version` بلا تأهيل مقابل معاملات `RETURNS TABLE` الخاصة بها | `column reference "id" is ambiguous` في **كل استدعاء** — الدالة كانت غير عاملة 100% | **0232** — تأهيل الجداول بأسماء مستعارة (`i`/`ii`) |

**ملاحظة:** الترحيلة 0059 (`daily_closings_select`) تحمل تحذيرًا حرفيًا من
الخطأ الأول نفسه؛ 0228 أعادت إدخاله.

---

## 3) منع `row_version` بقيمة NULL — داخل منطق المنتج، لا في الاختبار

`update_inventory_item()` كانت تقارن `v_old.row_version <> p_expected_version`.
مقابل `NULL` تكون النتيجة `NULL` لا `TRUE`، فيتخطى المتصل الذي يُغفل الإصدار
**فحص التزامن التفاؤلي بالكامل** ويكتب فوق ما التزمت به جلسة أخرى.

الإصلاح في **0232** (منطق المنتج):

```sql
if p_expected_version is null then
  raise exception 'رقم إصدار الصنف (row_version) مطلوب للتعديل' using errcode = 'P0001';
end if;

if v_old.row_version is distinct from p_expected_version then
  raise exception 'تم تعديل هذا الصنف من قِبل مستخدم آخر، ...' using errcode = 'P0001';
end if;
```

### إثبات أن الحارس مُغطّى فعليًا باختبار (Mutation Test)

| الحالة | النتيجة |
|---|---|
| 0232 كما هي (الحارس موجود) | `inventory_core_phase9.test.sql` **PASSED** |
| إزالة الحارس من **منطق المنتج فقط** (الاختبار بلا تعديل) | **FAILED**: `TEST FAILED: a NULL expected row_version bypassed the optimistic-concurrency check` |
| إعادة تطبيق 0232 | **PASSED** |

أي أن الاختبار يفشل فعليًا بغياب الإصلاح — ليس تأكيدًا مكتوبًا حول سلوك ناجح
أصلًا.

**نطاق مقصود:** نمط `<>` نفسه موجود في دوال تحديث أقدم (0075/0086/…)، لكن تلك
الترحيلات مجمَّدة وخارج نطاق Phase 9، فلم تُمس.

---

## 4) نتائج طبقة TypeScript

| الفحص | الأمر | النتيجة |
|---|---|---|
| Vitest | `npx vitest run` | **489/489 ناجح** عبر 35 ملفًا |
| TypeScript | `npx tsc --noEmit` | **صفر أخطاء** |
| ESLint | `npx eslint .` | **صفر أخطاء** (4 تحذيرات قائمة مسبقًا في `adjustments-entry-form-zero-charge-state-machine.test.tsx`، لم تُمس) |
| البناء الإنتاجي | `npx next build` | **نجح** — **51/51** صفحة |

---

## 5) حالة GitHub CI

**المصدر: مالك المستودع، عبر واجهة GitHub مباشرة.** بيئة تنفيذ الوكيل لا
تحتوي `gh` CLI ولا رمز وصول (`GITHUB_TOKEN`/`GH_TOKEN`)، فهذه النتائج
**مَنقولة عن تحقُّق المالك، لم يتحقق منها الوكيل بنفسه**.

| التشغيل | المرجع | النتيجة |
|---|---|---|
| Run #14 | الـcommit `ee4a5af` | **نجح** خلال 1m32s |
| Run #15 | الخاص بالـPR | **نجح** خلال 2m4s |
| Run #16 | بعد دمج Phase 9 إلى `main` | **نجح** |

الـcommit `ee4a5af` (الذي أضاف 0231/0232 ووظيفة `sql`) دُمج إلى `main` عبر
PR #2 ضمن الدمج `929fb40`.

**تمييز مهم:** كل النتائج في الأقسام 1–4 أعلاه من **تشغيلات محلية فعلية**
للوكيل مقابل PostgreSQL 16.4 حقيقي. أرقام CI في هذا القسم وحدها هي المنقولة
عن المالك.

---

## 6) المجموعة الكاملة لاختبارات SQL (Hotfix 9.1.0)

بعد ربط **كل** اختبارات SQL بالـCI:

| الفئة | العدد | النتيجة (تشغيلان كاملان من قواعد نظيفة) |
|---|---|---|
| اختبارات عادية | 28 | **28/28** في كلا التشغيلين |
| اختبارات تزامن (dblink، جلستان) | 6 | **6/6** في كلا التشغيلين |
| اختبارات ترقية (runners) | 14 | **14/14** في كلا التشغيلين |

كل اختبار يعمل على **نسخة نظيفة** من قاعدة template (`CREATE DATABASE …
TEMPLATE`)، فلا يرى أي اختبار أثر غيره.

### `settlements_phase7_concurrency.test.sql` — من متقطّع إلى حتمي

كان يفشل **3 مرات من 6** عند القسم B. السبب: المزامنة كانت مبنية على
`dblink_is_busy()`، وهي لا تفرّق بين «الجلسة الثانية محجوبة فعلًا على قفل
الأولى» و«لم تبدأ بعد» — فكان السيناريو قد يمر بلا أي تداخل حقيقي.

الإصلاح **داخل الاختبار فقط** (لا مساس بمنطق المنتج):

1. استدعاء A صار **متزامنًا** (`dblink()` بدل `dblink_send_query()`) — عند
   عودته يكون INSERT الخاص بـA قد نُفِّذ فعلًا، ومعاملته ما تزال مفتوحة.
2. أُضيف `_p7cc_wait_blocked_by(pid, blocker)` المبني على
   **`pg_blocking_pids()`**: ينتظر حتى تُرصد جلسة B **محجوبة على backend جلسة
   A تحديدًا**. هذا يُثبت الأمرين معًا: أن المعاملتين تداخلتا زمنيًا، وأن B
   منتظرة خلف قفل A — **قبل** تحرير A.
3. التأكيد النهائي صار على B مباشرة (`assert v_b_failed`) بدل «إحدى
   المحاولتين فشلت» — أقوى، ولا يمكن إرضاؤه بفشل A لسبب غير متصل.

انتظار محدود بميزانية زمنية يفشل بصوت عالٍ إن لم تتحقق الحالة — **ليس** إعادة
محاولة حتى النجاح، والعملية تحت الاختبار لا تُعاد أبدًا.

**النتيجة: 20/20 تشغيلًا متتاليًا ناجحًا** (كل تشغيل على قاعدة نظيفة).

### إثبات أن النجاح ليس شكليًا (اختبارا طفرة)

| الطفرة | النتيجة |
|---|---|
| حذف حارس التعارض `settlement_source_claims_active_unique_idx` | **فشل** — B لم تُرصد محجوبة على قفل A |
| كسر التزامن: التزام A **قبل** إرسال B (تشغيل تسلسلي بلا تداخل) | **فشل** — B لم تُرصد محجوبة على قفل A |

الطفرة الثانية مهمة تحديدًا: **النسخة القديمة كانت ستمر بها** (لأن B تفشل عند
الفحص المسبق، فيتحقق شرط «إحدى المحاولتين فشلت»)، بينما النسخة الجديدة ترفضها
لأنها تطالب بتداخل حقيقي مُثبَت.

### الاختبار اليتيم

`upgrade_phase8_reports_dashboard.test.sql` لم يكن له أي runner في `scripts/`،
فلم يكن يُنفَّذ في أي مكان. خط الأساس مأخوذ من ترويسة الملف نفسه حرفيًا
(«0199-0204 on top of the FROZEN 0001-0198 baseline (§0)») لا اعتباطًا: أُنشئ
`scripts/run_upgrade_test_phase8_reports_dashboard.sh` (خط أساس **198**، بلا
أي pre-fixture — وهو مقصود: الاختبار يُثبت أن الـ21 RPC آمنة مقابل قاعدة لم
تشغّل تثبيتة Phase 8 إطلاقًا). النتيجة عند التشغيل الفعلي: **PASSED**
(19 تأكيدًا).

أُضيفت أيضًا خطوة **SQL test coverage** في CI ترفض وجود أي `*.test.sql` بلا
مسار تنفيذ واضح، مع استثناء صحيح للسكربت السلبي المقصود
(`run_upgrade_test_hotfix_7_1_3_broken_fixture.sh`) الذي يُثبت فشل ترحيلة ولا
يشغّل ملف اختبار أصلًا.

---

## 7) فجوة معروفة مُصرَّح بها

`scripts/run_upgrade_test_phase9_inventory.sh` لم يُشغَّل كسكربت على هذه
الآلة (لا يوجد `bash`: نسخة Git المثبَّتة هي MinGit بلا صدفة). المُنفَّذ محليًا
هو **تسلسل `psql` المطابق حرفيًا** لخطوات السكربت. مسار السكربت نفسه يُغطّى
في وظيفة `sql` داخل GitHub Actions على Ubuntu.
